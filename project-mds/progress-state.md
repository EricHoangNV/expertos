# Progress

## Current State

Phase 1 (MVP) is functionally launch-ready. All in-sandbox-codeable review findings (Security Cycle 2 + Product Cycle 1) are REMEDIATED/ADDRESSED, pending re-review. Remaining open `[ ]` items are externally blocked (host E2E run, PM/legal sign-offs, schema decisions).

- Phase 0 + Phase 1 backend/admin/expert (M1–M11): COMPLETE.
- M12 (consumer web `/chat` UI): COMPLETE (M12.1–M12.9).
- M13 (UI i18n EN+VI): COMPLETE. Web + admin own `LocaleProvider`/`useT`; system content locale-aware; persist via `PATCH /me/locale`.
- M13 (admin/expert portal UI): DONE except voice-dimension widgets (M13.5.3/.4/.5, M13.5.6, M13.7.4 `.voice-bar`) — DEFERRED, no schema backing (PM/schema decision).
- M14 (access-control whitelist): COMPLETE.
- M15 (test coverage): M15.1 web jest + M15.2 admin jest COMPLETE; M15.3 E2E done in-sandbox (M15.3.1–.6); M15.3.7 = negative-space expansion (8 new tests, below).
- M16 (per-expert TidyCal calendar): COMPLETE in-sandbox (M16.1–.7). TidyCal has **no native webhooks** → per-expert **polling** of `GET /bookings` with each expert's AES-256-GCM-encrypted API token; `TidyCalProviderFactory`, `Consultation.expertId` attribution, expert/admin `calendar-settings` API + admin UI, backfill CLI. Webhook machinery deleted; offline-envelope seam disabled in prod. Host offline-E2E leg pending. LEARNINGS #36.
- Real LLM chat drivers landed (OpenAI/Anthropic/Gemini, shared SSE seam, env-selected, Echo fallback). SSE keep-alive guard (LEARNINGS #24, DIRECTIVES #41).
- M17 (runtime answer-tuning settings + real embedding provider): IN PROGRESS. **M17.1 + M17.2 + M17.3 + M17.4 + M17.5 + M17.6 DONE; M17.7 cutover validated locally, prod deploy/run/verify pending.** M17.7 (2026-06-05): dry-ran the full cutover on the local stack — `.env` `EMBEDDING_PROVIDER=openai` → dry-run reports `text-embedding-3-small (1536 dims)` → `reembed --commit` rewrote **2676 chunks, 0 failed** at default `--batch=256`. Fixed 2 defects it surfaced: (1) reembed CLI default 256-batch overran Prisma's 5s interactive-tx timeout (partial write → mixed vector space) → added 30s tx timeout in `reembed.cli.ts` (commit `ae7e234`, pushed; api 800 still green); (2) host-DB drift — M17.1 `app_settings` migration unapplied on host → admin Settings page 500, fixed via `prisma migrate deploy` (deploy pipeline must run this). Prod cutover (deploy + prod env+restart + prod `reembed --commit` + 4 verifications) still pending — the external gate. M17.6 = `OpenAiEmbeddingProvider` (`packages/ai/src/embedding/openai-embedding-provider.ts`, `text-embedding-3-small`, 1536-dim, ≤256-batch + index-reassembled order, reuses `defaultFetch`/`LlmRequestError`); `createDefaultEmbeddingProvider(env)` gated on `EMBEDDING_PROVIDER=openai`+`OPENAI_API_KEY` (throws if flag set but keyless — fail loud), default stays `HashingEmbeddingProvider`; re-embed CLI `apps/api/src/ingestion/reembed.cli.ts` (mirrors `bulk-publish`: dry-run + `--commit`, admin RLS spanning all tenants, re-embeds each chunk's stored `content`, `UPDATE chunks SET embedding`, `--batch`/`--limit`) + `"reembed"` npm script + knip entry. M17.5 = admin `apps/admin/app/settings/page.tsx` (cloned `concierge/page.tsx`): editable temp (0–2 step .05), default chat model `<Select>` (gpt-4o-mini | gpt-4o), retrieval score floor (0–1 step .005 + RRF helper text); embedding provider read-only with `EMBEDDING_PROVIDER` restart note. `getAppSettings`/`updateAppSettings` in `admin-client.ts` (API from M17.2, types from M17.1); new `dictionaries/settings.ts` (EN+VI) + `nav.settings` in `common.ts`; nav item in `AdminFrame` (SYSTEM, admin-only); directive #38 label association (htmlFor+id). i18n lockstep test auto-covers the namespace. M17.1 = `AppSettings` Prisma model (global singleton, no RLS) + migration + `packages/shared/src/app-settings.ts`. M17.2 = `apps/api/src/settings/` `SettingsService` (`getSettings`/`updateSettings` upsert+audit-in-tx; `getCached()` 30s TTL snapshot, bust-on-update) + admin GET/PATCH controller + `SettingsModule`. M17.3 = `LlmCallOptions {temperature?,model?}` on `LlmProvider` (`packages/ai`); OpenAI/Anthropic/Gemini/Echo honor it (temp omitted when unset; Gemini rebuilds URL per-call); `chat.service.ts` standard tier reads `getCached()` → `{temperature,model}`, `effectiveModel` (override ?? `llm.name`) drives cache key/persist/usage/log; degraded tier untouched. M17.4 = `minScore?` on `RetrievalRequest` (the fused RRF score, not cosine) → filtered after `fuseHybrid` in `pgvector.store.ts` when `> 0`; `RetrievalService` reads `getCached().retrievalScoreFloor`, threads it as `minScore` **and** forks `retrievalKey(...,minScore=0)` (directive #43); `chat.service`/answer-cache deliberately untouched (degraded-tier-untouched + same bounded staleness as temperature; trust invariant unaffected). Next: M17.5 Admin Settings page → M17.6 OpenAI embedding provider + re-embed CLI → M17.7 cutover.
- M18 (uploaded document management / "My Knowledge"): COMPLETE. `GET /uploads` + `DELETE /uploads/:id` (RLS-scoped, **NOT** entitlement-gated — a downgraded user keeps see+delete; only `POST` keeps the `document_upload` guard), best-effort blob reclaim on delete (directive §46), cascade chunks. `apps/web/app/knowledge/page.tsx` Saved/Temporary sections + sidebar entry point + `knowledge` i18n namespace (EN+VI); M18.3.2 mockup design-parity (`.umain`>`.pagehead`/`.eyebrow`, file icon, `.h3` filename, `.mono` size, right-aligned ghost Delete). E2E `web-my-knowledge.spec.ts` (upload→navigate→appears→delete→gone). Functional core shipped in `d6dad4a`; this loop added 3.2 design-parity + 4.1 E2E + reconciled the manifest/notes.
- M19 (mockup design-parity, 24 tasks): **COMPLETE.** DONE: M19.1.1 history, M19.1.2 account, M19.2.1 knowledge-detail, M19.2.2 knowledge-drafts, M19.2.3 knowledge-draft-detail, M19.2.4 experts, M19.2.5 expert-detail, M19.2.6 voice-profile-detail, M19.3.1 concierge-settings, M19.3.2 concierge-analytics, M19.3.3 answers, M19.3.4 failed-queries, M19.3.5 validation, M19.4.1 analytics, M19.4.2 funnel, M19.4.3 conversions, M19.4.4 recommendation-rules, M19.4.5 reconcile, M19.4.6 retention, M19.5.1 users, M19.5.2 user-detail, M19.5.3 access-control, M19.5.4 audit, **M19.5.5 settings**. M19.5.5 (settings, screenshot 24, verify+polish on M17.5): `SettingsEditor` now owns the `.pagehead` so a primary Save sits top-right (mirroring M19.3.1 concierge); the four `Field`s wrapped in a `.card .card-pad` panel (secondary bottom subtle Save kept below); the score-floor `Tooltip` wrapped in a `<span className="chip">` + trailing `scoreFloorGuide` word → the "ⓘ GUIDE" pill beside the label. One new i18n key `scoreFloorGuide` (EN "Guide" / VI "Hướng dẫn"), lockstep test auto-covers. All 24 M19 tasks done.
- Tests (unit/jest): 1724 pass / 0 fail (shared 202, ui 252, db 9, ai 210, api 809, web 113, admin 129).
- E2E (Playwright, host): collects 37 tests / 19 files (+`web-my-knowledge.spec.ts`). Last full host run green at 20 pass / 3 fixme; the added tests are tsc+eslint+collection clean, host-run pending.
- Security Cycle 2 (5) + Product Cycle 1 (High+3 Med) + Security Cycle 3 (1 Med) — all REMEDIATED in-sandbox, pending re-review. DIRECTIVES #42-48; LEARNINGS #26-34. (Detail in progress-log.)
- Security Cycle 4 + Product Cycle 2 (shared Critical+High+Med) — REMEDIATED in-sandbox: bulk-publish review-gate bypass (deleted `publish-drafts`; new `bulk-publish` CLI + shared `publishReviewedVersionTx` gate, `expert_review`-only, break-glass+approver+audit); TidyCal decrypt-fallback now skips+alerts (`failedTargets`) instead of polling the global calendar; bulk publish clears semantic cache. DIRECTIVES #50/#51; LEARNINGS #37/#38. Verdicts stay FAIL pending re-review.
- M15.3.7 E2E negative-space expansion: +8 tests (RLS isolation, citation trust-invariant, upload 402, concierge disclosure, conv→knowledge, voice publish, whitelist revocation, i18n persistence) + 3 fixtures. LEARNINGS #32 / DIRECTIVES #48.
- Gates: per-workspace build+eslint+jest, app tsc+next lint, root lint:css+knip clean. `next build` + `tsx` seed arch-blocked in-sandbox (LEARNINGS #25).
- **Sandbox cannot run live E2E** (4GB OOM + network policy blocks host.docker.internal); host runs Playwright (DIRECTIVES §3.4.1, LEARNINGS #22).
- **NT.3/NT.4 drafts in-repo** (`docs/legal/`): retention/deletion + high-stakes disclaimer copy; tasks stay `[~]` pending PM/legal sign-off (human gate).

## Next tasks
0. **M17.7 PROD cutover + verification** (external deploy/run gate — procedure now validated locally): deploy `main` (incl. reembed tx-timeout fix `ae7e234`) — **ensure the deploy runs `prisma migrate deploy`** (the M17.1 `app_settings` migration must land or Settings + answer path 500) → set `EMBEDDING_PROVIDER=openai` + `OPENAI_API_KEY`, restart → `pnpm --filter @expertos/api reembed -- --commit` over all chunks (default `--batch=256` now safe) → restart/await 30s cache TTL → verify a paraphrased query retrieves the right note, usage log shows `text-embedding-3-small`, temp/floor effective within 30s. M17.1–M17.6 done; cutover dry-run + full local reembed (2676 chunks, 0 failed) green.
1. **Host-run the E2E suite** incl. the M15.3.7 tests via `scripts/test-e2e-{users,admin}.sh` (expect ~28 pass / 2 fixme); fix any selector drift. Add the **M16 offline-E2E leg** (per-expert booking sync: offline envelope → consultation booked + attributed → cancel) and run `backfill-tidycal` on deploy + set `CREDENTIALS_ENCRYPTION_KEY`.
2. **Await Security Cycle 2 + 3 + 4 + Product Cycle 1 + 2 re-review** — all REMEDIATED/ADDRESSED in-sandbox.
3. **M19 — mockup design-parity pass (9 open tasks, one screen per loop).** Each restyles an already-built admin/web page to its single screenshot (`requirements/design/screenshots/pages/NN-*.png`) wired to the real client/DTO — not new features. Batches 1–3 done (incl. M19.3.5 validation); next are Batch-4 growth/revenue (M19.4.3 conversions, .4 recommendation-rules, .5 reconcile, .6 retention) + Batch-5 platform (M19.5.1–.5 users/user-detail/access-control/audit/settings); keep ExpertOS branding, add EN+VI keys per task. New reusable ds.css helper `.table .num` (right-align numeric column) is available for other table screens.
4. **M13.5.3/.4/.5 + M13.7.4** voice-dimension widgets — BLOCKED on PM/schema decision; don't invent fields.
5. **M11.4 / NT.3-6** PM/legal sign-offs. (deferred) Phase 2/3 + OD#6.

## Reference (detail in progress-log.md)
- i18n core, web/admin test harnesses, ds.css primitives: progress-log M13.1/M15.1.1/M15.2.1.
- Rebuild `packages/ui` after changing it (apps consume `dist/`; ds.css ships from `src/`).
- E2E host-run recipe + NEXT_PUBLIC build-time gotcha: LEARNINGS #32, `scripts/test-e2e-*.sh`.
