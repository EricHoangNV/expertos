# Progress

## Current State
- Completed:
  - **M8.3 (partial) — Admin revenue reports** (§"Admin & Expert portals" → "Revenue: transaction ledger + basic revenue reports"; PRD M8.3). **First slice of M8.3** (the riskiest sub-deliverable — new module + aggregation queries — taken first per fail-fast; the matrix editor / recommendation-rules editor / failed-query inspector remain open). New **`apps/api/src/revenue/`** module the PRD names. **`RevenueService.report(user, {months})`** is the single read-only choke point behind `GET /admin/revenue/report` (`@Roles("admin")`); it runs inside `RlsService.run` under an **admin** principal, so the `is_admin` GUC makes every read **platform-wide across all tenants** (the conversation-search cross-tenant pattern — no `tenant_id` predicate appears). Three sources combined: (1) **MRR + per-plan + active subscribers** — live (`active`/`trialing`) `subscriptions` `groupBy [planId, interval]`, priced from `plan_prices` and **normalized to a monthly amount** (yearly ÷ 12, rounded); a (plan, interval) with no configured price contributes 0 MRR but still counts as an active subscriber. byPlan is highest-tier-first. (2) **Trailing monthly ledger series + window totals** — `transactions` bucketed by `date_trunc('month', occurred_at)` via raw `$queryRawUnsafe` (the M3.3 search precedent — `date_trunc`/`FILTER` have no Prisma Client expression): gross = succeeded non-refund; refunded = refund-type **or** refunded-status amounts (`abs()`, sign-agnostic); net = gross − refunded. **Postgres `sum`/`count` come back `BigInt` → coerced with `Number()` in the mapper** (the gotcha that the `ts_rank` float path didn't hit). Window totals are derived by summing the series. (3) **AI cost / margin** — `usage_logs.cost_micros` summed over the window (M6.5), `marginCents = netCents − round(aiCostMicros / 1_000_000)`. The window start is the first day of the month `months − 1` months back (UTC), bound identically into the period SQL and the cost aggregate. New shared `revenueReportQuerySchema` (`months` z.coerce 1..36 default 12) + `RevenueReportDto`/`RevenueByPlanDto`/`RevenuePeriodDto` (`packages/shared/src/revenue.ts`; money in integer **cents**, AI cost stays in `cost_micros` so it never loses precision crossing the wire). **Admin UI:** new **`apps/admin/app/revenue/page.tsx`** (Stat cards: MRR, active subscribers, net revenue, AI cost, gross margin trend; By-plan + By-month tables) on the existing `AdminFrame`; new `getRevenueReport` in `admin-client.ts`; nav entry + home card added. **Decisions:** (1) **revenue is read-only here** — it's mirrored into `subscriptions`/`transactions` by the M6.2 billing webhook; this module only aggregates, so no provider import; (2) MRR via Prisma `groupBy` (testable) + raw SQL only for the month time-series (the part Prisma can't express); (3) `abs()` on refunds so a stored sign convention can't flip the total. **Seam-tested with a mocked tx** — the real `date_trunc`/`FILTER` aggregate + the BigInt round-trip + the admin cross-tenant RLS visibility join the M11 Testcontainers list (the raw-SQL caveat shared with `PgVectorStore`/conversation-search). All gates green (typecheck/lint/knip/build); full `apps/api` suite ran clean this session (446); `revenue.service.ts` = 100% all metrics; +5 service tests (`revenue.service.test.ts`) + 4 shared schema tests (`revenue.test.ts`).
  - **M8.1 + M8.2 (Admin UI)** — Knowledge publish-workflow + conversation-to-knowledge draft review portal (§"Admin & Expert portals"; §"Design System"; PRD M8.1, M8.2). **Closes the UI halves of M8.1 + M8.2** — both APIs shipped earlier; `apps/admin` was a bare scaffold. Built the publish-workflow admin/expert portal on the existing routes. **Auth + client layer (mirrors `apps/web/src/lib`):** `apps/admin/src/lib/firebase.ts` (lazy Firebase client, `NEXT_PUBLIC_*` only) + `auth-context.tsx` (`AuthProvider`/`useAuth`, Google sign-in, `getIdToken`) copied from web; new **`admin-client.ts`** — a thin typed fetch wrapper (Bearer ID token; surfaces the API's `{message}`/`{reason}` error body) over `GET /knowledge/documents` (status filter), `GET /knowledge/documents/:id`, `POST /knowledge/versions/:id/{submit,approve,request-changes,archive}` (M8.1) and `GET /knowledge-drafts` (status filter), `GET/PATCH /knowledge-drafts/:id`, `POST /knowledge-drafts/:id/{submit,request-changes,reject,publish}` (M8.2); new **`status-tone.ts`** — single source mapping `publish_status`/`KnowledgeDraftStatus` onto semantic `.badge` tones (published→green, expert_review→amber, rejected→red, neutral→ink/info) so the two queues never drift. **Frame:** new **`AdminFrame`** component renders the Design-System `.shell` (ink sidebar of review queues via `Shell`/`Topbar`/`Content` primitives, active-route highlight) and gates on Firebase auth (sign-in screen when signed out — the API still enforces the `expert`/`admin` role + tenant RLS, so this is a UX gate, not the security boundary). **Pages:** `/` landing (queue cards); **`/knowledge`** M8.1 review queue (status-filtered table → badge-toned status, version count, links to detail); **`/knowledge/[id]`** version-history detail (per-version status badge + `live` marker, status-appropriate Submit/Approve/Request-changes/Archive buttons); **`/knowledge-drafts`** M8.2 draft queue (status-filtered table); **`/knowledge-drafts/[id]`** draft detail/edit (title+content editable only while `draft`, Save via PATCH, status-appropriate Submit/Publish/Request-changes/Reject buttons). Added `@expertos/shared` + `firebase` deps to `apps/admin/package.json`. **Decisions:** (1) mirrored the web `firebase.ts`/`auth-context.tsx` verbatim (same lazy-init pattern keeps `next build` working without creds) rather than extracting a shared package — the two are independent Next apps with their own bundles; (2) status→tone in one `status-tone.ts` helper (not inline per page) so the badge semantics are single-sourced; (3) **"Mark valuable" (`POST /knowledge-drafts`) deliberately NOT surfaced yet** — it's an action from a conversation viewer, and no admin conversation viewer exists (that's M8.5/M8.3 scope); building an unused client fn would fail knip. **No page tests** (consistent with `apps/web` — no UI page tests in the repo; jest `--passWithNoTests`); typecheck/lint/build/knip all green for `apps/admin` (build emits all 6 routes). Test counts unchanged (683; purely additive frontend, no backend/shared code touched).
  - **M8.2 (API)** — Conversation-to-knowledge pipeline (§"Admin & Expert portals"; PRD M8.2). Captures a valuable chat answer as a `knowledge_drafts` row and drives it through its **own** review lifecycle (mirroring the M8.1 document gate but for free-text drafts, not uploaded files): `draft → expert_review → published` (`request-changes` returns to `draft`; `draft|expert_review → rejected` discards). New **`KnowledgeDraftService`** (`apps/api/src/knowledge/knowledge-draft.service.ts`) is the single draft-lifecycle choke point — `create` ("Mark valuable" — captures title/content/language, optionally tied to a source conversation **re-checked under RLS** so a foreign/forged conversationId 404s), `list`/`get` (review queue + detail), `update` (edit title/content **only while `draft`**), `submit`/`requestChanges`/`reject` (status moves), and **`publish`** = the gate. **`publish` ingests the draft's text into the knowledge base via `IngestionService.ingest({publish:true})`** — each draft maps to a unique `draft://<id>` source URI so a fresh published `document` (+ retrieval-visible chunks) is created with no prior generation to supersede; the step is **idempotent** (it pre-checks for an already-ingested `draft://<id>` document and skips re-ingestion, so a crash between ingest and the status flip can be retried without retrieval drift). An empty-document ingest failure maps to `400` (draft stays unpublished). New **`KnowledgeDraftController`** (`@Roles("expert")` — admin satisfies via hierarchy; tenant isolation is structural RLS) exposes `POST /knowledge-drafts`, `GET /knowledge-drafts`, `GET/PATCH /knowledge-drafts/:id`, `POST /knowledge-drafts/:id/{submit,request-changes,reject,publish}`. New shared `knowledgeDraftCreate/Update/ListQuerySchema` + `knowledgeDraftStatusSchema` + `KnowledgeDraftSummaryDto`/`KnowledgeDraftDto` (`packages/shared/src/knowledge.ts` — list is content-free, detail carries content). New migration `20260601050000_knowledge_draft_language` adds a `language` column to `knowledge_drafts` (default `en`) so a VI draft publishes as VI knowledge. **Decisions:** (1) the knowledge_draft's **own** review IS the expert-review gate (its `KnowledgeDraftStatus` enum mirrors the publish lifecycle) — publish ingests with `publish:true` (a fresh, unique-URI document, so no supersede needed; consistent with the seed/CLI publisher precedent), rather than routing through `KnowledgeService.approve` (which needs an `expert_review` *document* version — awkward to drive programmatically); (2) idempotent publish via the `draft://<id>` existence pre-check guards the only drift risk (a partial-failure retry); (3) **API + persistence only — admin UI pending** (same precedent as M8.1/M3.2). **Seam-tested with a mocked tx** (the real `knowledge_drafts`/`document` writes join the M11 Testcontainers list; no live DB this session, but the migration SQL is a single additive enum column mirroring the init-schema pattern). All gates green; `knowledge-draft.service.ts` covered by 18 service tests + 11 shared schema tests.
  - **M8.1 (API)** — Knowledge versioned-publish + expert-review gate (§"Admin & Expert portals"; PRD M8.1). **Starts M8.** The architectural core of the knowledge lifecycle: the publish workflow that sits on top of the M1.1 ingestion pipeline (which already stores an uploaded document as a `draft` version with `pending`, non-retrievable chunks via `DocumentVersionRepository.store({publish:false})`). New `apps/api/src/knowledge/` module. **`KnowledgeService`** is the single publish-workflow choke point — a state machine over the shared `publish_status` enum: `draft → expert_review → published` (`request-changes` returns to `draft`), `published → archived` (retire a live version). **The expert-review gate is `approve`** — and it is the integration point with M1.2 retrieval (which filters `chunks.status = published`): publishing (1) flips the version's chunks `pending → published` (now retrieval-visible), (2) points the parent `document.publishedVersionId` at it + sets `document.status = published`, and (3) **supersedes the document's previously-published version** by archiving its row AND its chunks — so retrieval never returns two generations of the same document at once. `archive` reverses this for a live version (chunks → archived; clears the document pointer + marks it archived when it was the live one). All transitions stamp via `RlsService.run` (tenant isolation is structural RLS, directive §4.21); `approve` stamps `approvedBy`/`approvedAt`. **`KnowledgeController`** (`@Roles("expert")` — admin satisfies via the hierarchy; the same role gate as the M2.3 voice-profile portal) exposes `GET /knowledge/documents` (review queue — filter by status/scope), `GET /knowledge/documents/:id` (full version history), and `POST /knowledge/versions/:versionId/{submit,approve,request-changes,archive}`. New shared `knowledgeListQuerySchema` + `KnowledgeVersionDto`/`KnowledgeDocumentDto`/`KnowledgeDocumentDetailDto` (`packages/shared/src/knowledge.ts`) — DTOs in shared so the M8.1 **admin UI** can consume them. **Decisions:** (1) `document.status` mirrors the latest version's status while `publishedVersionId` independently tracks the live generation — so a doc can have v2 live while v3 is a `draft` in progress; (2) the chunk-visibility flip + prior-version supersede live ONLY in `approve`/`archive` (the gate), never in ingestion — ingestion always produces drafts; (3) **upload-to-create-a-draft reuses the existing `IngestionService.ingest({publish:false})`** — no new ingest path; (4) **API-first, consistent with the codebase** (M2.3 routes shipped before the M8.5 portal): the **admin UI — the `.shell`, status `.badge` tones, publish queue — is the open M8.1 follow-up**, so M8.1 is marked `[~]` (API complete, UI pending). **Seam-tested with a mocked tx** (the real `document`/`document_version`/`chunk` writes + the chunk-visibility flip join the M11 Testcontainers list). All gates green; `knowledge.service.ts` 100% lines/stmts/funcs, 96.4% branch; 16 service tests + 4 shared schema tests.
  - **M7.3** — Resolve Open Decision #10: TidyCal webhook reliability / missed-event recovery (§"Consultation funnel"; §"Open Decisions" #10; PRD M7.3). **Closes M7.** M7.2 created the `consultations` row at Book-click (`status: recommended`) + returned the static TidyCal link; M7.3 wires the **booking-confirmation sync** that flips it to `booked` (records `bookingRef`/`scheduledAt`) when the user completes the booking — the booking analog of the M6.2 Stripe webhook, mirroring its discipline exactly. **Provider seam:** new **`TidyCalProvider`** interface (`apps/api/src/consultation/tidycal-provider.ts`) — no app code talks to TidyCal directly. **Offline default** `OfflineTidyCalProvider` (trusted-JSON envelope, no signing) keeps the whole book→webhook→sync + reconcile path runnable without TidyCal/network; the real **`HttpTidyCalProvider`** (HMAC-SHA256 raw-body verify via `node:crypto` + event parse + REST `listBookings` poll) swaps in behind the **`TIDYCAL_PROVIDER`** token when `TIDYCAL_WEBHOOK_SECRET` is set (`tidycal.defaults.ts` composition root, `TIDYCAL_API_TOKEN` enables the poll). A verified event normalizes onto a **`BookingEvent`** (`eventId`/`eventType`/`bookingRef`/`email`/`scheduledAt`/`status`); `statusForBookingEvent` maps created/rescheduled→`booked`, cancelled→`canceled`. **Service:** new **`BookingService`** (`booking.service.ts`, the coverage-gated brain): `handleWebhook(req)` verifies (→`400` on bad sig via `BookingWebhookVerificationError`→`BadRequestException`), parses (unmodeled type → silent no-op), and idempotently syncs in a **system RLS context** (`runAsSystem` = `applyRlsContext({tenantId:GLOBAL, isAdmin:true})`, mirroring `BillingService`); `reconcile({since?})` polls TidyCal (default 24h lookback) and replays each booking through the same idempotent apply = **missed-event recovery**. **Idempotency** = new **`booking_webhook_events`** ledger (migration `20260601040000`; RLS-exempt config/system table like `recommendation_rules`) keyed `@@unique([provider, eventId])` (pre-check + P2002 catch, the M6.2 `transactions` pattern); a reconcile entry uses a synthetic `reconcile:<bookingRef>:<type>` eventId so re-polling is idempotent. **Correlation** (the OD#10 concern — static links don't say which consultation): match by `bookingRef` first (a follow-up reschedule/cancel), then by booking **email** → the user's most-recent pending `recommended` consultation (the M7.2 row); a booking outside the funnel still **creates** a `booked` consultation so it never vanishes; an email matching no user is recorded **`matched=false`** (kept for admin reconcile, never dropped). **Controller:** new **`ConsultationBookingsController`** — `POST /consultation-bookings/webhook` (**`@Public()`**, reads `req.rawBody` + `tidycal-signature`, verified by signature not Firebase) + `POST /consultation-bookings/reconcile` (`@Roles("admin")`). New shared `bookingReconcileSchema` + `BookingReconcileInput`/`BookingReconcileResultDto`. **Decisions:** (1) offline-default + real-behind-a-token mirrors `PAYMENT_PROVIDER`/`STORAGE_PROVIDER`; (2) idempotency ledger as a dedicated table (not on `consultations`) because an unmatched booking can't create a consultation but still must be recorded (no-vanish); (3) the `consultations.status` itself is the user-facing pending state (`recommended`→`booked`→`canceled`). **Migration validated against live Postgres this session** (table + unique + indexes present, RLS-exempt, app_user grants); the real `booking_webhook_events`/`consultations` writes are seam-tested with a mocked tx (M11 Testcontainers caveat) + the REST poll needs live network (deploy-time, like the Stripe `FetchStripeHttpClient`). All gates green; `booking.service.ts` = 100% all metrics; offline + http drivers fully unit-tested.
  - **M7.2** — In-chat recommendation (Book / Maybe later / Ask another) + TidyCal booking + confirmation (§"Consultation funnel"; PRD M7.2). **The consumer-facing half of M7.1.** M7.1 surfaced the recommendation on the chat `done` event + persisted a `consultation_recommendations` row; M7.2 records the user's choice against it and opens the booking. **Service:** new **`RecommendationService.respond(user, recommendationId, {response})`** (the same choke point as `recommend`) — runs inside `RlsService.run`, loads the recommendation (RLS scopes `consultation_recommendations` to the actor, so a peer's row reads null → 404), stamps the `response` enum column (`book`/`maybe_later`/`ask_another` — the `pending` default is the un-answered state), and on **`book`** opens the TidyCal booking: resolves the bookable consultation type **server-side from the recommendation's stored `trigger`** (re-reads the trigger's rule for its `consultationTypeKey`, falls back to the active-default type — never trusts a client-supplied type, directive §26), **creates a `consultations` row** (`status: recommended`, stamped `typeId`/`amountCents`), **links it back** onto the recommendation (`consultationId` — the M10.2 funnel-attribution join question→conversation→recommendation→booking), and returns `{consultationId, tidycalLink}`. **Idempotent on `book`:** a second Book reuses the already-linked consultation (and recreates one only if it was `SetNull`'d away) rather than spawning duplicates. Unlike `recommend` (which degrades to null after a streamed answer), `respond` runs on an explicit user action so failures surface as real HTTP errors. **Controller:** new thin **`ConsultationRecommendationsController`** `POST /consultation-recommendations/:id/respond` (`@Roles("user")`, `ParseUUIDPipe` + `ZodValidationPipe(recommendationRespondSchema)`), registered in `ConsultationModule.controllers`. Not entitlement-gated — `consultation_booking` is enabled on every plan (PRD funnel table), and gating the route would also block dismissals. **Shared:** new `recommendationRespondSchema` (`z.enum(["book","maybe_later","ask_another"])`) + `RecommendationResponseValue`/`RecommendationRespondInput`/`ConsultationBookingDto`/`RecommendationResponseResultDto` (`packages/shared/src/consultation.ts` — now imports zod). **Web:** new `ConsultationPrompt` component in `apps/web/app/chat/page.tsx` renders the Book / Maybe later / Ask another prompt from `done.recommendation` (carried onto `UiMessage`); Book POSTs the response, opens `booking.tidycalLink` in a new tab (`noopener,noreferrer`), shows a green confirmation (generic "we'll be in touch" when no link is configured yet), the other two dismiss the prompt — all three recorded for funnel signal. New `respondToRecommendation` client fn. **Decisions:** (1) consultation type is re-resolved server-side from the stored trigger, never accepted from the client; (2) create the `consultations` row at Book-click (intent = funnel conversion datapoint) in `recommended` status — the webhook flips it to `booked` with a `bookingRef` (M7.3); (3) booking idempotent via the existing `consultationId` link. **M7.3 builds on this:** the TidyCal **webhook** (records `bookingRef`/`scheduledAt`, confirms the booking) + missed-event recovery = OD#10, mirroring the M6.2 Stripe webhook idempotency discipline. **Seam-tested with a mocked tx** (the real `consultations` write + the `consultationId` link join the M11 Testcontainers list). All gates green; `recommendation.service.ts` = 100% all metrics; +9 service tests (`recommendation.service.test.ts`) + 5 shared schema tests (`consultation.test.ts`).
  - **M7.1** — Rule-based recommendation hooks (admin-configurable: topic, depth, low confidence, high intent) (§"Consultation funnel"; PRD M7.1). **Starts M7.** The first half of the consultation funnel: after a chat turn is persisted, decide whether to surface an in-chat "book a consultation" prompt by evaluating admin-configured rules against the turn's signals. **Two seams.** (1) **Pure engine `@expertos/ai` `recommendation/evaluate.ts` `evaluateRecommendation(signals, rules)`** — deterministic/offline like `buildAnswerPrompt`/`fuseHybrid`; evaluates every enabled rule, collects those that fire, surfaces exactly one outcome (highest `priority`, ties broken by declared trigger order). Four triggers: **`high_intent`** (an intent phrase in the *question*), **`topic`** (a high-stakes term in question *or* answer), **`low_confidence`** (`insufficientKnowledge` OR `citationCount ≤ threshold`), **`depth`** (`assistantTurnCount ≥ threshold`). Keyword matching is whole-word over the **shared `tokenize`** (NFC+lowercase, directive §36) — so "tax" hits "income tax" not "syntax", multi-word phrases match a contiguous run, and VI diacritics stay whole (an NFD question still matches an NFC keyword). A null/≤0 threshold never fires (an unconfigured rule stays silent). (2) **`apps/api/src/consultation/RecommendationService.recommend(user, input)`** (the choke point) — loads the **enabled `recommendation_rules`** (RLS-exempt config), derives the conversation's true assistant-turn count (a `message.count`, not the windowed history), runs the engine, and on a fire **persists a `consultation_recommendations` row** (the funnel's first datapoint — M7.2 records Book/Maybe-later/Ask-another against its `id`; M10.2 attributes revenue through it) + returns the wire DTO (trigger, plain-language `reason`, resolved `consultationType`). **Non-fatal by design:** the whole evaluation is wrapped so any failure degrades to `null` (no prompt) rather than break an answer that already streamed. **Wiring:** `ChatService.answerStream` calls it on the terminal `done` event (both the freshly-generated and cache-hit paths) and carries the result on `ChatStreamEvent.done.recommendation`. **Schema:** new **`recommendation_rules`** table (migration `20260601030000_recommendation_rules`; one row per trigger, `@unique`; reference/config like `plan_entitlements` — no tenant RLS, admin-editable via the M8.3 editor) on the pre-existing `recommendation_trigger` enum; seeded with launch defaults (high_intent prio 50, low_confidence prio 40 thr 0, topic prio 30, depth prio 10 thr 4 — all → `intro_call`). New shared `RecommendationTriggerValue`/`ConsultationTypeDto`/`ConsultationRecommendationDto` (`packages/shared/src/consultation.ts`). **Decisions:** (1) rules are config (DB rows), never hardcoded in the engine — the funnel tunes with no deploy; (2) reuse the shared tokenizer for keyword matching so the funnel can't drift from the rest of the text pipeline; (3) **API + persistence only — the in-chat Book/Maybe-later/Ask-another UI + TidyCal booking are M7.2** (the DTO is on the wire ready for the client). Verified end-to-end against a live Postgres (migrate deploy + seed → all 4 rules present with correct priority/threshold/keywords). All gates green; `recommendation.service.ts` = 100%/100%/100%/100%; engine covered by 15 `@expertos/ai` unit tests + 8 service tests.
  - **M6.5** — Resolve Open Decision #4 (unit economics → seed quota matrix) (§"Paywall, Entitlements & Feature Gating"; §"Open Decisions" #4; PRD M6.5). **Closes M6.** Turned "cost is logged, not modeled" into a real cost model + calibrated the seed `ask_question` quotas to it. (1) **New per-token cost model `apps/api/src/observability/model-pricing.ts`** — `costMicrosFor(model, prompt, completion)` keyed by the `model` string callers already log; representative prod tiers (USD/1M tokens): standard $0.15/$0.60, premium $3/$15, degraded mini $0.05/$0.40, embedding $0.02 (offline `echo-dev`/`echo-dev-mini`/`hashing-dev` priced onto those tiers); unknown model → standard tier (never silently free). Conversion: `micros/token = USD-per-1M × 100` (`cost_micros` = millionths of a USD cent). (2) **`UsageLogService.record` now stamps a real `cost_micros`** when the caller omits one and a `model` is named (every existing caller passes model+tokens, none passed cost) → M10 analytics + billing reconciliation get a margin signal; a named-model cache hit (0 tokens) lands at explicit `cost_micros = 0` (the cache/degrade win is visible in the ledger, not hidden as null). (3) **Calibrated seed quota matrix** (`packages/db/prisma/seed.ts`, all admin-tunable): Free **10**/mo (was 5; model cost ≈ $0.008/mo — volume isn't Free's constraint, conversion is), Plus **200**/mo hard cap (was 100; ≈ $0.16/mo ≈ 4% of net), Premium `limit:null` + **softLimit 500**/mo (was 1000). **Worked margin finding:** a premium answer ≈ $0.018 (3000 prompt + 600 completion on the $3/$15 tier); a premium user goes cost-negative around ~520 premium-model answers/mo (net ≈ $9.39 after a ~$0.60 Stripe fee), so the **softLimit 500 → degrade** caps premium-model spend at ≈ $9.00 then drops to ≈ $0.0008/answer — the heaviest user is ≈ break-even, never deeply negative; the degrade threshold (not a hard cap) is what protects margin. Cache-hit rate is NOT assumed for margin (early volume → low hit rate; the math holds at 0%). Documentation-resolved in PRD §"Open Decisions" #4 (`> RESOLVED (M6.5)` block) + the decisions table + the pricing-table footnote. All gates green; `model-pricing.test.ts` ×6 + `usage-log.service.test.ts` +4 (cost-modeled / cache-hit-zero / named-no-tokens-zero / no-model-null); `usage-log.service.ts` 100% lines.
  - **M6.4** — Caching layers: semantic → retrieval → answer (§"Paywall, Entitlements & Feature Gating" / architecture; PRD M6.4). New `apps/api/src/cache/` module with **`ResponseCacheService`** as the single caching choke point (neither `RetrievalService` nor `ChatService` hand-rolls a key/TTL). **Three layers:** (1) **Retrieval cache** (in-process `LruCache`) keyed on `tenant + topK + status/language/scope + normalized query` → knowledge chunks; a hit skips the query embed + vector/keyword search (and its `retrieve.embed` usage log), benefits *every* turn since retrieval is history-independent. (2) **Answer cache** (in-process LRU) keyed on `tenant + model-tier + expertId + language + topK + normalized query` → the full resolved `CachedAnswer` (text + citations + provenance); the hot tier, skips the LLM call. (3) **Semantic cache** (persistent, **`PgSemanticCacheStore`** over the `semantic_cache` table) = the durable cross-instance tier of the answer cache (Cloud Run scale-to-zero ⇒ in-process cache is cold often); consulted on an in-process miss, warms it on a hit. **Wiring:** `RetrievalService.retrieve` checks/populates the retrieval cache; `ChatService.answerStream` checks the answer cache (in-process→semantic) **only for a cacheable turn** and write-throughs a grounded answer after a miss. **Cacheable = standalone + knowledge-only** (`history.length === 0 && uploads.length === 0`): a turn with prior context or the user's *private* uploads is user-specific and is never served from / written to the shared (tenant-scoped) cache. A cache hit (`serveCachedAnswer`) streams the cached prose as one delta, **persists the turn into the asker's conversation** (a hit is still a real turn) from the cached payload, and records it at **zero model cost** (the margin win). **Entitlement-correctness (the M6.3 requirement):** the **model tier is in the answer key**, so a degraded-model answer is never served to a standard-tier user (and vice-versa); caching **never touches `usage_counters`** — the entitlement guard already reserved one unit before the handler ran, so a hit neither double-counts nor refunds quota. **Only grounded (≥1 citation) answers are cached** — an uncited/insufficient answer isn't pinned (knowledge may be published later; TTL is the only invalidation today — publish-time invalidation is an M8 follow-up). **Schema:** new nullable **`semantic_cache.citations` (jsonb)** column (migration `20260601020000_semantic_cache_answer_payload`) storing the resolved citations verbatim (the existing uuid[] columns can't carry ordinal/content) + a `(tenant_id, normalized_question, model)` btree index for the exact-match lookup. **Decisions:** (1) in-process LRU first per the "no full infra Day 1 / Redis when needed" architecture — `LruCache` is the documented Redis swap point; (2) exact-match semantic lookup now, **embedding-cosine *approximate* match deferred** to the real embedder / M11 (the `embedding` column + HNSW index are reserved — same caveat as every other pgvector path); (3) cache *after* confirming no uploads/history rather than risk dropping a user's private grounding. All gates green; `response-cache.service.ts` / `chat.service.ts` / `retrieval.service.ts` = **100%** coverage; new `lru-cache.test.ts`, `response-cache.service.test.ts`, `semantic-cache.store.test.ts`.
  - **M6.3** — Transparent usage indicator (`.bar` quota meter; `.bar.warn` amber) + fair-use thresholds + degrade-don't-block (§"Paywall, Entitlements & Feature Gating"; §"Design System"; PRD M6.3). **The fair-use half of the metered gate** — M6.1's `enforce` allowed metered-unlimited outright; M6.3 makes "high fair-use cap → degrade to a cheaper model instead of blocking" real, config-driven. **Schema:** new nullable **`plan_entitlements.soft_limit`** (migration `20260601010000_entitlement_soft_limit`) — the per-entitlement fair-use threshold, admin-tunable like `limit`/`window` (M8.3 editor). Seed sets Premium `ask_question` `softLimit: 1000` (limit null = no hard cap). **`EntitlementService.enforce` now returns an `EntitlementDecision`** (`{outcome:"allow"} | {outcome:"degraded", feature}`; block still throws `402`): a metered feature with a `softLimit` (even when `limit` is null = unlimited) now **meters** (reserve-before-work increment), checks the **hard cap first** (block, rolls back), then the **soft threshold** (degrade, don't block). Truly-unlimited (both null) still early-allows without a counter write. `getEntitlements` surfaces `softLimit` so the usage meter can draw a fair-use band on an otherwise-unlimited plan. **Decision propagation:** the `EntitlementGuard` stashes the decision on the request (`req.entitlementDecision`); new **`@EntitlementDecisionParam()`** decorator (mirrors `@CurrentUser`) reads it; `ChatController` passes `{degraded}` into `ChatService.answerStream`. **Model-tier seam:** new **`CHAT_DEGRADED_LLM_PROVIDER`** token + `createDegradedLlmProvider()` (offline = `EchoLlmProvider("echo-dev-mini")`, distinct name so the degrade is observable in usage logs / persisted `model`; prod swaps a real mini/haiku tier). `ChatService` picks `degraded ? this.degradedLlm : this.llm` — the **only** behavioural fork (retrieval/voice/prompt/citation/persistence identical); the terminal `done` event carries `degraded` so the client can show a subtle fair-use note (never an error). **UI:** new **`UsageMeter`** primitive (`packages/ui`, exported) — a labelled `.bar` meter measuring `used` against `limit` (hard cap) else `softLimit` (fair-use line), amber `.bar.warn` when nearing the threshold or once the soft line is passed; reads "Unlimited" when neither is set. New `.meter` styles in `ds.css` (token-only). `EchoLlmProvider` constructor now takes an optional `name`. New shared field `EntitlementView.softLimit` + `ChatStreamEvent.done.degraded`. **Decisions:** (1) `soft_limit` as a real DB column (config-not-code, admin-editable) over a code constant — aligns with the "matrix is the business model" principle; additive/nullable so backwards-compatible. (2) hard-cap check precedes degrade so a capped plan is never silently downgraded to a fair-use pass. (3) degrade still **consumes quota** (reserve-before-work) so fair-use usage is tracked — degrade is not a free pass. (4) UI component stays dependency-free (plain props, not `@expertos/shared`); web/admin map `EntitlementView`→props. **Caveat:** the real `usage_counters` metering of an unlimited+softLimit feature joins the M11 Testcontainers list (seam-tested with a mocked tx, same as M6.1). All gates green; `entitlement.service.ts` 100% lines / 97.22% branch (≥90 gate; one pre-existing defensive `?? 0` branch), `chat.service.ts` 100%.
  - **M6.2** — `PaymentProvider` abstraction (Stripe driver): checkout / customer portal / idempotent webhooks → entitlement sync + transaction ledger (§"Paywall, Entitlements & Feature Gating"; PRD M6.2). **The integration point that finally populates the `subscriptions` rows `EntitlementService.resolvePlan` reads** (until now everyone resolved to Free — nothing wrote them). New `apps/api/src/billing/` module. (1) **`PaymentProvider` interface** (`payment-provider.ts`) — `createCheckoutSession`/`openCustomerPortal`/`verifyWebhook`/`parseEvent`/`cancelSubscription`. **No app code imports the Stripe SDK directly** — swapping to Paddle/Lemon Squeezy is a new driver, not a rewrite. The provider is the payment **source of truth**; `parseEvent` normalizes the provider event onto our **`BillingEvent`** discriminated union (`SubscriptionChange` | `LedgerEntry`) which `BillingService` mirrors into our own `subscriptions`/`transactions`. (2) **`OfflinePaymentProvider`** (the default, analog of `EchoLlmProvider`/`InMemoryStorageProvider`) — checkout/portal return `offline://…` URLs; webhooks carry a **trusted JSON `BillingEvent` envelope** (no signing offline) so a local script/test drives the exact same DB-sync code Stripe would. (3) **`StripePaymentProvider`** — the security-critical, network-free parts are **real + exhaustively tested**: webhook **signature verification** (HMAC-SHA256 over the raw body via `node:crypto`, Stripe's `t=…,v1=…` scheme, replay-tolerance window, constant-time compare) and **event parsing** (`customer.subscription.*` → `SubscriptionChange` reading `items[0].price`, `metadata.userId`, periods; `invoice.payment_succeeded/failed` + `charge.refunded` → `LedgerEntry` keyed by **event id** for idempotency). Checkout/portal/cancel build correct Stripe REST params through an injected `StripeHttpClient` seam (default `fetch` transport — live network, verified at deploy not CI, same caveat as the GCS driver). (4) **`BillingService`** (the coverage-gated brain): `createCheckout`/`createPortal` run under the user's `RlsService.run` (resolve plan→`plan_prices.provider_price_id`, reuse existing `providerCustomerId`; redirect URLs are **server-chosen** from `WEB_APP_URL`, never client-supplied → no open-redirect); `handleWebhook` verifies (→`400` on bad sig via `WebhookVerificationError`→`BadRequestException`), then **idempotently syncs** in a **system RLS context** (`applyRlsContext({isAdmin:true})` — no request principal, mirrors `AuthService.resolveUser`): subscriptions **upsert by `providerSubId`** (resolve user from checkout `clientReferenceId` else the existing row; resolve plan/interval from the price, keep the existing plan when the price is unmapped on an update; skip+warn when neither user nor plan is resolvable for a *new* sub), transactions **insert keyed by `[provider, providerRef]`** (pre-check + P2002 catch = idempotent on Stripe retries). (5) **`BillingController`** — `POST /billing/checkout` + `/portal` (`@Roles("user")`), `POST /billing/webhook` (**`@Public()`** — verified by signature, not a Firebase token; reads `req.rawBody`). **`main.ts` now sets `NestFactory.create(.., { rawBody: true })`** so the webhook verifies over the **unparsed** bytes (a JSON re-encode would invalidate the HMAC). New shared `billingCheckoutSchema` + `CheckoutSessionDto`/`PortalSessionDto` (`packages/shared/src/billing.ts`). **Decisions:** (1) offline-default + Stripe-behind-a-token mirrors the `STORAGE_PROVIDER`/`EMBEDDING_PROVIDER` composition root (`billing.defaults.ts` swaps Stripe when `STRIPE_SECRET_KEY`+`STRIPE_WEBHOOK_SECRET` are set). (2) idempotency key = the **provider event id** (`providerRef`) — unique per delivery, stable across retries. (3) seed `provider_price_id` left **null** (real Stripe ids come from the dashboard / M8.3 admin editor) — so offline checkout correctly `400`s until a price is configured; the **webhook path is fully exercisable offline** via the envelope. All gates green; `billing.service.ts` = **100%** across stmts/branch/funcs/lines; the offline + Stripe drivers (not coverage-gated) have their own unit tests (signature verify, parse, REST param construction).
  - **M6.1** — Entitlement catalog + `plan_entitlements` matrix + `@RequiresEntitlement` guard + `/me/entitlements` (§"Paywall, Entitlements & Feature Gating"; PRD M6.1). **Starts M6 (subscriptions) — the first gating surface.** The catalog default + plan×feature matrix already lived in `packages/db/prisma/seed.ts` (free/plus/premium × 7 features, `ask_question` metered 5/100/null per month) — M6.1 builds the **runtime** that reads it. New `apps/api/src/entitlements/` module: (1) **`EntitlementService`** (the single choke point) — `resolvePlan` reads the actor's live subscription (`active`/`trialing`, pinned `userId` so an admin bypassing RLS still resolves *their own* row) else Free; `getEntitlements(user)` powers `/me/entitlements` (every feature with boolean access or metered `limit`/`used`/`remaining` for the current window — the usage indicator's data); `enforce(user, feature)` is the guard's **reserve-before-work** check (directive §4.13) — **fail-closed** (unknown feature / disabled = `402`), boolean enabled = allow, metered unlimited/no-window = allow (degrade-don't-block is M6.3), metered with a cap **atomically increments** the per-window `usage_counters` row then verifies — over-cap throws `402` so the increment **rolls back in the same transaction** (exactly `limit` uses succeed/window, race-safe). A blocked gate throws `HttpException(EntitlementDeniedPayload, 402)` = `{ reason, feature, currentPlan, upgradeOptions, remainingQuota }`; `upgradeOptions` = higher tiers that grant the feature (and for a quota wall, that raise the cap). (2) **`@RequiresEntitlement(feature)` decorator + `EntitlementGuard`** — mirrors `@Roles`/`RolesGuard`; registered as a **global** `APP_GUARD` in `EntitlementsModule` (runs after the auth guards via the `imports:[AuthModule]` dependency order; no-op without the decorator; 401 if `authUser` is absent). (3) **`EntitlementsController`** `GET /me/entitlements` (shares the `/me` base path with `MeController`). (4) **Wired the gate live:** `ChatController` `POST /chat` now carries `@RequiresEntitlement("ask_question")`, so a real chat turn consumes one question-quota unit (or `402`s at the wall) — the canonical metered route, demonstrating the guard end-to-end (like `/me/admin` demonstrates `RolesGuard`). New shared contract `packages/shared/src/entitlements.ts` (`FeatureKey` union typing the decorator + `EntitlementView`/`EntitlementsDto`/`EntitlementDeniedPayload`). **Filter change:** `AllExceptionsFilter` now **echoes a structured HttpException object response verbatim** (alongside the normalized `statusCode`/`message`/`requestId`) so the flat `402` entitlement body survives to the client — previously it flattened every error to `{statusCode,message}`, which would have dropped `upgradeOptions`/`remainingQuota`; only deliberately-authored HttpException object bodies pass through (a generic 500 still leaks nothing). **Decisions:** (1) runtime reads the **DB** matrix, not a code default — the seed is the default, the table is admin-editable (no deploy to change the business model). (2) reserve-before-work via in-transaction increment-then-check-then-rollback (atomic, no `SELECT … FOR UPDATE`). (3) wired onto `/chat` now (not deferred) — Free's 5/month is an OD#4 placeholder but admin-tunable; service-level chat tests are unaffected (the guard is controller-level, no committed HTTP e2e). All gates green; `entitlement.service.ts` = 100% lines / 95.83% branch (≥90 gate; the one uncovered branch is a defensive `?? 0` fallback unreachable by construction).
  - **M5.4** — Distinct upload citations + retrieval folding (§"Document-assisted Q&A"; §"Design System"; PRD M5.4). **Closes M5.** M5.2/M5.3 wrote `upload_chunks` but **nothing read them** — M5.4 folds a user's own query-time uploads into chat retrieval and surfaces them as **distinct info-blue upload citations**. New **`PgUploadChunkStore`** (`apps/api/src/retrieval/upload-chunk.store.ts`): a pgvector cosine query over `upload_chunks ⋈ uploaded_files`. **Scope = the M5.2 mode contract:** `persistent` uploads are always foldable; `temporary` uploads only when attached to the **current conversation** (`uf.conversation_id = $conv`) AND unexpired (`expires_at IS NULL OR > now()` — defensive, since the sweeper isn't built). **Isolation = RLS, not a predicate:** `upload_chunks` is `tenant_only` but `uploaded_files` is `user_scoped`, so running inside `RlsService.run(user)` the JOIN limits chunks to the user's own files (directive §4.21 — no `tenant_id`/`user_id` in SQL). New **`RetrievalService.retrieveUploads(user, {text, topK, conversationId?})`** embeds the query (shared private `embedQuery`) and runs the store, usage-logged as `upload.retrieve.embed`. **`ChatService.answerStream`** now retrieves uploads (cap `UPLOAD_FACT_TOPK=5`) and **appends them as facts after knowledge** so knowledge keeps the low marker numbers `[1..N]` and uploads follow `[N+1..]`; an upload fact carries `kind:"upload"`, `uploadChunkId`, a human `sourceLabel` (`filename · sheet!cell`), and **empty** `chunkId`/`documentVersionId` (its provenance is the upload chunk, not a doc version). The M4.1 `buildCitations` resolves an upload marker exactly like a knowledge one (it already defaulted `kind`; M5.4 adds `uploadChunkId`/`sourceLabel` carry-through on `CitationSource`/`ResolvedCitation`/`PromptFact`). **Persistence:** `ConversationService.persistTurn` writes `uploadChunkId` and **coalesces empty knowledge ids → null** (an empty string isn't a uuid); `sourceVersionIds` filters out the empty upload versions. `ChatCitationDto` gains optional `sourceLabel`; the read-path `loadCitations` already derived `kind` from `uploadChunkId` (M4.2). **`insufficientKnowledge` stays `facts.length===0`** but now counts uploads — an answer grounded **only** on the user's upload is correctly NOT insufficient. **Web:** the sources drawer shows `sourceLabel` for upload citations in place of `documentVersionId` (info-blue `.cite.upload` already wired in M4.2). **Decisions:** (1) two independent embeds (knowledge + upload) over one shared vector — each retrieval seam stays single-responsibility; the extra embed of one short question is negligible (a shared-vector optimization is a documented follow-up). (2) temporary uploads are conversation-scoped (the natural "session" boundary), persistent are user-wide. (3) knowledge-before-upload ordering keeps citation numbers stable. All gates green; gated `*.service.ts` 100% lines (`retrieval.service.ts`/`chat.service.ts`/`conversation.service.ts` = 100%; the new store isn't coverage-gated but has its own `upload-chunk.store.test.ts`).
  - **M5.3** — Spreadsheet handling: sheets/tables/headers, row/col refs, real numeric values, sheet/cell citations (§"Document-assisted Q&A"; PRD M5.3). **The flagship structured-data feature.** Core abstraction: the `Parser` contract (`apps/api/src/ingestion/parser.ts`) gains an optional **`ParsedDocument.chunks?: ParsedChunk[]`** (`{content, sheetName?, cellRef?}`) — when a parser pre-segments (spreadsheets), the upload indexer persists those **verbatim** (one `upload_chunks` row each) instead of running `chunkText` over the flat text, so per-row sheet/cell **provenance survives** into `upload_chunks.sheet_name`/`cell_ref` (a citation can point at a sheet/cell). Backward-compatible: the M1.1 text-only ingestion pipeline ignores `chunks` and always chunks `text`. New shared renderer **`spreadsheet.ts`** (`SheetTable → renderSheets()/renderText()`): one chunk per data row rendered as `header: value` lines (column context + real values, empty cells skipped), tagged with sheet name + A1 cell range (`A2:C2`); `columnLetter()` for 0→A/26→AA; blank rows skipped but row numbers stay source-aligned (header=row 1); **`MAX_SPREADSHEET_ROWS=5000` cap** bounds the untrusted boundary (one embedding/row, so an unbounded sheet is a cost/memory amplifier). **CSV** parser enhanced to emit structured chunks (single unnamed sheet) while keeping its flat `text` byte-stable (no ingestion regression). New **`XlsxParser`** + dependency-free **`zip.ts`** (read-only ZIP reader via `node:zlib` `inflateRawSync`: central-directory parse, stored+deflate methods, 32 MiB per-entry inflate cap = zip-bomb guard, `InvalidZipError` on malformed) — reads `xl/workbook.xml` (sheet names) + `workbook.xml.rels` + `sharedStrings.xml` (incl. rich-text runs) + each worksheet, extracting each cell's **real stored `<v>`** (so `1200000` stays `1200000`, never a formatted display string), shared strings (`t="s"`), inline strings, booleans, and decoding XML entities. Registered in `createDefaultParserRegistry` (ingestion + upload share it). `UploadService.buildIndexedChunks` now: uses `parsed.chunks` when present (provenance), else `chunkText` fallback (prose, null provenance); **wraps parse in try/catch** → a malformed/spoofed-but-magic-valid file (e.g. fake XLSX) is **stored unindexed (chunkCount 0), not a 500** (warn-logged) — same graceful-degradation as an unsupported format. `persistChunks` writes `sheetName`/`cellRef`. **Decisions:** (1) dependency-free XLSX reader as the offline default (matches the codebase's offline-default + swap-real-driver-behind-a-seam philosophy; safest for the untrusted-upload boundary — no CVE-bearing parser dep); the `Parser` seam still allows swapping a Python worker later. (2) per-row chunks (not row-groups) for citation precision, capped for cost. (3) **PDF/DOCX parsing remains deferred** (still stored-but-0-chunks) — M5.3's line item is spreadsheet handling. **M5.4 reads these chunks** (retrieval folding + distinct upload citations). All gates green; `upload.service.ts` = 100%/92.68%/100%/100% (branch ≥90 gate; defensive `String(error)` + `?? null`-when-present branches uncovered, parser files aren't coverage-gated but have unit tests).
  - **M5.2** — Temporary vs persistent upload modes: retention + indexing strategy per mode (§"Document-assisted Q&A"; PRD M5.2). `mode` is now on `uploadCreateSchema` (enum `temporary|persistent`, **defaults `temporary`** so omitting it keeps M5.1 behavior). **`UploadService.upload` now indexes** (M5.1 only stored bytes): after validate→scan→ownership-check it runs the file through the **same parse→chunk→embed pipeline as ingestion** (reusing the ingestion `ParserRegistry` + `createDefaultEmbeddingProvider` factory behind new `UPLOAD_PARSER_REGISTRY`/`UPLOAD_EMBEDDING_PROVIDER` tokens, so upload chunk vectors share the expert-knowledge space), then persists the `uploaded_files` row **and** its `upload_chunks` atomically in one `RlsService.run` (embeddings via raw `UPDATE upload_chunks SET embedding=$1::vector`, the `DocumentVersionRepository` pattern). **Mode divergence** via the pure `retentionFor(mode)` helper: **temporary** → scope `temporary_upload` (excluded from searchable knowledge, session-scoped) + `retentionDays = TEMPORARY_RETENTION_DAYS (7)` + `expiresAt = now+7d` (a sweeper reclaims it later); **persistent** → scope `user_private`, no expiry (`retentionDays`/`expiresAt` null), retrievable by later questions. Parse+chunk+embed runs **before** storing bytes, so an embed failure leaves no orphan. **Binary formats deferred (M5.3):** an allowlisted-but-unparseable type (PDF/DOCX/XLSX — no parser registered) is still **stored** but yields **0 chunks** rather than rejecting (new `ParserRegistry.tryResolve → Parser|null` returns null instead of throwing); `UploadedFileDto` gains `chunkCount` + `expiresAt` so the client can tell whether the upload is queryable. Embedding cost recorded via `UsageLogService` (`upload.embed`) only when chunks are produced. **Decisions:** (1) both modes parse into `upload_chunks` — the difference is **scope + retention**, not whether-parsed (per PRD: temporary = transient chunks scoped to the session, persistent = indexed into user-private knowledge); (2) **retrieval/chat folding of upload chunks is deferred to M5.4** (M5.2 writes the chunks; M5.4 reads them + distinct info-blue upload citations + per-user isolation at retrieval time); (3) binary parsers (PDF/DOCX/XLSX) land in M5.3 (`tryResolve` is the seam — store-now/index-when-the-parser-lands). All gates green; `upload.service.ts` = 100%/96.77%/100%/100% (only pre-existing defensive `?? ""`/embed-mismatch guards uncovered).
  - **M5.1** — Query-time document upload with file-type/size validation + malware scan (§"Document-assisted Q&A"; §"Security"; PRD M5.1). **Starts M5.** New `apps/api/src/uploads/` module — the first multipart file route. **`UploadService.upload(user, filePart, {conversationId?})`** is the validate→scan→store→persist pipeline (the single coverage-gated choke point): (1) **size** — reject empty (400) and over the 10 MiB `MAX_UPLOAD_BYTES` (413) before any work; (2) **type allowlist** — `UPLOAD_TYPES` maps the 6 PRD formats (txt/md/csv/pdf/docx/xlsx) by **normalized MIME**, anything else → 415; (3) **anti-spoof** — the filename extension must match the declared type (else 400), and binary formats are **magic-byte sniffed** (`%PDF`; `PK` ZIP header for the OOXML xlsx/docx) so a renamed binary is rejected; (4) **malware scan** behind the swappable `MalwareScanner` contract (offline default `SignatureMalwareScanner` flags the EICAR test signature) — an unclean file is rejected (422) and **never stored** (warn-logged with the signature, no row); (5) **storage** behind the swappable `StorageProvider` contract (offline default `InMemoryStorageProvider` → `memory://` URI; production swaps GCS behind the `STORAGE_PROVIDER` token); (6) **persist** the `uploaded_files` row inside `RlsService.run` (`user_scoped` → tenant/user isolation is structural, no manual predicate), under the DB-default `temporary` mode/`temporary_upload` scope. An attached `conversationId` is **ownership-rechecked** via the `user_scoped` `conversation.findUnique` (404 if not the actor's) **before** bytes are stored, so a rejected attach leaves no orphan. Untrusted **filename is sanitized** (basename only, strip control + path/markup-unsafe chars, NFC, ≤200, fallback `upload`) before storage/echo-back (directive §1.2). Thin **`UploadController`** (`POST /uploads`, `@Roles("user")`) adapts multer's multipart shape (`FileInterceptor`, default memory storage + `limits.fileSize` DoS guard) into the structural `UploadFilePart`; no `@types/multer` dep (structural type, same pattern as the chat `SseResponse`). New shared `uploadCreateSchema` + `UploadedFileDto`/`UploadCreateInput` (`packages/shared/src/upload.ts`). **Decisions:** mode/retention is **deliberately deferred to M5.2** (every M5.1 upload is `temporary`); parsing into `upload_chunks` is **deferred to M5.2/M5.3** (M5.1 stores the raw file + a validated row, not chunks); storage/scanner are offline-default seams mirroring the ingestion provider factories (one composition root in `upload.defaults.ts`). All gates green; `upload.service.ts` = 100%/95%/100%/100% (only an unreachable defensive `?? ""` branch uncovered).
  - **M4.3** — Resolve Open Decision #7 (streaming vs citation-resolvability UX) (§"Open Decisions" #7; §"Design System"; PRD M4.3). **Closes M4.** **Documentation/decision task — no code change** (the engineering was fully built across M3.1 → M4.1 → M4.2). The likely resolution from the PRD is **confirmed and adopted** as the Eng + Design sign-off: **stream the prose, defer the markers** — while a turn streams, any `[n]` stays inert plain text; markers become interactive `.cite` chips only after the terminal `done` event AND only where they resolve (web `renderAnswer` gates on `resolved = message.done && message.citations.length > 0`). Resolvability is enforced **once, server-side, on the complete answer** (`buildCitations` in `ChatService.answerStream`), so a citation can never flash-then-vanish or renumber. The same render-after-resolve gate applies to re-hydrated history (`ConversationService.get` citations). The interaction is **click-to-passage** (highlight + scroll the source row), chosen over hover-preview so the UX is identical live vs. in history and is keyboard-accessible. **Key finding:** the two goals the decision framed as conflicting (streaming feel vs. integrity) do **not** actually conflict under deferral — prose streams token-by-token AND no citation is shown before it resolves, with no buffering trade-off. Recorded as a `> RESOLVED (M4.3)` block under §"Open Decisions" #7 and in the decisions table. All gates remain green (384 pass; no code touched).
  - **M4.2** — Sources drawer + click-to-passage + `document_version_id` provenance (§"Design System"; §"Citations"; PRD M4.2). Built on the M4.1 builder. **Backend:** (1) **`ChatCitationDto` gains `kind: "knowledge"|"upload"`** (`packages/shared/src/chat.ts`) — promoted off the internal `ResolvedCitation` (M4.1's reserved field) now that it has a consumer; `ChatService.toCitationDto` sets it on the `done` event, the drawer reads it to pick the `.cite` colour. (2) **Persisted-ordinal bug fixed (M4.1 follow-up a):** `TurnCitation` now carries `ordinal` and `ConversationService.persistTurn` writes the **true marker ordinal**, not the loop index — a sparse list (lone `[2]`) now stores ordinal 2, so a re-hydrated answer stays aligned with its prose. (3) **Citation read path:** `ChatMessageDto` gains `citations: ChatCitationDto[]`; `ConversationService.get` re-hydrates them via a new private `loadCitations(tx, assistantIds)` (one `findMany`, grouped by message, ascending by ordinal), deriving `kind` from `uploadChunkId ? "upload" : "knowledge"` (forward-compatible with M5 uploads; the shared nullable `chunk_id`/`document_version_id` columns coalesce to `""`). **Web (`apps/web/app/chat/page.tsx`):** new `AssistantAnswer` component renders the answer prose with `[n]` markers turned into **clickable `.cite` chips** (`renderAnswer` splits on `/\[(\d+)\]/g`) — but **only after the stream completes AND the marker resolves** (render-after-resolve: an unresolvable `[9]` stays plain text, never a live `.cite`); below it a **sources drawer** lists each resolved source with its quote + `document_version_id` provenance, and clicking an inline marker highlights + `scrollIntoView`s the matching `.source` row (**click-to-passage**), keyboard-accessible (role=button, Enter/Space). New `.sources`/`.source`/`.source.active` design-system styles in `ds.css` (crimson active highlight via existing tokens). **Decision: deferred no further** — the live chat page is the consumer; the history-view drawer can reuse `ConversationDetailDto.citations` when the M3.2 history UI is built. **OD#7 (M4.3) engineering behavior is now fully realized in the UI** (stream prose → validate → render markers); the Eng+Design sign-off remains the open half. All gates green; `conversation.service.ts`/`chat.service.ts` = 100% coverage.
  - **M4.1** — Citation builder with chunk-resolvability guarantee (§"Citations"; PRD M4.1). **Starts M4.** New pure, deterministic `@expertos/ai` module `prompt/citations.ts` — `buildCitations({ answer, citations }) → { text, citations }` — the single enforcement point for "never emit an unresolvable citation". Given the COMPLETE post-stream answer + the ordered resolution table (`AnswerPrompt.citations`, marker `[i+1]`→`citations[i]`), it: (1) parses every `[n]` marker (grammar `/\[\s*\d+(?:\s*[,\s]\s*\d+)*\s*\]/gu` — covers `[1]`, adjacent `[1][3]`, and comma/space drift `[1,3]`/`[1 3]`; non-numeric brackets like `[abc]`/array literals are left as literal prose), (2) **drops** any marker outside `1..N` (`[0]`, `[99]`, hallucinated) from BOTH the returned text and the citation list, (3) returns **only the sources a surviving marker referenced** (referenced-only — de-duplicated, ascending by ordinal), and (4) **does NOT renumber** — the marker the model wrote still equals the citation's `ordinal`, so the streamed prose the user saw stays valid. `ChatService.answerStream` now calls `buildCitations` after the stream completes: persists `built.text` (markers sanitized) + `built.citations`, derives `sourceVersionIds` from the cited sources, and emits referenced-only `ChatCitationDto[]` on the `done` event. **`insufficientKnowledge` stays `facts.length === 0`** (retrieval-side), deliberately decoupled from citation filtering so a grounded-but-uncited answer doesn't flip it. **Decisions** (from a design workflow: parallel seam-map → 2 independent proposals → synthesis): referenced-only over emit-all (grounding-honest; the prior code listed every retrieved chunk as a source even if uncited); keep-ordinal over renumber (no new `done`-event text field; M4.2 owns gap-free 1..k display); no change to `answer-prompt.ts`/`echo-llm-provider.ts` (the `[n]` grammar in SOURCES is untouched, so the echo path still emits all-in-range markers). **Known interaction (flagged for M4.2):** `persistTurn` re-derives the persisted citation row's `ordinal` from its loop index, so a filtered non-contiguous list (e.g. only `[2]`) stores row ordinal 1 — harmless today (no read path re-hydrates citation ordinals; the wire DTO carries the true marker ordinal), but the M4.2 sources-drawer read path must carry the true ordinal. **`kind: "knowledge"|"upload"`** is reserved on the internal `ResolvedCitation` for the M5 upload-citation distinction but deliberately NOT yet on the wire `ChatCitationDto` (kept minimal/knip-clean). All gates green; `chat.service.ts` = 100% coverage; new `citations.ts` covered by 14 unit tests (global `@expertos/ai` branch coverage 99.34%, ≥90% gate).
  - **M3.5** — Conversation context-window / cost ceiling policy, resolving Open Decision #8 (§"Chat experience"; §"Open Decisions" #8; PRD M3.5). **Closes M3.** Retired the interim `HISTORY_LIMIT = 10` message cap (M3.1) in `ConversationService.loadHistory` (`apps/api/src/chat/conversation.service.ts`) and replaced it with a **token-budget window**: the most-recent user/assistant messages whose combined `estimateTokens` (reused from `@expertos/ai`, the same word→token heuristic that sizes ingestion chunks) fits `HISTORY_TOKEN_BUDGET = 1500`. Token-bounding — not message count — is what actually caps prompt size and per-answer spend. The window is **deterministic and offline** (no LLM call, zero added cost — matching the `deriveTitle` precedent), keeps **whole messages newest-first**, and **always carries the single most-recent message** even if it alone exceeds the budget (a follow-up never loses its antecedent). A hard `HISTORY_MAX_MESSAGES = 40` row ceiling backstops the DB read so a burst of tiny messages can't scan unbounded rows. **LLM summarization of out-of-window turns is deferred** (documented seam in the constant's doc comment): if it lands it must use a cheap model and must not summarize away a concierge "inject corrected answer into context" edit (M9) — already safe today since the window keeps the most-recent turns where a correction enters. Added 2 `loadHistory` tests (token-budget windowing + always-keep-latest). All gates green; `conversation.service.ts` = 100% coverage.
  - **M3.4** — Insufficient-knowledge path + answer feedback (👍/👎 + reason) (§"Chat experience"; PRD M3.4). Two pieces. (1) **Insufficient-knowledge signal on the API:** `ChatService.answerStream` now sets `insufficientKnowledge: facts.length === 0` on the terminal `done` SSE frame (`ChatStreamEvent` in `packages/shared/src/chat.ts`). Zero retrieved sources is the deterministic, server-side signal that the prompt builder's INSUFFICIENT-KNOWLEDGE rule governed the answer (the `EchoLlmProvider` already emits it) — so a client can surface a graceful next step (rephrase / book a consultation) instead of presenting an ungrounded reply as confident. The turn is still persisted (it IS a real answer) with `sourceVersionIds: []`. (2) **Answer-feedback CRUD** — new **`AnswerFeedbackService`** (`apps/api/src/chat/answer-feedback.service.ts`) on the existing `answer_feedback` model (`user_scoped`, unique `(userId,messageId)`). Copies the **`SavedAnswerService` ownership shape**: client supplies only `messageId`; the service looks up the message (must be `role:"assistant"` → else 404), then the **`user_scoped`** `conversation.findUnique` (null = the real ownership boundary → 404, since `messages` is only `tenant_only`). **Key difference from a bookmark: `submit` is an idempotent upsert, not create-or-409** — a user may flip 👍↔👎 or revise the reason, so an existing `(userId,messageId)` row is updated (reason cleared to null when omitted) rather than conflicting. `remove(user, messageId)` retracts (404 when none). New thin **`AnswerFeedbackController`** (`POST /answer-feedback`, `DELETE /answer-feedback/:messageId` → 204), `@Roles("user")`, validates via `ZodValidationPipe(answerFeedbackSubmitSchema)`, wired into `ChatModule`. New shared `answerFeedbackSubmitSchema` (`messageId` uuid, `helpful` boolean, `reason` trimmed/≤500 optional) + `AnswerFeedbackDto`/`AnswerFeedbackSubmitInput` in `packages/shared/src/chat.ts`. **Decision: API + persistence only; the consumer-facing web feedback buttons / insufficient-knowledge next-step UI are deferred** (same M3.2/M3.3 precedent — no unused web exports, keeps knip clean). All gates green; `answer-feedback.service.ts` = 100% coverage.
  - **M3.3** — Full-text conversation search over message content + titles (§"Chat experience"; PRD M3.3). New **`ConversationService.search(user, {q,limit,offset})`** (`apps/api/src/chat/conversation.service.ts`) — the first raw-SQL path in this service (the `ts_rank`/`ts_headline` functions have no Prisma Client expression, exactly like the M1.2 `PgVectorStore` keyword path), run inside `RlsService.run` so **RLS does the isolation**: `conversations` is `user_scoped` (only the owner's chats survive) and `messages` is `tenant_only`, so the `conversations ⋈ messages` intersection is exactly the actor's own messages. A conversation matches when its **title** OR any **user/assistant message** matches the `websearch_to_tsquery('simple', $1)` (`'simple'` config keeps VI undistorted — same OD#9 rule as retrieval); ranked by the stronger of the title `ts_rank` and the best message hit, ties broken by `updatedAt desc`. Each hit carries a `ts_headline` snippet of its best-matching message (null when only the title matched), and the snippet uses **guillemet selectors (`«…»`) not the default `<b>` tags** so the API never emits markup (directive §1 — a text-rendering client is XSS-safe). New shared `conversationSearchQuerySchema` (`q` trimmed/≤200/NFC-normalized like `chatRequestSchema`, z.coerce pagination) + `ConversationSearchResultDto` (`{conversation, snippet, messageId}`) in `packages/shared/src/chat.ts`. New thin route **`GET /conversations/search?q=`** declared **before** `@Get(':id')` so `search` isn't captured as a conversation id. New migration `20260601000000_conversation_search_indexes` adds expression **GIN indexes** on `to_tsvector('simple', content)` (messages) + `to_tsvector('simple', coalesce(title,''))` (conversations) matching the query expressions verbatim. **Raw SQL is seam-tested only** (mocked `$queryRawUnsafe`) — same M11 Testcontainers caveat as the other raw-SQL stores. All gates green; `conversation.service.ts` = 100% coverage.
  - **M3.2** — Conversation history + auto-titling + saved answers (§"Chat experience" / "History & retention"; PRD M3.2). Built on the M3.1 persistence seam (no re-wiring). **`ConversationService`** (`apps/api/src/chat/conversation.service.ts`) gains three read/write methods — `list(user, {limit,offset})` (the user's conversations, `updatedAt desc`, RLS-scoped), `get(user, id)` (one conversation + full user/assistant transcript, oldest-first; 404 when not the actor's), `rename(user, id, title)` (override the auto-title; ownership via RLS) — plus **auto-titling**: `persistTurn` now sets `title: deriveTitle(firstQuestion)` on conversation create (a pure, **offline/deterministic** helper — collapse whitespace, truncate on a word boundary at 80 chars with an ellipsis; no LLM call, so no added cost — aligned with the OD#8 cost concern). New **`SavedAnswerService`** (`saved-answer.service.ts`) — `create` (bookmark an **assistant** answer; takes only `messageId` and derives the owning conversation server-side per directive §26, re-checking ownership via the `user_scoped` conversation lookup since `messages` is only `tenant_only`; 404 on missing/non-answer/unowned, 409 on duplicate via the `(userId,messageId)` unique), `list` (RLS-scoped, newest-first), `remove` (404 when not the actor's). New thin controllers: **`ConversationsController`** (`GET /conversations`, `GET /conversations/:id`, `PATCH /conversations/:id`) + **`SavedAnswersController`** (`POST /saved-answers`, `GET /saved-answers`, `DELETE /saved-answers/:id` → 204), both `@Roles("user")`, wired into `ChatModule`. New shared DTOs/schemas (`packages/shared/src/chat.ts`): `ConversationSummaryDto`/`ConversationDetailDto`, `conversationListQuerySchema`, `conversationRenameSchema`, `SavedAnswerDto`, `savedAnswerCreateSchema`, `savedAnswerListQuerySchema` (z.coerce pagination, length-bounded per directive §1.1). **Decision: API + persistence only; consumer-facing web history UI deferred** (mirrors the M2.3 precedent — HTTP routes landed before the M8.5 portal UI); no unused web exports added (keeps knip clean). All gates green; `conversation.service.ts` + `saved-answer.service.ts` = 100% coverage.
  - **M3.1** — Chat UI with streaming + context-retaining follow-ups (§"Phase 1 — MVP" core Q&A loop; PRD M3.1). **First M3 surface — the first consumer that wires M1 retrieval + M2 voice + the `@expertos/ai` prompt builder into a generated, streamed, persisted answer.** Backend: new `@expertos/ai` streaming contract (`LlmProvider.completeStream?(messages) → AsyncIterable<LlmStreamChunk>`, optional so the M2 eval harness is unaffected) + offline deterministic `EchoLlmProvider` (`packages/ai/src/llm/`, the completion-side `HashingEmbeddingProvider` — cites every numbered SOURCE, states INSUFFICIENT-KNOWLEDGE when none, no network/key) bound via new `createDefaultLlmProvider()` in `ingestion.defaults.ts` (one composition root, swap to real driver here). New `apps/api/src/chat/`: **`ConversationService`** (RLS-scoped persistence — `loadHistory` capped at 10 msgs [interim, OD#8→M3.5], `persistTurn` writes user+assistant messages + ordinal-indexed citations + creates the conversation, all in one transaction), **`ChatService`** (`answerStream(user, input)` async generator: load history → retrieve facts → resolve voice (or neutral) → `buildAnswerPrompt` → splice prior turns between system & user → stream LLM deltas → persist → record `chat.answer` usage → terminal `done` event with resolved citations), **`ChatController`** (`POST /chat` SSE over Express `@Res()`; `@Roles("user")`). New `ExpertsController` (`GET /experts`, any authed user) so the M2.2 voice picker finally has its consumer. New shared `chatRequestSchema` + `ChatStreamEvent`/`ChatCitationDto`/`ChatMessageDto` DTOs (`packages/shared/src/chat.ts`). Web: `apps/web/src/lib/chat-client.ts` (SSE fetch parser + `fetchExperts` + `renditionLabel`) + `apps/web/app/chat/page.tsx` (streaming chat UI, voice picker, render-after-complete citations per **OD#7**, "AI rendition of [Expert]" disclosure). **OD#7 resolved (engineering):** citations emitted only in the terminal SSE frame after generation+persistence. **OD#8 deferred to M3.5** (hardcoded 10-msg cap with code comment). All gates green; `chat.service.ts`/`conversation.service.ts` = 100% coverage; full Nest DI graph boot-smoked.
  - **M2.4** — Voice-vs-facts separation tests + voice-fidelity assertion in the eval harness (§"Expert voice layer"; §"LLM/RAG eval harness"; PRD M2.4). **Finishes M2.** Two pieces, both in `@expertos/ai`. (1) **Dedicated voice-vs-facts separation tests** (`prompt/voice-vs-facts.test.ts`) asserting against `buildAnswerPrompt` output — the single enforcement point — that: the **citation list is identical** with/without a voice and across different voices; the **SOURCES+QUESTION user message is byte-identical** regardless of voice (voice lives only in the system message); a number that exists **only** in a guideline or style example (decoys "37%"/"7%") **never** leaks into the SOURCES block or the resolvable citations; all voice content is confined to the system message; and the facts-authoritative / voice-presentation-only / insufficient-knowledge rules survive even under a heavy voice. (2) **Voice-fidelity eval harness** mirroring the M1.3 retrieval harness's "deterministic-offline + out-of-band real model" split: new `eval/voice-types.ts` (contracts), `eval/voice-metrics.ts` (pure scoring + acceptance bars), `eval/voice-harness.ts` (`evaluateVoice(goldenSet, { llm?, judge? })`), `eval/voice-golden-set.ts` (`VOICE_GOLDEN_SET` — terse-EN, narrative-EN guidelines-only, VI cases). The **structural layer** (always run, network-free) checks the prompt contract per case — disclosure present, guidelines/examples injected & labeled style-only, separation rules stated, `expectStyleMarkers` present, and the load-bearing **facts-invariant-under-voice** check (voice-on user message + citations === voice-off). The optional **live layer** (inject an `LlmProvider` + `VoiceJudge`, out-of-band) generates the answer and scores `voiceFidelity`/`factAdherence`. **OD#2 (voice-fidelity acceptance bar):** took an engineering stance — `FACT_ADHERENCE_BAR = 1.0` (any invented/altered claim fails) + `VOICE_FIDELITY_BAR = 0.7` (mean); the *product* (expert-signed) bar + golden-set ownership remain OD#6/the open half of #2. All gates green; **100% coverage** across the whole `@expertos/ai` package.
  - **M2.3** — Expert sign-off workflow on own voice profile + language-aware voice EN/VI (§"Expert voice layer"; PRD M2.3). The voice domain's **first write path**. New `VoiceProfileService` (`apps/api/src/voice/voice-profile.service.ts`) runs the publish-lifecycle state machine inside `RlsService.run` using Prisma Client model methods (the `DocumentVersionRepository` pattern — voice_profiles has no Unsupported column, so no raw SQL): `create` (author a `draft`), `update` (edit free-text **only while draft**; `""` clears description/guidelines to NULL), `submit` (`draft→expert_review`), **`approve`/sign-off** (`expert_review→published`, stamps `approvedBy`=actor + `approvedAt`=now — the moment a voice becomes answer-eligible), `requestChanges` (`expert_review→draft`), and `list` (sign-off queue). Invalid transitions → 409, missing → 404. **Ownership rule (NT.2 "an expert signs off on their own voice"):** a non-admin actor may only touch a profile whose `Expert.userId` === their user id; admins act across the tenant; `list` auto-scopes a non-admin to their own profiles. **Language-aware:** each `(expert, language)` profile is signed off independently. New shared schemas (`packages/shared`): `publishStatusSchema` (new `publish.ts`, reused by M8 knowledge gate) + `voiceProfileCreateSchema`/`voiceProfileUpdateSchema`/`voiceProfileListQuerySchema` (NFC-normalized text, EN/VI). New **`VoiceProfileController`** (`POST /voice-profiles`, `PATCH /:id`, `POST /:id/{submit,approve,request-changes}`, `GET /`) — first admin/expert-portal surface — gated `@Roles("expert")` (admin satisfies via hierarchy), ownership enforced in the service. New reusable `ZodValidationPipe` (`apps/api/src/common`, structurally typed so apps/api takes **no** zod dependency) → 400 with field issues on bad input. All gates green; 100% coverage on new code (gated `*.service.ts` = 100%).
  - **M2.2** — Multiple selectable expert voices + attribution / "AI rendition of [Expert]" disclosure (§"Expert voice layer"; PRD M2.2). Two pieces. (1) **Single-source-of-truth attribution** — new pure `@expertos/ai` `prompt/attribution.ts` `buildAttribution(voice?) → { rendition, expertName?, disclosureText }`; the disclosure phrase `"AI rendition of [Expert]"` now lives **only** here. `buildAnswerPrompt` was refactored to embed `attribution.disclosureText` verbatim (emitted strings byte-identical → existing `answer-prompt.test.ts` unchanged & green), so the LLM framing and the UI label can never drift. Exported from the `@expertos/ai` index for the (future M3) chat UI to render the identical label. (2) **Selectable-voice listing** — new shared `expertListQuerySchema` (`language` optional = all languages, `limit` 1..100 default 20); new `apps/api/src/voice/expert.store.ts` `PgExpertStore.listExperts(language?, limit)` (raw SQL, bound params, `array_agg(DISTINCT vp.language)` per expert; only **active experts with a published voice profile** are eligible — enforced in SQL; RLS-scoped tx, no `tenant_id` predicate) → maps to new API-layer `ExpertVoiceMeta { expertId, displayName, languages[], hasActiveProfile:true }`; `VoiceService.listExperts(user, query)` runs the store inside `RlsService.run` (no embedding/token-billed call). **Decision: NO HTTP controller yet** — no UI/conversation consumer exists (M3 not built); M3's chat layer calls `listExperts`/`buildAttribution` in-process and adds the route alongside the picker. "Which expert answered" is **returned/derivable, not persisted** (no conversation/message model until M3). All gates green; 100% coverage on new code.
  - **M2.1** — Expert voice layer: voice profiles + runtime voice-example retrieval + voice-on-top-of-facts prompt builder (§"Expert voice layer"; PRD M2.1). Two seams: (1) **`@expertos/ai` prompt builder** (`prompt/answer-prompt.ts` `buildAnswerPrompt`, pure/deterministic) — assembles `ChatMessage[]` with a system prompt that makes FACTS authoritative, pins the `[n]` citation format, and labels voice guidelines + retrieved style examples as STYLE-ONLY (never override a cited number/claim); renders the "AI rendition of [Expert]" framing; defaults EN, supports VI; NFC-normalizes all injected text; returns `{ messages, citations }` where `citations[i]` resolves marker `[i+1]` (the M4 resolvability contract). (2) **`apps/api/src/voice` runtime retrieval** — `PgVoiceExampleStore` (single-modality cosine over the HNSW `voice_examples.embedding` index; published-profile + active-expert gate; bound params; RLS-scoped tx) + `VoiceService.retrieveVoice(user, voiceQuery)` (embeds topic with the same model as ingestion, loads the published `VoiceProfile` for expert+language, retrieves top-K examples, usage-logs `voice.embed`; empty voice layer when no profile → neutral-voice fallback). New shared `voiceQuerySchema` (expertId uuid, NFC text, language default `en`, topK default 3). `VoiceModule` wired into `AppModule`. Field names align with the prompt builder's `VoiceProfileInput`/`VoiceExampleInput` so M3 passes them straight through. All gates green, 100% coverage on new code.
  - **P0.1** — git init + pnpm + Turborepo monorepo scaffold (apps/web, apps/admin, apps/api, packages/shared, packages/db, packages/ai, packages/ui, infra/). All feedback loops green.
  - **P0.2** — Postgres + pgvector via Prisma; tenant-ready schema + RLS migration (§"Data Model"). Full multi-tenant schema (36 models), pgvector `vector(1536)` embedding columns + HNSW cosine indexes, two migrations (`init_schema`, `rls_and_vector_index`), idempotent seed, and `applyRlsContext` helper. RLS verified end-to-end through Prisma as the non-superuser `app_user`.
  - **P0.3** — Firebase Auth wiring + RBAC roles (§"Security"). API: `auth/` module — `TokenVerifier` abstraction + `FirebaseTokenVerifier` (Firebase Admin SDK), global `FirebaseAuthGuard` (verify token → find-or-create user under admin/system RLS context → attach `req.authUser`), `RolesGuard` + `@Roles()` (hierarchical via `satisfiesRole`), `@Public()`, `@CurrentUser()`, `RlsService.run()` (per-request transaction + `applyRlsContext`), `MeController` (`GET /me`, `GET /me/admin` role-gated). `DatabaseModule` provides the `PRISMA` token. Health route marked `@Public()`. Web: lazy Firebase client (`getFirebaseAuth`), `AuthProvider`/`useAuth` (Google sign-in via popup, `getIdToken`), sign-in/out on home page.
  - **P0.4** — Manual build & deploy (§"Phase 0 — Foundation"). Per-app Dockerfiles (`apps/{api,web,admin}/Dockerfile`, build context = repo root, pnpm-workspace aware): API multi-stage with `pnpm deploy --prod` + openssl for Prisma engine binding `PORT`; web/admin use Next `output: "standalone"` (+ `outputFileTracingRoot` → repo root) verified to emit `apps/<app>/server.js`. Root `.dockerignore`. Minimal scale-to-zero Terraform (infra/): Cloud Run v2 ×3 (`min_instance_count = 0`), Artifact Registry, Cloud SQL PG15 (pgvector-capable) + db, GCS uploads bucket, Secret Manager containers (`DATABASE_URL`, `FIREBASE_*`), least-priv runtime SA + IAM, outputs. `infra/deploy.sh` + root `pnpm deploy[:api|:web|:admin]` scripts (docker build → push → `gcloud run deploy --image`). `infra/README.md` documents the full apply → out-of-band (pgvector, app_user, secrets) → deploy → smoke flow. Coverage gate already enforced by `jest.base.cjs` (90%).
  - **P0.5** — Observability baseline (§"Phase 0 — Foundation" item 5). New `apps/api/src/observability/` module (`@Global`): **structured JSON logging** (`StructuredLogger` implements Nest `LoggerService` + `info/warn/error/debug(msg, fields)`; emits one JSON line/stdout with Cloud Logging `severity` + `logging.googleapis.com/trace`; wired via `app.useLogger` with `bufferLogs`); **request tracing** (`RequestContextMiddleware` + `AsyncLocalStorage` → per-request `requestId` (reuses inbound `x-request-id` or mints one, echoes in response header) + Cloud Trace id from `X-Cloud-Trace-Context`; every log auto-stamped); **Sentry** error tracking (`@sentry/node` 8.55.2, lazy/opt-in on `SENTRY_DSN`, no-op otherwise — mirrors lazy-Firebase; `initSentry` at bootstrap); **catch-all `AllExceptionsFilter`** (`APP_FILTER`) → 4xx logged WARNING (not reported), 5xx/unknown → generic 500 + ERROR log + Sentry report, `requestId` in body; **`UsageLogService.record()`** writes cost/usage rows to `usage_logs` via `RlsService` (best-effort: swallows+logs failures). PII redaction (`redact`) scrubs sensitive keys on every log line (directive §4.10). DI graph validated via dist bootstrap smoke (init+close).
  - **P0.6** — Design system foundation (§"Design System"). `packages/ui` full primitive set over `ds.css` + Google Fonts (imported at both Next app roots); stylelint (raw hex/off-scale px) + eslint hex-literal guard wired into `pnpm lint`; `Cite` renders only after `resolved`.
  - **M1.3** — Vietnamese retrieval quality (Open Decision #9) resolved + VI eval cases (§"Phased Delivery Roadmap" M1; §"Open Decisions" #9). **Root-cause fix:** Vietnamese diacritics encode as NFC (precomposed) *or* NFD (decomposed combining marks); combining marks are Unicode `Mark` not `Letter`, so the letter/number tokenizer shatters decomposed words (`"Việt"`→`["vie","t"]`) — a query and document in different forms share almost no tokens, silently killing recall in **both** the vector (embedder) and keyword (`to_tsvector`) paths. Fix: **NFC-normalize at every text boundary** — embedder tokenizer + chunker (`@expertos/ai` new `text.ts` `normalizeText`/`tokenize`, single tokenizer definition shared by embedder/chunker/eval), and query text via the `retrievalQuerySchema` transform (`@expertos/shared` new `text.ts`, directive §1 boundary sanitization). **Decision:** retrieval is **cross-lingual/multilingual by default** (no language filter unless caller sets one); `'simple'` keyword config keeps VI undistorted. **Eval harness:** new `@expertos/ai` `eval/` — `evaluateRetrieval(goldenSet, opts)` reuses the production primitives (chunk→embed→cosine+keyword-overlap→`fuseHybrid` RRF) for a deterministic, DB-free recall@k / MRR / precision / hit-rate runner, plus `RETRIEVAL_GOLDEN_SET` (EN, VI-NFC, mixed EN-VI, and an NFD-vs-NFC regression case). Offline hashing embedder guards tokenization/normalization/fusion in CI; the **semantic** VI number (true cross-lingual recall) is measured out-of-band with the real model using the same fixtures (golden-set ownership/size/refresh = Open Decision #6, still open). This eval harness is the reusable seam M2.4 (voice fidelity) and M4 (citation resolvability) build on.
  - **M1.2** — Hybrid retrieval behind the `VectorStore` interface (§"Phased Delivery Roadmap" M1). `packages/ai` evolved the `VectorStore` contract to `retrieve(RetrievalRequest)` and added a pure, DB-free **RRF fusion** (`fuseHybrid`) that blends the vector + keyword ranked lists by position (no score-scale tuning), plus the retrieval value/filter types (kept dependency-free; mirror the shared/Prisma enums). `packages/shared` owns the canonical zod validation (`retrievalQuerySchema`/`retrievalFiltersSchema`/`chunkStatusSchema`, `status` defaults to `published`). `apps/api/src/retrieval`: `PgVectorStore implements VectorStore` — a thin `$queryRawUnsafe` driver doing vector (cosine `<=>` over the HNSW `chunks.embedding` index) + keyword (Postgres `websearch_to_tsquery`/`ts_rank` full-text over content+summary, `'simple'` config so VI isn't English-stemmed) with bound-param metadata filters (`status`/`language`/`scope`; `tenant_id` left to RLS); `RetrievalService` embeds the query with the same provider as ingestion and runs the store inside `RlsService.run` (tenant isolation). Mirrors M1.1's no-controller seam (the chat M3 / citations M4 will call `RetrievalService`). `formatVector` extracted to shared `database/vector.ts` (`toVectorLiteral`, reused by ingestion). **Integration (real pgvector) deferred to M11** per the testing strategy — unit tests mock the tx.
  - **M1.1** — Versioned ingestion pipeline (§"Phased Delivery Roadmap" M1; seed/CLI-loaded). Pipeline: validate (Zod `ingestionInputSchema` in `packages/shared`) → parse (`Parser` contract + `TextParser`/`CsvParser` + `ParserRegistry` in `apps/api/src/ingestion`, PDF/DOCX/XLSX deferred to M5 via `UnsupportedContentTypeError` seam) → chunk (`chunkText` overlapping word-window, `packages/ai`) → summarize (`Summarizer` iface: offline `ExtractiveSummarizer` default + `LlmSummarizer`) → embed (`HashingEmbeddingProvider` — deterministic offline 1536-dim dev driver; real OpenAI driver later behind same `EmbeddingProvider`) → persist as immutable `DocumentVersion` + `Chunk`s (`DocumentVersionRepository`, versioned per `(tenant,scope,sourceUri)`, embeddings written via raw SQL `UPDATE chunks SET embedding=$1::vector` since pgvector col is Prisma-Unsupported). `IngestionService` orchestrates inside `RlsService` (admin/system context) + best-effort `UsageLogService` (`ingest.embed`). Default providers shared by `IngestionModule` (DI tokens) and the `pnpm --filter @expertos/api ingest <manifest.json>` CLI loader via `ingestion.defaults.ts` (one composition root). **Adversarial review caught + fixed a bootstrap-breaking DI bug** (`IngestionModule` now `imports: [AuthModule]` for non-`@Global` `RlsService`; verified via `createApplicationContext` smoke — see LEARNINGS #5).
- Tests: 688 pass / 0 fail / 0 skip (shared 87, ui 3, db 9, ai 143, api 446). **M8.3 (revenue) adds `apps/api` +5** (`revenue.service.test.ts`: combines MRR/per-plan/series/AI-cost-margin with yearly→monthly normalization + BigInt coercion; zeros for an empty platform; counts an active subscriber with no configured price as 0 MRR; negative margin when AI cost exceeds net; window scoped consistently across the series SQL + cost aggregate) and **`apps/shared` +4** (`revenue.test.ts`: `revenueReportQuerySchema` default-12 / coerce-query-string / reject non-positive+over-cap / reject non-integer). `revenue.service.ts` 100% all metrics; the full `apps/api` parallel suite ran clean this session (53 suites / 446). **M8.2 adds `apps/api` +18** (`knowledge-draft.service.test.ts`: create tied-to-visible-conversation + 404-foreign-conversation + skip-lookup-when-none; list status-filter→where + empty-where + content-free summaries; get full-content + 404-missing; update partial-patch-while-draft + 409-non-draft; submit draft→expert_review + 409-non-draft; requestChanges expert_review→draft + 409-from-draft; reject from-draft/from-review/409-published; publish ingest-publish:true→published + idempotent-skip-when-document-exists + 409-not-under-review + EmptyDocument→400-leaves-unpublished) and **`apps/shared` +11** (`knowledge.test.ts`: `knowledgeDraftCreateSchema` trim+default-language+omit-absent-conversationId / keep-valid-conversationId+vi / reject-empty-content+non-uuid; `knowledgeDraftUpdateSchema` title-only+content-only / reject-empty-patch; `knowledgeDraftListQuerySchema` default-limit-50+coerce / reject-unknown-status). `knowledge-draft.service.ts` covered (gated `*.service.ts` 100% lines). **(Sandbox note: the full parallel `pnpm test` run cannot complete on this box — the native Prisma engine SIGILLs ~1 random worker per jest process and ts-jest intermittently falls back to a babel transform that can't parse `type` imports; counts above are confirmed per-suite/per-file in isolated runs with zero assertion failures — the documented aarch64/linuxkit quirk, restart the session for a clean parallel run.)** **M8.1 adds `apps/api` +16** (`knowledge.service.test.ts`: listDocuments status+scope filter→where + latest-version/count mapping + empty-where/null-latest; getDocument full-history isPublished mapping + 404; submit draft→expert_review + reject-non-draft + 404-missing; requestChanges expert_review→draft + reject-draft; approve publish-stamps+chunks-published+document-pointer (no prior) + supersede-prior (archive old row+chunks) + reject-non-review + 404-document-vanished; archive live-version chunks-archived+pointer-cleared + non-live-leaves-document + reject-non-published) and **`apps/shared` +4** (`knowledge.test.ts`: `knowledgeListQuerySchema` default-limit-50, coerce-limit+keep-filters, reject-over-cap, reject-unknown-status). `knowledge.service.ts` 100% lines/stmts/funcs, 96.4% branch (≥90 gate; one defensive optional-chain in the count mapper). **M7.3 adds `apps/api` +33** (`booking.service.test.ts` ×16: verification-error→400, unexpected-verify-error rethrow, unmodeled-event no-op, link-existing-by-bookingRef, cancellation-leaves-scheduledAt, flip-pending-recommended, flip-pending-without-overwriting-scheduledAt, create-outside-funnel, unmatched-no-user warns, no-email-skips-user-lookup, idempotent-redelivery no-op, P2002-race swallowed, non-unique rethrow; `reconcile` ×3: empty-poll default-lookback, explicit-since matched+duplicate tally, unmatched-applied-not-matched; `offline-tidycal-provider.test.ts` ×7: verify-JSON, malformed→error, poll-empty, parse created+date-coerce, cancelled→canceled, eventId-falls-back-to-bookingRef + null defaults, null/unknown-type/missing-ref→null; `http-tidycal-provider.test.ts` ×10: HMAC verify valid/missing/tampered/non-JSON, parse nested-contact+numeric-id, cancellation-aliases+bookingRef-fallback, null/unmodeled/no-id→null, poll-disabled-without-token, poll maps+synthetic-ids+cancellations, no-data-array tolerant) and **`apps/shared` +3** (`consultation.test.ts`: `bookingReconcileSchema` empty-body, ISO-since-coerced, unparseable-since rejected). `booking.service.ts` 100% all metrics (the offline/http drivers aren't coverage-gated — gate collects `*.service.ts` only — but are fully unit-tested). **M7.2 adds `apps/api` +9** (`recommendation.service.test.ts`: 404 when not the actor's, `maybe_later`/`ask_another` record without creating a consultation, `book` resolves the rule's type + creates/links a consultation + returns the link, `book` idempotent reuse, reuse with null type → null link, recreate when the linked consultation was SetNull'd, fall back to active default when the rule key is missing, `book` with no active type → null type/link) and **`apps/shared` +5** (`consultation.test.ts`: accepts each of book/maybe_later/ask_another, rejects the `pending` default, rejects unknown/missing). `recommendation.service.ts` 100% all metrics. **M7.1 adds `apps/ai` +15** (`recommendation/evaluate.test.ts`: no-fire null, high_intent whole-word match + matched-keyword carry, multi-word contiguous phrase, whole-word boundary non-match, topic matches in answer, high_intent NOT in answer-only, depth at threshold, unconfigured-threshold never fires, low_confidence on insufficient-knowledge regardless of citations, low_confidence at/below threshold, skip disabled rules, ignore empty/whitespace keywords, highest-priority wins, declared-order tie-break, NFC decomposed-VI match) and **`apps/api` +11** (`recommendation.service.test.ts` ×8: no-rules-no-write null, rules-but-none-fire null, fire→persist row + DTO, depth from true assistant-turn count, fall back to active default type when key missing, null type when no active type, degrade-to-null-on-tx-failure logs Error message, stringify non-Error throw; `chat.service.test.ts` ×3: recommendation threaded onto `done` for generated + cache-hit paths, null when no rule fires). `recommendation.service.ts` 100% all metrics (engine `evaluate.ts` isn't coverage-gated — gate collects `*.service.ts` only — but is fully unit-tested). **M6.5 adds `apps/api` +10**: `model-pricing.test.ts` ×6 (standard tier rate, premium ~20× standard + exact $0.018 modeled answer, mini-below-premium degrade win, embedding cheap + zero completion cost, unknown-model → standard tier never zero, default token counts), `usage-log.service.test.ts` ×4 (models cost_micros from tokens when omitted; named-model cache hit at 0 tokens → cost 0; named model with no token fields → cost 0; no model named → cost null). `usage-log.service.ts` 100% lines (model-pricing.ts isn't coverage-gated — gate collects `*.service.ts` only — but is fully unit-tested). **M6.4 added `apps/api` +27**: `lru-cache.test.ts` ×6 (live get/miss, TTL expiry-on-read, LRU eviction past capacity, re-set refreshes recency, delete, min-1-capacity), `response-cache.service.test.ts` ×9 (retrieval key whitespace/case-insensitive + forks on topK/status/language/scope + scope-order-insensitive; answer key forks on model/expert/language/topK; retrieval in-process round-trip; answer in-process hit skips DB / write-through replace-then-create / persistent fallback warms in-process + age-cutoff / full miss), `semantic-cache.store.test.ts` ×5 (lookup returns+derives provenance+bumps hits+age-bounds, miss returns null, row-lacks-model/citations fallback; store deletes-then-creates + drops empty chunkIds), `retrieval.service.test.ts` ×2 (cache hit skips embed/DB/usage/set; miss caches the fused results), `chat.service.test.ts` ×6 (cache hit serves w/o retrieve+model at zero cost; tier in key keeps degraded separate; write-through grounded after miss; not-cached w/ history / w/ uploads / uncited). `response-cache.service.ts` 100% all metrics; `chat.service.ts`/`retrieval.service.ts` 100%. **M6.3 added `apps/ai` +1** (`echo-llm-provider.test.ts`: default name `echo-dev` + overridable `echo-dev-mini` for the fair-use tier) and **`apps/api` +7**: `entitlement.service.test.ts` ×4 (meter+degrade past soft threshold / allow under it / hard-cap-before-degrade precedence / `getEntitlements` surfaces `softLimit` with remaining null), `entitlement.guard.test.ts` (the enforce-passes case rewritten to assert the gate decision is stashed on the request), `chat.service.test.ts` ×2 (degrade serves the cheaper model → persisted `model`/cost-log/`done.degraded`; standard path reports `degraded:false`), `chat.controller.test.ts` ×1 (the fair-use decision threads through to `answerStream({degraded})`). `entitlement.service.ts` 100% lines / 97.22% branch (≥90 gate). Earlier — M6.2 added `apps/api` +55: `billing.service.test.ts` ×27 (checkout: price resolved + customer reuse / no-prior-customer / 400 no-price / 400 null-price-id; portal: success / 400 no-account; webhook verify: WebhookVerificationError→400, unexpected error rethrow, null-event no-op; subscription sync: create-mapped-to-checkout-user, update-keeps-user + new plan/interval, status-only-when-price-unmapped, skip+warn no-user / unmapped-price-new-sub / unknown-user, monthly-interval fallback; ledger sync: append-resolved-to-customer, no-user-when-customer-unknown / -no-subscription, idempotent-dup-no-op, P2002-race-swallowed, non-unique-error-rethrow), `offline-payment-provider.test.ts` ×13 (checkout/portal URLs, cancel no-op, verify JSON/malformed, parseOfflineEvent subscription+transaction shapes/date-coerce/null-defaults/missing-required/unknown-kind), `stripe-payment-provider.test.ts` ×15 (signature-header parse, verify valid/missing/tampered/replay/non-JSON, parseEvent subscription-updated/deleted/missing-ids + invoice-paid/failed + charge-refunded + unhandled-type, checkout w/ email vs customer + no-url throw, portal + no-url throw, cancel DELETE). Earlier — M6.1 added `apps/api` +19: `entitlement.service.test.ts` ×14 (Free default + live-subscription plan resolution, boolean/metered/unlimited/no-window `getEntitlements` views with used/remaining, free-plan-not-seeded throw, `enforce` allow/deny across boolean-enabled/disabled/unknown/unlimited/no-window/under-cap/at-cap, and day/week/month window-start keying), `entitlement.guard.test.ts` ×4 (no-metadata no-op, missing-authUser 401, enforce-passes allow, enforce-throws propagation), `all-exceptions.filter.test.ts` ×1 (structured 402 object response echoed). M5.4 added `apps/ai` +1 (`citations.test.ts` upload `uploadChunkId`/`sourceLabel` carry-through) and `apps/api` +9: `retrieval.service.test.ts` ×2 (retrieveUploads conversation-scoped + usage-logged; persistent-only when no conversation), new `upload-chunk.store.test.ts` ×4 (row→chunk mapping, conversation-bound + expiry clause, persistent-only `OR false`, null sheet/cell for prose), `chat.service.test.ts` ×2 (uploads folded as upload citations after knowledge; not-insufficient when only an upload grounds), `conversation.service.test.ts` ×1 (upload citation persists via `uploadChunkId`, empty knowledge ids → null). `apps/api` gated `*.service.ts` still 100% lines (`upload.service.ts` 100/92.68/100/100; branch ≥90 gate met).
- Build: passing — `pnpm build` (turbo) builds all 7 workspaces.
- Gates: typecheck ✅, test ✅ (coverage gate met, 100%), lint ✅ (incl. stylelint), build ✅, deadcode (knip) ✅
- Next tasks (from PRD.md Task Manifest, Phase 1) — **M3 COMPLETE; M4 COMPLETE; M5 COMPLETE; M6 COMPLETE (M6.1–M6.5); M7 COMPLETE (M7.1–M7.3); M8.1 COMPLETE (API + admin UI); M8.2 COMPLETE (API + admin UI)**:
   1. **Rest of M8 — Admin & Expert portals (the `apps/admin` portal frame + auth + client now exist — build the remaining pages on them).** **M8.3 revenue reports DONE** (`apps/api/src/revenue/` + `apps/admin/app/revenue`). Remaining M8.3 sub-deliverables (each needs a new API route first, then a page): **plan-entitlement matrix editor** (M6.5 quotas + soft thresholds, no deploy — write path over `plan_entitlements`; the only **mutation** surface, build carefully), **recommendation-rules editor** (M7.1 `recommendation_rules`), **failed/low-confidence query inspector** over `answer_feedback` (read-only — easiest; reuse the admin cross-tenant RLS pattern `RevenueService` established). M8.5 = the first-class expert portal (consume the M2.3 `/voice-profiles` + the new M8.1 `/knowledge` routes + show **consultation conversions** over the M7 funnel: `consultation_recommendations`→`consultations`→`booking_webhook_events`). The **manual TidyCal reconcile** (`POST /consultation-bookings/reconcile`, admin-only) + unmatched `booking_webhook_events` (`matched=false`) want an admin surface here too. **Publish-time cache invalidation** (the M6.4 follow-up) hooks cleanly into `KnowledgeService.approve`/`archive` now that they're the single publish choke point.
   2. In parallel: remaining Phase-0 Open Decisions (#1, #3, product halves of #2/#6); deferred consumer-web UI — **entitlements/usage page consuming `/me/entitlements` via the `UsageMeter` primitive** (M6.3 shipped the API + UI primitive, no web page yet), history/saved-answers/search/feedback (M3.2–M3.4 shipped API only), **web upload UI** (temp/persistent mode picker POSTing to `/uploads`). The chat UI should also surface the `done.degraded` flag as a subtle fair-use note.

## Notes for next agent
- **M8.3 revenue seam (build the remaining M8.3 editors on this):** (1) **`RevenueService` (`apps/api/src/revenue/revenue.service.ts`) is the single revenue-read choke point + the admin cross-tenant RLS template** — it runs `RlsService.run` under an **admin** principal so the `is_admin` GUC reads all tenants (no `tenant_id` predicate). Copy this exact pattern for the **failed-query inspector** (read `answer_feedback` cross-tenant) — it's the easiest remaining sub-task. (2) **Aggregation rule:** Prisma `groupBy`/`aggregate` for anything Prisma can express (MRR, AI-cost sum); raw `$queryRawUnsafe` ONLY for `date_trunc`/`FILTER` time-series. **Coerce `BigInt` → `Number()` on every raw `sum`/`count`** (the gotcha — Postgres returns aggregates as BigInt; the `ts_rank` float path didn't hit it). (3) **Money convention:** integer **cents** on the wire; AI cost stays in `cost_micros` (÷ 1_000_000 = cents). MRR normalizes yearly prices ÷ 12. (4) **`RevenueModule` is read-only** (imports only `AuthModule`); the **matrix/rules editors are the mutation surfaces** — they need new write routes over `plan_entitlements`/`recommendation_rules` (both RLS-exempt config tables) + freeze-on-update care, and admin-client `update*` fns (don't add a client fn before its page consumer or knip fails). (5) **Publish-time cache invalidation** (the standing M6.4/M8 follow-up) is still unbuilt — clear in-process caches + `deleteMany` the tenant's `semantic_cache` rows inside `KnowledgeService.approve`/`archive` + `KnowledgeDraftService.publish`. (6) **Seam-tested with a mocked tx** — the real `date_trunc`/`FILTER` aggregate + the admin cross-tenant visibility join the M11 Testcontainers list (the raw-SQL caveat shared with conversation-search / `PgVectorStore`).
- **Admin portal seam (build the rest of M8 on this):** (1) **`apps/admin` is now a real Next app, not a scaffold** — `src/lib/firebase.ts` + `auth-context.tsx` (verbatim copies of web's; lazy Firebase, `useAuth`/`getIdToken`) + **`src/lib/admin-client.ts`** (the single typed fetch wrapper — add new admin routes here, it already surfaces the API's `{message}`/`{reason}` error body and attaches the Bearer token). New admin pages **must** wrap their body in **`src/components/AdminFrame`** (the `.shell` + sidebar + auth gate); add a nav entry in its `NAV` array. (2) **`src/lib/status-tone.ts`** is the single status→`.badge`-tone mapper — reuse `publishStatusTone`/`draftStatusTone`/`statusLabel` for any new status surface (e.g. voice profiles, consultations) rather than re-deciding tones. (3) **Auth is a UX gate only** — the API enforces `@Roles("expert")`/`@Roles("admin")` + RLS; a signed-in non-expert sees the queues but every call 403s (surfaced as the page error badge). M8.5 (expert portal) is the same frame + the M2.3 `/voice-profiles` routes; M8.3 (matrix/rules editors, revenue reports, query inspector) needs **new API routes first** (none exist for plan_entitlements editing / revenue aggregation / answer_feedback admin reads) — build the route then a page. (4) **"Mark valuable" (`POST /knowledge-drafts`) has no UI yet** — it belongs on a conversation viewer (M8.3/M8.5); when that lands, add a `createDraft` fn to `admin-client.ts` + the action (don't add the client fn before its consumer or knip fails). (5) **No page tests** — the repo has no Next page tests (web included); gates are typecheck/lint/build/knip. (6) **NEXT_PUBLIC_API_URL** (default `http://localhost:3001`) + the `NEXT_PUBLIC_FIREBASE_*` set configure the admin app, same as web; admin runs on port 3002.
- **M8.2 conversation-to-knowledge seam (build the admin UI on this):** (1) **`KnowledgeDraftService` (`apps/api/src/knowledge/knowledge-draft.service.ts`) is the single draft-lifecycle choke point** — `create`/`list`/`get`/`update` + the `submit`/`requestChanges`/`reject`/`publish` state machine over `KnowledgeDraftStatus` (`draft → expert_review → published`; `request-changes`→`draft`; `draft|expert_review → rejected`). Don't transition a `knowledge_drafts` row anywhere else; extend this. Exported from `KnowledgeModule` (which now also `imports: [IngestionModule]`). (2) **`publish` is the gate AND the only knowledge-creation path for drafts** — it calls `IngestionService.ingest(user, {sourceUri: \`draft://<id>\`, contentType: "text/plain", scope: "global_expert", language}, content, {publish:true})`, so a fresh **published** `document` + retrieval-visible chunks are created. Each draft has a **unique source URI**, so there's never a prior generation to supersede (unlike M8.1's `approve`); publish is **idempotent** via a `draft://<id>` document existence pre-check (skips re-ingestion on a retry after a partial failure — the only drift risk). If you change the publish path, preserve both the unique-URI invariant and the idempotency pre-check. (3) **"Mark valuable" (`create`) is the source-conversation entry** — `conversationId` is optional and **re-checked under RLS** (`conversation.findUnique` → 404 if invisible/foreign); `conversations` is `user_scoped` so a non-admin expert can't see a user's conversation (admin bypasses RLS), which is why M8.2 is admin-centric. The content is **client-supplied** (the admin UI assembles the Q&A text); the service stores it verbatim. (4) **Authz = `@Roles("expert")` + RLS tenant isolation** — `knowledge_drafts` is `tenant_only` (no per-expert ownership), same model as M8.1 knowledge. (5) **`language` column** (migration `20260601050000`) carries the draft's language into the published knowledge (VI drafts stay VI); `expertId` exists on the table but is **unused by M8.2** (documents have no expert link — reserved for future expert-authored attribution). (6) **DTOs in `packages/shared/src/knowledge.ts`** (list = `KnowledgeDraftSummaryDto` content-free, detail = `KnowledgeDraftDto` with content) — the admin UI (the open follow-up: a draft review queue + "Mark valuable" from the conversation viewer) consumes them. (7) **Publish-time cache invalidation** (the M6.4 follow-up) applies here too — a freshly-published draft can leave a stale cached answer until TTL; the clean hook is the same as M8.1 (clear in-process caches + `deleteMany` the tenant's `semantic_cache` rows after `ingest` in `publish`). (8) **Seam-tested with a mocked tx** (the real `knowledge_drafts`/`document` writes join the M11 Testcontainers list; no live DB this session — the migration SQL is a single additive enum column, not yet `migrate deploy`'d).
- **M8.1 knowledge publish-workflow seam (build the admin UI + M8.2/M6.4-invalidation on this):** (1) **`KnowledgeService` (`apps/api/src/knowledge/knowledge.service.ts`) is the single knowledge publish-workflow choke point** — `listDocuments`/`getDocument` (read) + `submit`/`approve`/`requestChanges`/`archive` (the `publish_status` state machine). Don't transition a `document_version` anywhere else; extend this. Exported from `KnowledgeModule` (imported by `AppModule`). (2) **`approve` IS the expert-review gate and the retrieval integration point** — it's the ONLY place chunks flip `pending → published` (M1.2 retrieval filters `chunks.status = published`), the document pointer is set, AND the **prior published version is superseded** (its row + chunks → archived) so retrieval never sees two generations. `archive` reverses it. **Ingestion never publishes chunks** — `DocumentVersionRepository.store({publish:false})` makes drafts with `pending` chunks; the seed/CLI `{publish:true}` path is the only other publisher (it short-circuits the gate, intended for seeding). If you add a second publisher, route it through `approve`'s chunk-flip + supersede logic or retrieval will drift. (3) **`document.status` mirrors the latest version's status; `publishedVersionId` independently tracks the live generation** — a doc can have v2 live (`publishedVersionId=v2`) while v3 is a `draft` (`document.status=draft`). Don't conflate them. (4) **Authz = `@Roles("expert")` (admin satisfies via hierarchy) + RLS tenant isolation** — knowledge has no per-expert ownership like voice profiles (it's tenant/scope-scoped), so there's no `assertOwnership`; any expert in the tenant can review/publish. If per-expert knowledge ownership is ever needed, add it in the service (the `VoiceProfileService.assertOwnership` template). (5) **DTOs live in `packages/shared/src/knowledge.ts`** (not api-local like `voice.types.ts`) on purpose — the **admin UI (apps/admin, still bare) consumes them**; that UI is the open M8.1 follow-up (the `.shell`, status `.badge` tones, the Submit/Approve/Request-changes/Archive queue). (6) **`ai_processing` status is reserved but unused by the workflow** — the synchronous ingestion goes straight to `draft`; if async embedding lands, set `ai_processing` during the embed and `draft` when chunks are ready. (7) **M6.4 publish-time cache invalidation** is the clean follow-up here: clear the in-process caches + `deleteMany` the tenant's `semantic_cache` rows inside `approve`/`archive` (the M6.4 note flagged this; `KnowledgeService` is now the hook). (8) **Seam-tested with a mocked tx** — the real `document`/`document_version`/`chunk` writes + the chunk-visibility `updateMany` join the M11 Testcontainers list with the stores.
- **M7.3 booking-confirmation seam (OD#10 resolved; build M8 admin reconcile UI on this):** (1) **`BookingService` (`apps/api/src/consultation/booking.service.ts`) is the single booking-sync choke point** — `handleWebhook(req)` (unauthenticated TidyCal callback, **system RLS context** via the private `runAsSystem`, the `BillingService.handleWebhook` template) + `reconcile({since?})` (admin missed-event poll). Don't sync bookings anywhere else; extend this. Exported from `ConsultationModule`. (2) **`TidyCalProvider` (`tidycal-provider.ts`) is the swap seam — no app code talks to TidyCal directly.** Offline default + `HttpTidyCalProvider` behind the `TIDYCAL_PROVIDER` token (`tidycal.defaults.ts`, swaps the real driver when `TIDYCAL_WEBHOOK_SECRET` is set; `TIDYCAL_API_TOKEN` enables the `listBookings` reconcile poll). A new provider = implement `verifyWebhook`/`parseEvent`/`listBookings` + map events onto the **`BookingEvent`** shape; `BookingService` is provider-agnostic. (3) **Idempotency = the `booking_webhook_events` ledger**, unique on **`[provider, eventId]`** (pre-check + P2002 catch, the M6.2 `transactions` pattern). It's a **dedicated RLS-exempt table** (not a column on `consultations`) on purpose — an unmatched booking can't create a consultation but must still be recorded so it doesn't vanish (the OD#10 no-vanish guarantee). A reconcile entry uses a synthetic `reconcile:<bookingRef>:<type>` eventId so re-polling is idempotent; don't key on the raw booking id (a created-then-cancelled booking would collide). (4) **Correlation order is `bookingRef` → email → create.** TidyCal links are static, so the inbound event matches first by `bookingRef` (a follow-up reschedule/cancel for a booking already linked), then by booking **email** → the user's most-recent pending `recommended` consultation (the M7.2 row). A booking outside the funnel creates a `booked` consultation; an email matching no user is recorded **`matched=false`** (kept for an admin to reconcile — the M8 surface to build). Email is matched in the system context (`user.findFirst` by email, most-recent). (5) **Webhook needs the RAW body** — `main.ts` already sets `rawBody: true` (M6.2); the controller reads `req.rawBody` + the `tidycal-signature` header. Route is **`@Public()`** (verified by HMAC-SHA256 over the raw body, not Firebase) — keep it public. (6) **`consultations.status` IS the user-facing pending state** (`recommended` = booking opened/pending → `booked` = webhook-confirmed → `canceled`); there's no separate "pending confirmation" flag. (7) **Fully testable offline** via the `OfflineTidyCalProvider` JSON envelope (`{eventType, bookingRef, email, scheduledAt}`) POSTed to `/consultation-bookings/webhook`; `reconcile` is offline-no-op (inject a stub provider to drive it). (8) **`HttpTidyCalProvider` REST poll needs live network** — deploy-time only (the Stripe `FetchStripeHttpClient` / GCS caveat); the HMAC verify + event parse + param construction ARE fully unit-tested. The TidyCal webhook event-name map + the `/bookings` poll shape are best-effort structural guesses — **verify against the real TidyCal docs/payloads when wiring the live account** (adjust `EVENT_NAME_MAP` + `toBookingEvent` only). (9) **Seam-tested with a mocked tx** (the real `booking_webhook_events`/`consultations` writes join the M11 Testcontainers list); the migration was validated against live Postgres this session.
- **M7.1 recommendation-funnel seam (build M7.2/M7.3 on this):** (1) **`RecommendationService` (`apps/api/src/consultation/recommendation.service.ts`) is the single recommendation choke point** — `recommend(user, input)` returns the `ConsultationRecommendationDto` or null and **persists a `consultation_recommendations` row** when a rule fires. Don't re-evaluate rules anywhere else; M7.2's "record the user's Book/Maybe-later/Ask-another response" extends this service (the persisted row `id` is the join key; the `recommendation_response` enum already exists — add the response column + a `respond(user, recId, response)` method + a `POST /consultation-recommendations/:id/respond` route). Exported from `ConsultationModule` (imported by `ChatModule`). (2) **The engine `@expertos/ai` `evaluateRecommendation` is pure/deterministic — rules are CONFIG, not code.** `recommendation_rules` is RLS-exempt config (admin-editable = M8.3, like `plan_entitlements`); the seed only populates launch defaults. To add a trigger you must extend `RECOMMENDATION_TRIGGERS` + the `RecommendationTrigger` DB enum + the `RecommendationTriggerValue` shared union **in lockstep** (the `matchRule` switch is exhaustive — TS will flag a missing case). (3) **Keyword matching reuses the shared `tokenize`** (NFC+lowercase whole-word) so the funnel can't drift from the rest of the text pipeline — keep keyword rules lowercased in the seed; a phrase matches a contiguous token run. (4) **Non-fatal contract:** `recommend` catches everything and degrades to `null` — a funnel hiccup must NEVER turn a delivered answer into an error (it runs *after* the answer streamed). Keep that try/catch around any new work. (5) **`depth` counts the conversation's TRUE assistant-turn count** (a `message.count`), not the token-windowed history the prompt replayed — so it measures real engagement. (6) **Both chat paths recommend** (generated + cache-hit in `ChatService.answerStream`); a cache hit is grounded + first-turn so topic/high_intent can still fire (depth/low_confidence won't on turn 1). (7) **API + persistence only — NO web UI yet.** The Book/Maybe-later/Ask-another prompt + TidyCal booking are M7.2; the DTO (`reason`, `consultationType.tidycalLink`) is on `ChatStreamEvent.done.recommendation` ready for the client. `ConsultationType.tidycalLink` is null in the seed (real link configured later, like the Stripe `provider_price_id`). (8) **Seam-tested with a mocked tx** (the real `consultation_recommendations` write + the `message.count` join the M11 Testcontainers list); but the migration + seed were validated against a live Postgres this session (all 4 rules present).
- **M6.5 cost-model seam (OD#4 resolved; build M8.3 reports + the real LLM driver on this):** (1) **`apps/api/src/observability/model-pricing.ts` is the single cost-model source** — `costMicrosFor(model, prompt, completion)`. Don't compute cost anywhere else; when the real OpenAI/Anthropic driver lands, update the model ids + USD/1M rates + the modeled answer size **here** and every usage row reprices automatically. (2) **`UsageLogService.record` auto-derives `cost_micros`** from `model` + token counts when the caller omits it (explicit `costMicros` still wins). So a new usage caller gets costed for free just by passing `model`. A **named-model 0-token row = cost 0** (cache hit / degrade margin win, visible in the ledger); **no model named = cost null** (nothing to price, e.g. a pure `cache.hit` marker). (3) **The OD#4 worst-case finding:** premium answer ≈ $0.018, cost-negative ≈ 520 premium-model answers/mo, so Premium `ask_question` is `limit:null` + **`softLimit:500`** → degrade past it (the degrade threshold, not a hard cap, protects margin). **The numbers live in `packages/db/prisma/seed.ts` MATRIX (admin-tunable via the M8.3 editor — no deploy).** Free 10 / Plus 200 hard cap / Premium softLimit 500, all /month. (4) **`model-pricing.ts` is NOT coverage-gated** (the apps/api gate collects `*.service.ts` only) but is fully unit-tested (`model-pricing.test.ts`). (5) **Per-plan premium-model selection is NOT wired** — the codebase has only the standard + degraded providers (M3.1/M6.3); a Premium user gets `echo-dev` (standard) unless degraded. The $3/$15 "premium model" is an OD#4 *modeling* assumption; actually routing Premium to a premium model is a future milestone (the cost model already prices it when that model id starts being logged). (6) **`cost_micros` is now the margin signal M10 analytics + the M8.3 reconciliation view consume** — sum it per user/window against `plan_prices.amountCents` to see realised margin.
- **M6.4 cache seam (build M6.5 / M8 invalidation on this):** (1) **`ResponseCacheService` (`apps/api/src/cache/response-cache.service.ts`) is the single caching choke point** — `retrievalKey`/`getRetrieval`/`setRetrieval` (retrieval layer) + `answerKey`/`lookupAnswer`/`storeAnswer` (answer + persistent semantic layer). Don't hand-roll a cache key or LRU anywhere else; extend this. Exported from `CacheModule` (imported by `RetrievalModule` + `ChatModule`). (2) **Keys are pure string composites** (`join` on `\n`, query text run through `normalizeKey` = NFC+lowercase+collapse-whitespace). The **answer key includes the model tier** — this is the entitlement-correctness guarantee (a degraded answer never serves a standard-tier user); never drop `model` from the key. The retrieval key includes topK + status/language/scope (sorted). (3) **Cacheability is decided in `ChatService.answerStream`: `history.length === 0 && uploads.length === 0`.** A turn with prior context or the user's private uploads is user-specific → never cached. If you add a "session memory" or make uploads shareable, that predicate is the line to revisit. (4) **Quota is NOT touched by the cache** — the entitlement guard reserves one unit at the controller before `answerStream` runs, so a cache hit neither double-counts nor refunds (a hit still consumes quota, intended). A cache hit records `chat.answer` usage at **0 tokens** (the margin win is visible as zero model cost). (5) **Only grounded answers (≥1 citation) are cached.** Insufficient/uncited answers are deliberately not pinned. (6) **Invalidation today is TTL-only** (`cache.config.ts`: retrieval 5 min, answer 10 min in-process, semantic 24 h age cutoff). **Publish-time invalidation is the M8 follow-up** — when a `document_version` is published/unpublished, the tenant's cached answers can go stale until they age out; the clean hook is to clear the in-process caches + `deleteMany` the tenant's `semantic_cache` rows on publish. (7) **`PgSemanticCacheStore` matches EXACTLY on the normalized key (+ model)** — the **embedding-cosine *approximate* ("semantic") match is deferred** to the real embedder / M11 Testcontainers (the `semantic_cache.embedding` column + HNSW index are reserved for it; same caveat as `PgVectorStore`). The new `semantic_cache.citations` jsonb column stores the resolved citations verbatim so a hit rebuilds the `done` event + persisted rows faithfully. (8) **`LruCache` is the documented Redis swap point** — when Memorystore lands (PRD "Redis when volume justifies it"), replace the in-process `Map` behind `get`/`set`; `cache.config.ts` becomes its config. Seam-tested with a mocked tx (the real `semantic_cache` read/write joins the M11 list).
- **M6.2 billing seam (build M6.3/M6.4/M8.3 on this):** (1) **`BillingService` (`apps/api/src/billing/billing.service.ts`) is the single billing choke point** — `createCheckout`/`createPortal` (authenticated, under `RlsService.run`) + `handleWebhook` (unauthenticated provider callback, **system RLS context** via the private `runAsSystem` = `applyRlsContext({tenantId:GLOBAL, isAdmin:true})`, mirroring `AuthService.resolveUser`). Don't re-sync subscriptions/transactions anywhere else; extend this. Exported from `BillingModule` for the M8.3 revenue reports / reconciliation queries to reuse. (2) **`PaymentProvider` (`payment-provider.ts`) is the swap seam — no app code imports the Stripe SDK directly.** Offline default + Stripe driver behind the `PAYMENT_PROVIDER` token (`billing.defaults.ts`, swaps Stripe when `STRIPE_SECRET_KEY`+`STRIPE_WEBHOOK_SECRET` are set). A new provider = implement the 5 methods + map its events onto the **`BillingEvent`** union (`SubscriptionChange`|`LedgerEntry`); `BillingService` is provider-agnostic. (3) **Idempotency:** subscriptions **upsert by `providerSubId`** (naturally idempotent); transactions are unique on **`[provider, providerRef]`** where `providerRef` = the **provider event id** (stable across retries) — pre-check + P2002 catch. Keep this; don't key the ledger on invoice id (a failed-then-paid invoice would collide). (4) **Webhook needs the RAW body** — `main.ts` sets `NestFactory.create(.., { rawBody: true })`; the controller reads `req.rawBody`. Stripe signature = HMAC-SHA256 over `${t}.${rawBody}`, so any body-reparse breaks it. The route is **`@Public()`** (verified by signature, not Firebase) — keep it public or the guard 401s Stripe. (5) **Redirect URLs are server-chosen** from `WEB_APP_URL` (default `http://localhost:3000`) — never accept success/cancel/return URLs from the client (open-redirect guard). (6) **`subscriptions.provider_price_id` is null in the seed** — real Stripe price ids come from the dashboard, set via the M8.3 admin matrix editor; until then `createCheckout` `400`s ("no purchasable price"). **The webhook→DB-sync path is fully testable offline** via the `OfflinePaymentProvider` JSON envelope (`{kind:"subscription"|"transaction", …}`) POSTed to `/billing/webhook` — use this for a local end-to-end demo without Stripe. (7) **M6.3 hook:** now that a live subscription resolves a real plan, the `/me/entitlements` meter shows a non-Free quota; the fair-use "degrade-don't-block" for metered-unlimited still belongs in M6.3 (M6.1's `enforce` allows unlimited outright). (8) **Stripe driver REST transport (`FetchStripeHttpClient`) needs live network** — not exercised in CI (deploy-time / M11 caveat, same as the GCS storage driver); the signature-verify + event-parse + REST-param-construction logic IS fully unit-tested. The driver currently handles `customer.subscription.{created,updated,deleted}` + `invoice.payment_{succeeded,failed}` + `charge.refunded`; `checkout.session.completed` is intentionally **not** needed (we stamp `subscription_data.metadata.userId` at checkout so the subscription.* events carry the user mapping). Add new event types in `parseEvent` only.
- **M6.1 entitlement seam (build M6.2/M6.3 on this):** (1) **`EntitlementService` (`apps/api/src/entitlements/entitlement.service.ts`) is the single entitlement choke point** — `enforce(user, feature)` (guard path) + `getEntitlements(user)` (`/me/entitlements`). Don't re-resolve plans/quotas anywhere else; extend this. The service is **exported** from `EntitlementsModule` for direct in-process use (e.g. M6.3 metered consumption outside a route). (2) **Runtime reads the DB matrix, NOT a code default.** `plan_entitlements`/`features`/`plans` are RLS-exempt config (admin-editable = M8.3); the seed (`packages/db/prisma/seed.ts`) only populates defaults. The `FeatureKey` union in `packages/shared/src/entitlements.ts` types the decorator and must stay in lockstep with the seed's `FEATURES` list (drift = the guard fail-closes an unknown key). (3) **Plan resolution pins `userId`** in `resolvePlan` (and the usage-counter composite keys include `userId`) **on purpose** — an `admin` actor bypasses RLS, so a bare `findFirst` would grab a peer's subscription. This is self-lookup by natural key, NOT the isolation predicate the directive bans. (4) **Metering = reserve-before-work, in-transaction.** `enforce` upserts-increments the `usage_counters` row for the current window then throws `402` if over cap — the throw rolls the increment back (same `RlsService.run` tx). So a *successful* gate consumes quota even if the downstream work later fails (intended anti-abuse). The guard runs in its **own** tx, separate from chat persistence. Window-start keying is UTC (`day`/`week`=Mon/`month`) in the private `currentWindowStart` — when a real session/timezone concept lands, that's the line. (5) **`enforce` allows metered-unlimited outright** (Premium `limit: null`). The PRD's "degrade to a cheaper model instead of blocking" for fair-use is **M6.3** — add it there, not by making the gate block. (6) **`subscriptions` is read but never written yet** — everyone resolves to Free until **M6.2** wires checkout/webhooks to populate it. (7) **402 contract:** thrown as `HttpException(EntitlementDeniedPayload, 402)`; the **`AllExceptionsFilter` now echoes structured HttpException object bodies verbatim** (a general improvement — any structured error survives now, not just 402). Frontend upgrade modal (M6.2/M6.3) reads `reason`/`upgradeOptions`/`remainingQuota` off the flat error body. (8) **Seam-tested with a mocked tx** (the upsert-increment-rollback race-safety + the real `usage_counters` unique need the M11 Testcontainers pass, same caveat as the stores). The guard is a global `APP_GUARD` — its cross-module ordering after the auth guards relies on `EntitlementsModule` importing `AuthModule` (keep that import).
- **M5.4 upload-retrieval / upload-citation seam (M5 is now closed):** (1) **Upload folding lives in `RetrievalService.retrieveUploads` + `PgUploadChunkStore`** (`apps/api/src/retrieval/upload-chunk.store.ts`), called by `ChatService.answerStream` *after* knowledge `retrieve`. **Scope rule = M5.2 mode:** `persistent` always; `temporary` only for the **current `conversationId`** and unexpired. If you add a "session" concept distinct from a conversation, that's the line to change. **Isolation is the `uploaded_files` JOIN under RLS** (`upload_chunks` is `tenant_only`; `uploaded_files` is `user_scoped`) — never add a manual `user_id`/`tenant_id` predicate. (2) **An upload fact has empty `chunkId`/`documentVersionId`** and provenances via `uploadChunkId`; `ChatService` builds them at `[N+1..]` *after* knowledge `[1..N]` (stable numbering). `PromptFact`/`CitationSource`/`ResolvedCitation` now carry optional `uploadChunkId`/`sourceLabel`/`kind` — `buildCitations` resolves an upload marker identically to a knowledge one. **`persistTurn` coalesces empty ids → null** (a uuid column rejects `""`); keep that. (3) **`sourceLabel` (`filename · sheet!cell`) is only on the live `done` event** — the history read path (`loadCitations`) re-hydrates `kind:"upload"` (from `uploadChunkId`) but NOT the label (no column stores it). If a history view needs the label, JOIN `upload_chunks`→`uploaded_files` in `loadCitations` (the chunk may be `SetNull`'d, so handle a missing row). (4) **Two embeds per chat turn now** (knowledge + upload), each usage-logged (`retrieve.embed` + `upload.retrieve.embed`). A shared-vector optimization is an open follow-up — fine to leave; the cost is one short-text embed. (5) **Still seam-tested only** (mocked tx) — the real `upload_chunks ⋈ uploaded_files` cosine + the mode/expiry WHERE join the M11 Testcontainers list with `PgVectorStore`/`PgExpertStore`. (6) **No web upload UI yet** — uploads work end-to-end on the API (store→index→fold→cite) but `apps/web` has no file picker; that + the temp/persistent mode toggle is the open consumer work.
- **M5.3 spreadsheet seam (build M5.4 on this):** (1) **The provenance channel is `ParsedDocument.chunks?: ParsedChunk[]`** (`{content, sheetName?, cellRef?}`, `apps/api/src/ingestion/parser.ts`). A parser that pre-segments emits `chunks`; `UploadService.buildIndexedChunks` persists them **verbatim** (no `chunkText`), carrying `sheetName`/`cellRef` into `upload_chunks`. **M5.4 read path:** `upload_chunks` now has populated `sheet_name`/`cell_ref` — surface them on the upload `Citation` (a sheet/cell label like `Q1 KPIs!A2:B2`) when you fold upload chunks into retrieval + set `Citation.uploadChunkId`. CSV chunks have `sheetName = null` (flat) but still a `cellRef`. (2) **One renderer, one cap:** `parsers/spreadsheet.ts` `renderSheets(SheetTable[])` is the single place row→chunk formatting + the `MAX_SPREADSHEET_ROWS=5000` cap live — both CSV and XLSX go through it; extend there, not per-parser. Rows beyond the cap are dropped from the index (file still stored) — a deliberate untrusted-boundary cost guard (one embedding/row). (3) **XLSX = dependency-free** `parsers/xlsx-parser.ts` + `parsers/zip.ts` (`node:zlib`, read-only, 32 MiB per-entry inflate cap, `InvalidZipError` on malformed). It extracts the **real stored `<v>`** (not formatted display) — that's the "real numeric values" guarantee; if a future need wants the *formatted* string it requires `styles.xml`/number-format parsing (not done, deliberate). The `Parser` seam still lets a Python worker replace it if coverage falls short (PRD hybrid-ready note). (4) **Malformed/spoofed binary = stored, not 500:** `buildIndexedChunks` try/catches `parser.parse` → a file that passes the magic-byte sniff but isn't a real container is stored with `chunkCount 0` + a warn log, NOT a request failure. Keep this — uploads are attacker-controlled. (5) **PDF/DOCX parsers are still NOT built** (stored-but-0-chunks) — when added, register them in `createDefaultParserRegistry` and (for DOCX tables) consider emitting `chunks` too; PDF is prose → flat `text` is fine. (6) Ingestion (`IngestionService`) now also resolves XLSX via the shared registry (uses `parsed.text` → `renderText` output), so seed/CLI XLSX ingestion works; CSV `text` is byte-stable (no regression).
- **M5.1 upload seam (build M5.2/M5.3/M5.4 on this):** (1) **`UploadService.upload(user, UploadFilePart, {conversationId?})`** (`apps/api/src/uploads/upload.service.ts`) is the single upload choke point — validate→scan→store→persist, all branchy logic here (controller is thin, coverage-gated only on the service). Extend it for modes; don't add a second upload path. (2) **It stores the RAW FILE + a validated `uploaded_files` row — it does NOT parse/chunk.** Parsing into `upload_chunks` is M5.2/M5.3. **This is where the M1.1 `ParserRegistry` `UnsupportedContentTypeError` seam finally gets real PDF/DOCX/XLSX parsers** (M5.1 only allowlists those MIME types + sniffs their magic bytes; it never calls a parser). (3) **Two swappable contracts, offline defaults, one composition root (`upload.defaults.ts`)** — `StorageProvider` (default `InMemoryStorageProvider`, `memory://` URI, keeps bytes by key so a future parse step can read them back; **add a `get`/`download` method + the real GCS driver in M5.2**) behind `STORAGE_PROVIDER`, and `MalwareScanner` (default `SignatureMalwareScanner`, EICAR-only) behind `MALWARE_SCANNER`. Swap real drivers here, mirroring `EMBEDDING_PROVIDER`. (4) **Validation is layered (uploads are an untrusted boundary):** size (empty=400, >10 MiB `MAX_UPLOAD_BYTES`=413) → MIME allowlist (`UPLOAD_TYPES` in `upload-content-types.ts`, else 415) → **extension-must-match-MIME** (anti-spoof, 400) → **magic-byte sniff** for binary formats (`%PDF`, `PK` for OOXML, 400) → malware scan (unclean=422, **never stored**). Keep this order. (5) **`mode` is NOT in the request yet** — every M5.1 upload persists under the DB default `temporary`/`temporary_upload`. M5.2 adds `mode` to `uploadCreateSchema` + the divergent retention (`retentionDays`/`expiresAt`) vs indexing behavior; the DTO already carries `mode` so adding it is non-breaking. (6) **Isolation = RLS** — `uploaded_files` is `user_scoped`, so persistence inside `RlsService.run` is the boundary (no manual `tenant_id`/`user_id`); an attached `conversationId` is re-checked via the `user_scoped` `conversation.findUnique` (the `SavedAnswerService` template) **before** bytes are stored (no orphan on a rejected attach). (7) **Multipart:** `UploadController` uses `FileInterceptor` (multer default memory storage gives `buffer`; `limits.fileSize` is the streaming DoS guard); `MultipartFile`/`UploadFilePart` are **structural types** so apps/api takes no `@types/multer` dep (same pattern as the chat `SseResponse`). multer is a transitive dep of `@nestjs/platform-express` and resolves from its context at runtime (not hoisted to apps/api — verified). (8) **Persist-then-read caveat:** the real DB write path is seam-tested with a mocked tx (same M11 Testcontainers caveat as the other stores).
- **M4.2 sources-drawer / citation read-path seam (build M5 + history UI on this):** (1) **`ChatCitationDto.kind` is now on the wire** (`"knowledge"|"upload"`, `packages/shared/src/chat.ts`) — set on the live `done` event from `ResolvedCitation.kind` (M4.1) and on the read path. The web drawer maps it straight to the `Cite` `variant` (crimson knowledge / info-blue upload). When M5 adds upload citations, persist `uploadChunkId` and `loadCitations` already derives `kind: uploadChunkId ? "upload" : "knowledge"` — no DTO change needed. (2) **`persistTurn` now writes the TRUE marker ordinal** (the M4.1 bug is fixed): `TurnCitation.ordinal` flows from `ResolvedCitation.ordinal`, so a sparse list (lone `[2]`) stores ordinal 2. **Do not reintroduce a loop-index ordinal.** (3) **`ConversationService.get` re-hydrates citations** via private `loadCitations(tx, assistantMessageIds)` → `Map<messageId, ChatCitationDto[]>` (one `citation.findMany`, ascending by ordinal). `ChatMessageDto.citations` is now a **required** array (empty for user msgs / uncited answers) — the history detail view can render the same drawer the live turn shows. The shared nullable `chunk_id`/`document_version_id` columns coalesce to `""` on the read path (knowledge rows are always non-null; this only guards the M5 upload shape that provenances via `uploadChunkId`). (4) **Render-after-resolve lives in the web `AssistantAnswer` component** (`apps/web/app/chat/page.tsx`): `renderAnswer` turns `[n]` → clickable `.cite` ONLY when `message.done` AND the ordinal resolves to a citation; an unresolvable `[9]` stays plain text. Click-to-passage = `scrollIntoView` + `.source.active` highlight on the matching drawer row. **This is the structural realization of OD#7 (M4.3)** — keep markers non-interactive mid-stream. (5) **New `.sources`/`.source`/`.source.active` styles in `ds.css`** (packages/ui, token-only) — reuse them for the admin/history drawers; the `Sources` heading uses the existing global `.label` class. (6) The drawer shows `document_version_id` as the provenance line (`source: <id>`); when a real "click-to-open-the-document-passage" deep link exists (M8 knowledge viewer), wire it off that id + `chunkId`.
- **M4.1 citation-builder seam (build M4.2 on this):** (1) **`buildCitations({ answer, citations })` from `@expertos/ai` (`prompt/citations.ts`) is the single chunk-resolvability enforcement point** — pure/deterministic (no IO/clock/RNG), mirrors `buildAnswerPrompt`. It runs on the COMPLETE post-stream answer in `ChatService.answerStream` (after the delta loop), NOT per-delta. Don't re-resolve markers anywhere else; extend this. (2) **Referenced-only + keep-ordinal:** the `done` event now emits ONLY the sources a surviving marker cited (de-duped, ascending), and **never renumbers** — `ResolvedCitation.ordinal` is the marker the model wrote (so it can be non-contiguous, e.g. just `[2]`). M4.2's drawer should show gap-free 1..k display as a *presentation* concern, not by renumbering the source list. (3) **The guarantee covers both the list AND the text:** `built.text` has unresolvable markers (`[0]`/`[99]`/hallucinated) stripped + whitespace squeezed; that sanitized text is what's persisted, so a re-hydrated history answer can never show a dangling `[n]`. (4) **`insufficientKnowledge` stays `facts.length === 0`** — a retrieval-side signal, deliberately NOT `built.citations.length === 0` (a grounded answer that happens to cite nothing must not flip it). Don't couple them. (5) **Persisted-ordinal caveat (fix in M4.2 if you add a citation read path):** `ConversationService.persistTurn` writes each citation row's `ordinal` as its loop index `i+1`, so a filtered non-contiguous list stores row ordinal 1 for a lone `[2]`. Harmless today (no read path re-hydrates citation ordinals; the wire DTO carries the true ordinal), but a sources-drawer read path MUST persist/carry the true marker ordinal — either pass an explicit ordinal into `persistTurn` or store `ResolvedCitation.ordinal`. (6) **`kind: "knowledge"|"upload"`** is reserved on `ResolvedCitation` (defaults `knowledge`) for the M5 upload-citation distinction but deliberately NOT yet on the wire `ChatCitationDto` — add it to the DTO in M4.2/M5 alongside its `.cite` variant consumer (adding an unconsumed optional field early risks knip). (7) **Grammar lockstep:** the marker parser reads the model ANSWER, not the SOURCES block, so `answer-prompt.ts` + `echo-llm-provider.ts` are untouched; if you ever change the SOURCES `[n]` formatting, the echo provider's `/\[(\d+)\]/g` parser must move in lockstep (unchanged here).
- **M3.5 context-window seam (OD#8 resolved):** (1) **`loadHistory` now windows by token budget, not message count.** The policy + its four decisions live in the `HISTORY_TOKEN_BUDGET` (1500) doc comment in `conversation.service.ts`; `HISTORY_MAX_MESSAGES` (40) is only a DB-read backstop, NOT the cap (don't reintroduce a count cap as the real limit). The estimate is `estimateTokens` from `@expertos/ai` — the **same** word→token heuristic as ingestion chunking; when the real tokenizer lands, replace that one helper and both ingestion sizing and context-windowing move together. (2) **Whole messages, newest-first, ≥ latest.** A message is kept in full or not at all; the single most-recent message is always carried even if it exceeds the budget (the `windowed.length > 0` guard) so a follow-up never loses its antecedent. Windowing is message-level, not turn-level — the oldest kept message can be an assistant reply whose question fell outside the window; acceptable as context, documented. (3) **Deterministic/offline by design** (no LLM, zero cost — like `deriveTitle`). **Summarization is deferred** (documented seam in the constant comment): if it lands, cheap model + must NOT summarize away a concierge correction (M9). Because the window keeps the most-recent turns and a concierge correction enters as recent context, truncation of older turns is already M9-safe today. (4) To re-tune cost/quality, change `HISTORY_TOKEN_BUDGET` in one place; it bounds only the *replayed history* portion of the prompt (system message + freshly-retrieved facts + new user message are separate and unbounded by this).
- **M3.4 feedback / insufficient-knowledge seam:** (1) **`AnswerFeedbackService` (`apps/api/src/chat/answer-feedback.service.ts`) is an UPSERT, not create-or-409** — this is the deliberate divergence from `SavedAnswerService`. A bookmark is a one-shot toggle (duplicate=409); feedback is a mutable verdict (flip 👍↔👎, revise reason), so `submit` updates the existing `(userId,messageId)` row instead of conflicting, and `reason` is cleared to null when omitted on resubmit. The **ownership shape is identical** to the bookmark template: take only `messageId`, require `message.role==="assistant"` (→404), then `conversation.findUnique` on the `user_scoped` table is the real boundary (peer's chat is RLS-invisible →404). Never add a manual `tenant_id`/`user_id` predicate. `remove` keys on `(userId,messageId)` (retract by message, not by feedback-row id — the controller exposes `DELETE /answer-feedback/:messageId`). (2) **The insufficient-knowledge signal is `facts.length === 0` on the `done` event**, computed in `ChatService.answerStream` — NOT a model-emitted flag and NOT a confidence score. Zero retrieved sources is the deterministic proxy for "the INSUFFICIENT-KNOWLEDGE prompt rule governed this answer" (the echo provider already emits that text). If/when a real LLM lands that can be insufficient *even with* sources, revisit: the flag is currently a retrieval-side signal, not an answer-content signal. The turn is still persisted (it's a real answer) with empty `sourceVersionIds`/citations. (3) **No web UI** — feedback buttons + the graceful next-step surface are deferred (M3.2–M3.4 are all API-only; consume via `apps/web/src/lib/chat-client.ts` when built — don't add unused exports or knip fails). (4) **`answer_feedback` has no `conversationId` column** (unlike `saved_answers`) — the conversation is derived only for the ownership check, never stored; `AnswerFeedbackDto` is `{id, messageId, helpful, reason, createdAt}`. The admin "failed/low-confidence query inspector" (M8.3) will add its own admin-side read path over these rows.
- **M3.3 conversation-search seam:** (1) **`ConversationService.search`** is the **first raw-SQL path in this service** — it uses `tx.$queryRawUnsafe(SEARCH_SQL, q, limit, offset)` (the `SEARCH_SQL` constant + `toConversationSearchResult` mapper live at the bottom of `conversation.service.ts`). Raw SQL is required because `ts_rank`/`ts_headline` have no Prisma Client expression (same reason the M1.2 `PgVectorStore` keyword path is raw); the Client-method reads (`list`/`get`/`rename`) stay as-is. **Isolation is RLS, not a `where`** — the `conversations ⋈ messages` join intersects a `user_scoped` table with a `tenant_only` table, so the result is exactly the actor's own messages; never add a manual `tenant_id`/`user_id` predicate. (2) **The snippet is NOT HTML** — `ts_headline` is configured with `StartSel=«,StopSel=»` instead of the default `<b>…</b>`, so the API stays markup-free (directive §1). The deferred **web** search UI must HTML-escape the message/snippet as text (then optionally restyle the `«»` markers); do NOT `dangerouslySetInnerHTML` the snippet. (3) **Route order matters** — `@Get("search")` is declared **before** `@Get(":id")` in `ConversationsController` or NestJS captures `search` as a conversation id (and `ParseUUIDPipe` 400s it). Any new literal `/conversations/<word>` route has the same constraint. (4) **GIN indexes** are in migration `20260601000000_conversation_search_indexes`; their `to_tsvector('simple', …)` expressions must stay byte-identical to the ones in `SEARCH_SQL` or Postgres won't use them. (5) **Seam-tested only** (mocked `$queryRawUnsafe`) — the actual `ts_rank`/`ts_headline`/LATERAL query + GIN-index usage join the M11 Testcontainers list with `PgVectorStore`/`PgExpertStore`.
- **M3.2 history / saved-answer seams (build M3.4+ on these):** (1) **`ConversationService`** now owns history reads too — `list`/`get`/`rename` alongside `loadHistory`/`persistTurn`. All use Prisma **Client model methods** inside `RlsService.run`; `conversations` is `user_scoped` so list/get need **no `where` for isolation** (RLS does it) — `get`/`rename` still call `requireConversation`/a null-check to turn an invisible peer row into a 404. M3.3 full-text search should be a new method here (or a sibling service) over `messages.content`; reuse the M1.2 `'simple'` `to_tsvector`/`websearch_to_tsquery` keyword pattern (raw SQL is required there because of `ts_rank`, unlike the Client-method reads). (2) **Auto-title is `deriveTitle(firstQuestion)`** (private pure fn in `conversation.service.ts`) — set on conversation **create only**, inside `persistTurn`. It is **deterministic/offline by design** (no LLM, no cost) — if a future milestone wants LLM-generated titles, swap it there, but keep a deterministic fallback. Users override via `PATCH /conversations/:id`. (3) **`SavedAnswerService` is the bookmark-ownership template for M3.4 feedback** — both `saved_answers` and `answer_feedback` are `user_scoped` with a `(userId,messageId)` unique, and a message is only `tenant_only`. So to act on "an answer", take **only `messageId`** from the client, look up the message (must be `role:"assistant"`), then look up its conversation via the **`user_scoped`** `conversation.findUnique` — a null there is the real ownership boundary (→404). Duplicate → 409. Copy this exact shape for `AnswerFeedback` in M3.4. (4) **Controllers stay thin** (only `*.service.ts` is coverage-gated): `ConversationsController` + `SavedAnswersController` just validate via `ZodValidationPipe(sharedSchema)` and delegate. New shared chat DTOs/schemas live in `packages/shared/src/chat.ts` (exported from the index) — `z.coerce` pagination, lengths per directive §1.1. (5) **Web UI is NOT built** — M3.2 shipped the API + persistence only (precedent: M2.3 routes before M8.5 portal). The consumer `apps/web` history sidebar / saved-answers view is open follow-up; when added, consume these endpoints via `apps/web/src/lib/chat-client.ts` (don't add unused exports before the UI or knip fails).
- **M3.1 chat seams (build M3.2+ on these):** (1) **`ChatService.answerStream(user, ChatRequestInput)`** (`apps/api/src/chat/chat.service.ts`) is the single chat entry point — an async generator yielding `ChatStreamEvent`s (`delta` → `done` | `error`). It catches its own errors and yields a terminal `error` event (so the controller loop never throws after headers are sent). Don't re-implement retrieval/voice/prompt wiring elsewhere — extend this. (2) **`ConversationService`** (`conversation.service.ts`) owns ALL chat persistence; it uses Prisma **Client model methods** inside `RlsService.run` (the `DocumentVersionRepository`/`VoiceProfileService` pattern — conversations/messages/citations have no Unsupported columns). `loadHistory` windowing is now the **token-budget policy** (`HISTORY_TOKEN_BUDGET`) resolved in M3.5 — see the M3.5 seam note above (the interim `HISTORY_LIMIT = 10` cap was retired). A full turn (conversation create-if-new + user msg + assistant msg + citations) is one transaction so a mid-stream failure leaves no orphan. **Continuing a conversation you don't own throws 404** (RLS makes a peer's `user_scoped` conversation invisible). (3) **Streaming transport = raw SSE over Express `@Res()`** (NOT NestJS `@Sse()`/RxJS, NOT WebSockets) — `ChatController` writes `data: ${JSON.stringify(event)}\n\n` per generator event. Keep controllers/modules thin (apps/api coverage gate only collects `*.service.ts`); push branchy logic into the services. (4) **OD#7 is satisfied structurally**: `prompt.citations` (the `@expertos/ai` builder's list) is the citation source of truth; the `done` event maps it to `ChatCitationDto[]` (`ordinal = i+1`) and the web renders them only after the stream ends. The M4 citation builder MUST resolve markers against this same array. (5) **LLM swap point:** `createDefaultLlmProvider()` in `ingestion.defaults.ts` returns the offline `EchoLlmProvider`; replace it with the real OpenAI/Anthropic driver there (implement `complete` + optionally `completeStream`; if `completeStream` is absent `ChatService` falls back to `complete`). The echo provider's INSUFFICIENT-KNOWLEDGE-on-zero-sources behavior is what M3.4's UI path should surface. (6) **Web:** `apps/web/src/lib/chat-client.ts` (`streamChat` SSE parser, `fetchExperts`, `renditionLabel`) + `apps/web/app/chat/page.tsx`. `@expertos/shared` is now a web dependency. **`renditionLabel` is a web-local mirror of `buildAttribution`** — `@expertos/ai` is CommonJS so importing it would pull the whole package (incl. eval harnesses) into the client bundle; consolidate if the API ever returns the disclosure text. `NEXT_PUBLIC_API_URL` (default `http://localhost:3001`) configures the API base.
- **M2.4 voice-fidelity eval seam:** (1) **`evaluateVoice(VOICE_GOLDEN_SET, opts)`** from `@expertos/ai` is the entry point — same shape as `evaluateRetrieval`. With no `opts` it runs the **structural** layer only (pure, CI-safe): per case it builds the prompt and a **voice-off twin** of the same facts and asserts they share an identical user message + citation list — that is the machine-checkable "voice never touches facts" guarantee. Extend the structural checks in `eval/voice-metrics.ts` `scoreStructural`, not in the harness. (2) **Live layer is out-of-band** — pass `{ llm, judge }` (a `VoiceJudge` REQUIRES an `llm`, else it throws) to generate answers with the real model and score `voiceFidelity`/`factAdherence`. There is **no real judge implementation yet** — the in-CI tests inject stubs; M2.4 only builds the seam (deliberately, like the M1.3 real-embedder slice). When you wire a real judge, implement the `VoiceJudge` interface and feed `buildAnswerPrompt` output through it; do NOT bake judging into the harness. (3) **OD#2 bars live in code** as `VOICE_FIDELITY_BAR`/`FACT_ADHERENCE_BAR` (exported) — these are the *engineering* stance; the *product* (expert-signed) bar is still open. Change them in one place (`voice-metrics.ts`) when product calibrates. (4) The voice golden set's profiles are **illustrative fixtures**, not real signed profiles — real ones come from the M2.3 sign-off workflow + M8.4 example authoring; that's also the OD#6 (golden-set ownership) work.
- **M2.3 voice-profile authoring/sign-off seams:** (1) **The write seam is `VoiceProfileService`** (`apps/api/src/voice/voice-profile.service.ts`) — it uses Prisma **Client model methods** (`tx.voiceProfile.create/update/findUnique/findMany`, `tx.expert.findUnique`) inside `RlsService.run`, NOT raw SQL (the read stores `PgVoiceExampleStore`/`PgExpertStore` use raw SQL only because they touch the pgvector `embedding` column / `array_agg`; the profile row has no Unsupported column). Follow this split for future voice writes. (2) **Ownership is enforced in the service, not RLS** — `voice_profiles` RLS is `tenant_only` (no `user_id`), so the "expert signs off on their **own** voice" rule (NT.2) lives in `assertOwnership(user, expert.userId)`: non-admin ⇒ `Expert.userId === user.id`, admin ⇒ any. `list` adds `where.expert = { userId }` for non-admins. **Any new mutation MUST call `loadManageable`/`assertOwnership`** or an expert could touch a peer's profile within the tenant. (3) **State machine:** `draft→expert_review→published`, `expert_review→draft` (request-changes); only `approve` stamps `approvedBy`/`approvedAt`; only `draft` is editable; invalid transition = `ConflictException` (409). Extend `transition()` for archive/deprecate (M8). (4) **`ZodValidationPipe`** (`apps/api/src/common/zod-validation.pipe.ts`) is the reusable body/query validator — structurally typed (`safeParse`) so **apps/api still has no `zod` dependency**; pass a `@expertos/shared` schema. Reuse it for every future controller body/query; do NOT add zod to apps/api just to type a schema. (5) **HTTP routes now exist** (`/voice-profiles` CRUD+workflow) — the first admin/expert-portal API surface; the **portal UI is still M8.5** (apps/admin is a bare scaffold). M2.3 returns `VoiceProfileSummary` (carries `status`/`approvedBy`/`approvedAt`) ready for the review-queue UI. (6) **Still seed-only for a working demo:** there is no admin authoring of **voice examples** (with embeddings) yet — that's M8.4; a published profile with zero examples still renders (guidelines-only voice). The `PgVoiceProfileStore`-style writes are unit-tested with a mocked tx; same M11 Testcontainers caveat as the other stores.
- **M2.2 attribution + voice-list seams:** (1) **Disclosure is single-sourced** — `buildAttribution(voice?)` from `@expertos/ai` (`prompt/attribution.ts`) is the ONLY place the `"AI rendition of [Expert]"` phrase is constructed; `buildAnswerPrompt` embeds `attribution.disclosureText` verbatim. The M3 chat UI MUST render the visible "AI rendition" label from this same helper (don't hardcode it in the UI) or prompt-vs-label drift returns. Any future wording change happens in `attribution.ts` + `answer-prompt.test.ts` together. (2) **Voice picker** — call `VoiceService.listExperts(user, expertListQuerySchema-input)`; it returns `ExpertVoiceMeta[]` (`expertId`, `displayName`, `languages[]`, `hasActiveProfile:true`) for **active experts with a published profile** only, so a picker never offers a dead voice. `language` omitted = all; set it to disable voices that lack a profile in the asking language. M3 selects an `expertId` → feeds it to `VoiceService.retrieveVoice` → `buildAnswerPrompt({ voice, voiceExamples })`. (3) **No HTTP route yet** (deliberate — no consumer); add an `ExpertsController` (or fold into the chat controller) in M3 alongside the conversation UI, and that is when "which expert answered" gets persisted on the message row. (4) **`PgExpertStore` raw SQL is seam-tested only** (mocked tx) — its `array_agg(DISTINCT vp.language)`→`text[]` mapping and the conditional `$1::language`/`LIMIT $n` param shift need the M11 Testcontainers pass (same policy as `PgVectorStore`/`PgVoiceExampleStore`).
- **Voice + prompt seam (M2.1):** Two reusable pieces the M3 chat layer will wire together. (1) `buildAnswerPrompt({ query, facts, voice?, voiceExamples?, language? })` from `@expertos/ai` returns `{ messages, citations }` — feed it `RetrievedChunk[]` (facts) from `RetrievalService` and the voice layer from `VoiceService`. `citations[i]` resolves marker `[i+1]`; M4's citation builder MUST resolve against this array (never trust the model to emit a marker outside it). The builder is the **single enforcement point** for voice-on-top-of-facts — M2.4's voice-vs-facts separation tests should assert against its output (and against a live LLM in the eval harness), not re-implement the rule. (2) `VoiceService.retrieveVoice(user, voiceQuery)` keys on `expertId` + `language`; it returns `{ profile: null, examples: [] }` when the expert has no *published* profile in that language — the caller must fall back to a neutral voice (call `buildAnswerPrompt` without `voice`), facts still enforced. **Voice-example embeddings must be written with the same model as the query** (both from `createDefaultEmbeddingProvider`) — they aren't populated yet (no admin authoring/seed for voice examples); when seeding voice examples for M2.2 demo, embed via that factory or cosine match is meaningless. The `voice_examples.embedding` raw-SQL cosine query is **not** exercised against real pgvector here (same as `PgVectorStore`) — add it to the M11 Testcontainers integration pass.
- **Eval harness seam (M1.3):** `evaluateRetrieval(RETRIEVAL_GOLDEN_SET, opts)` from `@expertos/ai` is the RAG eval entry point. It is **deterministic and offline** by design — pass `{ embedder }` to swap the default `HashingEmbeddingProvider` (lexical only) for the real multilingual model in an out-of-band run to get the *semantic* VI/cross-lingual numbers (the offline run only guards tokenization/normalization/fusion). **Important nuance:** with the lexical hashing embedder a tiny corpus trivially returns every doc (all chunks fit in `topK`), so meaningful recall/MRR needs `topK` < corpus size — the seed golden set has 12 docs for this reason. The cross-lingual *miss* is intentional and asserted (`harness.test.ts`): the lexical model cannot bridge EN↔VI; only the real model can. Extend this for M2.4 (add voice-fidelity / voice-on≈voice-off assertions) and M4 (citation-resolvability) rather than starting a new harness.
- **NFC normalization is load-bearing for VI (M1.3):** all indexed text (chunk content) and all query text MUST be NFC-normalized or Vietnamese recall silently collapses (NFD combining marks shatter tokens). This is enforced at three boundaries — `@expertos/ai` `text.ts` `tokenize`/`normalizeText` (embedder + chunker + eval share one tokenizer), and `@expertos/shared` `retrievalQuerySchema` `.transform(normalizeText)` for query text (covers the Postgres keyword path, which does NOT normalize). When the real OpenAI embedder driver lands, keep NFC normalization in its tokenization path too. See LEARNINGS.
- **Retrieval seam (M1.2):** Call `RetrievalService.retrieve(user, query)` (`apps/api/src/retrieval`) — `query` is the shared `retrievalQuerySchema` (`text`, `topK` default 8, `filters` with `status` default `published`). It returns `RetrievedChunk[]` (`chunkId`, `documentVersionId`, `content`, fused `score`, optional `vectorScore`/`keywordScore`) — exactly what the M4 citation builder needs (`documentVersionId` provenance is already carried). The `VectorStore` interface is now `retrieve(RetrievalRequest)`; swap pgvector for Vertex/Qdrant behind it. Pure ranking lives in `@expertos/ai` `fuseHybrid` (RRF) — extend there, not in the driver. **The query embedder MUST stay the same model as ingestion** (both wired from `createDefaultEmbeddingProvider`); when the real OpenAI driver lands, change that one factory so both sides move together, else query/chunk vectors stop being comparable.
- **M1.2 needs integration validation in M11 (no DB in unit tests):** the two `$queryRawUnsafe` queries in `PgVectorStore` are not exercised against real pgvector here. Two things to verify with Testcontainers: (1) `scope = ANY($n::content_scope[])` binds a JS `string[]` correctly through Prisma `$queryRawUnsafe`; (2) cosine `<=>` ordering + `ts_rank` scores come back as JS numbers. Also consider adding a GIN index on `to_tsvector('simple', content||' '||summary)` for keyword perf (currently computed per-row) when corpus grows.
- **Design system (P0.6):** `packages/ui` exports the full primitive set (Button/Badge/Card/Chip/Cite/Field/Input/Select/Textarea/Table/Stat/Bar/Shell/Topbar/Content). Token guard is live in `pnpm lint`: stylelint bans raw hex + `px` outside ds.css (ds.css is the exempt token source-of-truth) via `lint:css`; eslint `no-restricted-syntax` bans hex-color string literals in TS/TSX (root + both app configs). `Cite` returns null until `resolved` (citation-integrity). ds.css + Google Fonts already imported at both Next app roots.
- **DB engine quirk (this sandbox only):** Prisma's default **library** query engine SIGILLs (exit 132) at runtime on this aarch64/linuxkit box. The schema/migration engine is fine (`prisma migrate deploy` works). To run anything through Prisma **Client** locally (seed, scripts), prefix with `PRISMA_CLIENT_ENGINE_TYPE=binary`. The committed schema intentionally keeps the **default library engine** (correct for prod amd64 Cloud Run) — do NOT pin `engineType=binary` in schema.prisma. See LEARNINGS #1.
- **Running migrations locally:** `prisma migrate dev` / `--create-only` are interactive and fail in this non-interactive shell. Generate SQL with `prisma migrate diff --from-empty --to-schema-datamodel ... --script` into a `prisma/migrations/<ts>_<name>/migration.sql` dir (+ `migration_lock.toml`), then `prisma migrate deploy`.
- **Auth flow (P0.3):** `FirebaseAuthGuard` resolves the user via `AuthService.resolveUser`, which find-or-creates the `users` row inside an **admin/system RLS context** (`isAdmin:true`, GLOBAL tenant) — necessary because `users` is RLS-protected and the tenant isn't known until after lookup, and `firebase_uid` is globally unique. After resolution, real request DB work should run inside `RlsService.run(authUser, tx => …)` (scopes by the user's tenant/user; `is_admin` GUC = true for the `admin` role so the admin/expert portals get cross-tenant visibility). No data endpoints consume `RlsService` yet (only `/me`, which echoes the already-resolved user) — wire it into the first real CRUD route.
- **Prod DB role:** `DATABASE_URL` must point to the non-superuser `app_user` (LOGIN + password provisioned out of band via Secret Manager) so RLS enforces. The committed `.env.example` uses the owner role for local dev convenience — acceptable locally, NOT for prod.
- **Firebase init is lazy on the web** (LEARNINGS #3): module-level `getAuth()` breaks `next build`. Use `getFirebaseAuth()` from effects/handlers only; `isFirebaseConfigured` gates the auth subscription. Set `NEXT_PUBLIC_FIREBASE_*` (web) + `FIREBASE_PROJECT_ID/CLIENT_EMAIL/PRIVATE_KEY` (API) to actually sign in.
- pgvector embedding columns are `Unsupported("vector(1536)")` — Prisma Client can't read/write them; the M1 VectorStore driver must use `$queryRaw`. Dimension 1536 = OpenAI text-embedding-3-small; changing the model = new migration.
- `prisma generate` runs in db's `postinstall`, `build`, and `typecheck` so the gitignored `generated/client` always exists for downstream typecheck/build/knip.
- Stylelint is configured but NOT wired into lint; ds.css fails it. That cleanup is P0.6.
- `@nestjs/testing`/`@nestjs/schematics` removed from apps/api; re-add `@nestjs/testing` (+ knip ignore) when the first NestJS module-level (DI container) test is written. Current auth tests construct services/guards directly (no Nest test harness needed). P0.5 validated the full DI graph (global module + middleware + APP_FILTER) by bootstrapping the built `dist/app.module` (`NestFactory.create` → `init` → `close`) with dummy creds — a throwaway smoke, not committed.
- **Observability (P0.5):** Use the injectable `StructuredLogger` for app logging (`logger.info("msg", { field })`); it auto-stamps `requestId`/trace and redacts PII — never `console.*`, never put raw email/token/secret in fields (redaction is a backstop, not a license). Record AI/cost usage with `UsageLogService.record(user, { featureKey, model, promptTokens, completionTokens, costMicros, conversationId })` — `costMicros` = millionths of a USD cent (integer-safe). The observability types (`LogSink`, `UsageLogEntry`, `RequestContext`, `LogSeverity`) are intentionally **un-exported** (knip-clean); re-add `export` when a consumer in another file needs to name them. Sentry only ships when `SENTRY_DSN` is set (+ optional `SENTRY_ENVIRONMENT`/`SENTRY_RELEASE`/`SENTRY_TRACES_SAMPLE_RATE`); `GOOGLE_CLOUD_PROJECT` makes the logged trace a full `projects/<id>/traces/<tid>` resource path for Cloud Trace linking.
- **Jest memory (this sandbox):** `jest.base.cjs` now sets `maxWorkers: 2` + `workerIdleMemoryLimit: "512MB"`. Without it, the default worker pool (cores−1) OOM-SIGKILLs a worker on this ~4 GB box once enough heavy suites (firebase-admin) run concurrently — symptom: `A jest worker process was terminated ... signal=SIGKILL`. See LEARNINGS.
- **Deploy (P0.4):** Dockerfiles/Terraform are authored but NOT runnable in this sandbox (no `terraform`/`gcloud` binaries; no network for base-image pulls). Validated what's runnable: all gates pass and Next `standalone` output emits `apps/<app>/.next/standalone/apps/<app>/server.js` exactly as the Dockerfile COPY/CMD expect. **Gotcha:** turning on `output: "standalone"` over a pre-standalone `.next` cache makes `next build` fail with `SyntaxError: Unexpected end of JSON input` (stale PackFileCache) — `rm -rf apps/*/.next apps/*/.turbo` then rebuild. `apps/api/src/main.ts` now binds `0.0.0.0:$PORT` (Cloud Run injects PORT=8080). Web/admin have no `public/` dir, so the Dockerfiles intentionally don't COPY one (add it when one exists). NEXT_PUBLIC_* are build-time → pass as `--build-arg` when wiring Firebase web config into deploy. Cloud SQL `app_user` + secret values are provisioned out of band (see `infra/README.md`).
