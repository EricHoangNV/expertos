# ExpertOS — Build Notes

> Companion to `PRD-TRACKING.md`. Implementation detail (files, decisions, gotchas) for completed tasks,
> keyed by task id. Append here when you finish a task; keep the manifest line itself terse.

### M8.1 — Admin: upload + versioned publish with expert-review gate (`Draft → AI Processing → Expert Review → Published`) — status as semantic `.badge` tones (§"Design System"); `.shell` shared with expert portal

**API DONE** (`KnowledgeService` publish state machine + chunk-visibility/supersede integration + `/knowledge` routes); **admin UI DONE** (`apps/admin` `.shell` portal: badge-toned review queue + version-history detail + Submit/Approve/Request-changes/Archive on the new API)

### M8.2 — Admin: conversation-to-knowledge pipeline (Mark Valuable → Draft → Expert Review → Publish)

**API DONE** (`KnowledgeDraftService` draft lifecycle + publish-via-ingestion gate + `/knowledge-drafts` routes); **admin UI DONE** (`apps/admin` draft review queue + detail/edit + Submit/Request-changes/Reject/Publish; "Mark valuable" deferred to the future conversation viewer)

### M8.3 — Admin: plan-entitlement matrix editor + basic revenue reports (MRR, by plan/period) + failed/low-confidence query inspector

**revenue reports DONE** (`apps/api/src/revenue/` `RevenueService` + `GET /admin/revenue/report` + `apps/admin/app/revenue` dashboard: MRR, per-plan, trailing monthly series, `cost_micros` margin); **plan-entitlement matrix editor DONE** (`apps/api/src/entitlements/entitlement-matrix.service.ts` `EntitlementMatrixService` + `GET/PATCH /admin/entitlements` + `apps/admin/app/entitlements` matrix table editor: per-cell enable/limit/softLimit/window with server-side type-coherence validation); **recommendation-rules editor DONE** (`apps/api/src/consultation/recommendation-rules.service.ts` `RecommendationRulesService` + `GET/PATCH /admin/recommendation-rules` + `apps/admin/app/recommendation-rules` per-rule editor: enable/keywords/threshold/priority/consultation-type with server-side trigger-coherence validation); **failed/low-confidence query inspector DONE** (`apps/api/src/feedback-inspector/failed-query.service.ts` `FailedQueryService` + `GET /admin/failed-queries` + `apps/admin/app/failed-queries` flagged-answers feed: cross-tenant admin-RLS read over the 👎 `answer_feedback` rows, raw-SQL `LATERAL` join to the preceding question + insufficient-knowledge flag)

### M8.4 — Admin: manage users / subscriptions / experts / voice profiles; admin audit logs; user-data deletion

**DONE.** users/subscriptions/fair-use + admin audit logs + user-data deletion (`apps/api/src/admin/` `AdminAuditService` immutable audit backbone [`record` in-tx + `GET /admin/audit-logs`] + `AdminUserService` [`GET /admin/users`, `GET /admin/users/:id`, `PATCH .../role`, fair-use raise/resolve, `POST .../deletion-request`, `DELETE /admin/users/:id` GDPR cascade] all `@Roles("admin")` cross-tenant admin-RLS; subscriptions read-only — provider stays billing source of truth; `apps/admin/app/users` list+detail + `apps/admin/app/audit` feed); **expert CRUD** (`AdminExpertService` on the `AdminUserService` template — `GET/POST /admin/experts`, `GET/PATCH /admin/experts/:id`, `PATCH .../active`; audit-in-tx; slug frozen; operator link/unlink; P2002→409 + `apps/admin/app/experts` list+detail) + **voice-profile admin UI** over the existing M2.3 `/voice-profiles` routes (`apps/admin/app/voice-profiles` sign-off queue + inline draft edit + create)

### M8.5 — Expert portal (first-class `expert` role): approve voice + knowledge, review AI answers, view consultation conversions

**DONE.** Voice + knowledge approval reuse the existing expert-scoped `/voice-profiles` (M2.3) + `/knowledge` (M8.1) routes; new `apps/api/src/expert/` `ExpertPortalService` adds `GET /expert/conversions` (funnel by trigger/response/status + booked revenue + recent feed) + `GET /expert/answers` (AI-answer review feed) — **voice-scoped** via an elevated-but-bounded RLS read (`is_admin` context + explicit `tenant_id` + `conversation.expert_id` predicates; resolve-expert-first, short-circuit-empty-when-none); the admin portal is now **role-aware** (`/me` → `AdminFrame` Expert/Admin nav groups) + `apps/admin/app/conversions` + `apps/admin/app/answers` pages

### M9.0 — **GATE:** Open Decision #5 (Mode B legal/brand ruling) resolved

**RESOLVED:** Mode B approved as default; AI-reviewed/edited answers must show a visual indicator (asterisk or info icon with tooltip: "This response includes AI-reviewed/edited content"). ToS already covers this; no Mode-A-only fallback needed.

### M9.1 — Admin trigger config (off / user-prompted / auto-silent) + confidence threshold + SLA + volume cap

**DONE** (`apps/api/src/concierge/` `ConciergeConfigService` over the `review_configs` global singleton + `GET/PATCH /admin/concierge-config` admin-RLS editor + audit-in-tx + `apps/admin/app/concierge` page). **OD#5 RESOLVED:** Mode B (`auto_silent`) is now the default; `CONCIERGE_ALLOW_SILENT` should default to `true`. The admin UI should enable Mode B by default. AI-reviewed/edited answers must display a visual indicator (asterisk or info icon with tooltip).

### M9.2 — Concierge review queue in Expert portal; reviewer verdict (Good/Bad/Great) + edit

**DONE.** Enqueue: `apps/api/src/concierge/concierge-queue.service.ts` `ConciergeQueueService.enqueueIfTriggered` (consumed by `ChatService` after a turn — Mode B `auto_silent` + low-confidence proxy → silent `HumanReviewRequest`; elevated-tenant-bounded for the tenant-wide daily volume cap; idempotent; non-fatal). Reviewer: `concierge-review.service.ts` `ConciergeReviewService` (`list`/`get`/`respond`, `@Roles("expert")` `/concierge-reviews` routes) — voice-scoped via the M8.5 elevated-but-bounded RLS pattern (resolve-expert-first, `tenant_id`+`message.conversation.expertId` predicates); `respond` writes a `ReviewResponse` (verdict + `edited`-derived edit) and moves the request to `answered`. Admin UI: `apps/admin/app/concierge-reviews` (queue + open-to-review verdict/edit form, Expert nav group). OD#5 gate flipped: `CONCIERGE_ALLOW_SILENT` now defaults allowed. Escalate-to-consultation + the global flywheel are M9.4; async delivery (push-back + email) is M9.3.

### M9.3 — Async delivery (visible update vs silent) + transactional email notification

**DONE.** New email-provider seam `apps/api/src/email/` (`EmailProvider`/`EmailService` choke point + `OfflineEmailProvider` default + dependency-free `HttpEmailProvider` `fetch` driver behind `EMAIL_PROVIDER`, swapped when `EMAIL_API_URL`+`EMAIL_API_KEY`+`EMAIL_FROM` set — the Stripe/TidyCal seam pattern). New `ConciergeDeliveryService` (invoked by `ConciergeReviewService.respond` after the verdict commits, non-fatal, elevated-tenant-bounded): when the reviewer **edited** the answer → appends the refined answer as a new assistant message marked `refined_from_message_id` (migration `20260601080000`; the OD#5 visual-indicator hook surfaced on `ChatMessageDto` + a web history badge), bumps `conversation.updatedAt`, stamps `review_responses.delivered_to_user=true`, and sends a transactional email (outside the tx, best-effort). A verdict-only/unchanged response stays **silent** (the M9.4 context injection feeds it forward). "visible update vs silent" = edited→visible, unedited→silent. `email.service.ts`+`concierge-delivery.service.ts` 100% all metrics; +18 `apps/api` tests. **M9 COMPLETE.**

### M9.4 — Reviewer-feedback flywheel: conversation-context injection (immediate) + `voice_examples`/`knowledge_drafts`/chunk-flagging (global); escalate-to-consultation

**DONE.** `apps/api/src/concierge/concierge-flywheel.service.ts` `ConciergeFlywheelService.applyReviewOutcome` (invoked by `ConciergeReviewService.respond` after the verdict commits, non-fatal, elevated-tenant-bounded): great/edited → `knowledge_drafts` row (→ Expert Review → publish/re-embed) **+** an embedded `voice_examples` row on the expert's published profile (same embedder as voice retrieval); bad → flags the answer's source chunks (`chunks.flag_count`/`last_flagged_at`, migration `20260601070000`). Escalate-to-consultation: `ConciergeReviewService.escalate` (`POST /concierge-reviews/:id/escalate`) opens a `recommended` consultation for the asking user + moves the request to `escalated`. Immediate context injection: `ConversationService.loadHistory` substitutes the latest reviewer-edited revision into replayed prompt history (display untouched — visible delivery is M9.3). Admin UI: `apps/admin/app/concierge-reviews` "Escalate to consultation" action.

### M10.1 — Usage & cost analytics

**DONE** (`apps/api/src/analytics/` `AnalyticsService` admin cross-tenant RLS read over `usage_logs` → `GET /admin/analytics/usage`: window totals [events/tokens/`cost_micros`/distinct active users] + per-feature + per-model rollups [Prisma `groupBy`] + trailing **daily** series [raw `date_trunc('day')` + `count(DISTINCT user_id)`, BigInt-coerced]; `apps/admin/app/analytics` dashboard. OD#1-independent instrumentation — the kill-line M10.4 is the OD#1-gated piece.)

### M10.2 — Consultation funnel + attribution (question→conversation→recommendation→booking→revenue)

**DONE** (`apps/api/src/analytics/` `AnalyticsService.funnel` → `GET /admin/analytics/funnel`: admin cross-tenant RLS read tracing conversations → recommendations (`groupBy` trigger/response) → funnel-attributed consultations (`groupBy` status, scoped to `recommendations:{some:{}}`) → booked revenue (`_sum.amountCents`); the `ExpertPortalService.conversions` shape but platform-wide; `apps/admin/app/funnel` dashboard. OD#1-independent instrumentation.)

### M10.3 — Concierge volume/SLA/verdict metrics + knowledge-quality signals

**DONE** (`apps/api/src/analytics/` `AnalyticsService.concierge` → `GET /admin/analytics/concierge`: admin cross-tenant RLS read over `human_review_requests`/`review_responses` (M9) + the M9.4 chunk-flagging signal — **volume** (one `groupBy` folded into status/trigger-mode/visibility + total), **SLA** (raw FILTERed aggregate: met/breached/open-overdue vs `sla_due_at` + mean response minutes), **verdicts** (`groupBy` verdict + windowed edited/delivered counts), **knowledge-quality** (cumulative flagged-chunk count + total flags + windowed `recentlyFlagged` + most-flagged top-10 with excerpts); `apps/admin/app/concierge-analytics` dashboard. OD#1-independent instrumentation.)

### M10.4 — Instrument all validation metrics (activation, engagement, willingness-to-pay, funnel conversion, revenue/user) — thresholds set post-launch with real data (OD#1 resolved)

**DONE** (`apps/api/src/analytics/` `AnalyticsService.validation` → `GET /admin/analytics/validation`: admin cross-tenant RLS scorecard folding **activation** (new-user cohort reaching ≥1 cited answer within 24h of signup), **engagement** (active users + total/median questions + 1–7d return rate), **willingness-to-pay** (cumulative paying/trialing vs all users, free→paid rate), **funnel** (recommendation→booking rate + booked revenue per buyer); raw numbers + headline rates only — no thresholds per OD#1; `apps/admin/app/validation` scorecard. Parameterized raw SQL only where Prisma lacks an expression (`count() FILTER`, `percentile_cont`, `count(DISTINCT)`).)

### M11.1 — Full E2E path matrix (Playwright) — see §"Testing Strategy"

**EXECUTED GREEN against a live stack: 15 passed / 0 failed / 4 skipped** (3 documented `test.fixme` + the voice test which skips cleanly when no expert voice is seeded). Stack stood up in-sandbox: `infra/local-test-db.sh` DB + `firebase-tools` Auth emulator + the 3 built services; chromium needs system-deps **and base fonts** (LEARNINGS #11). New `e2e/global-setup.ts` makes it repeatable — mirrors the 4 identities via `GET /me`, promotes admin/expert roles + puts the member on Plus directly in the DB (reads `DATABASE_URL`). Sign-in is now **programmatic** (`window.__e2eSignIn` email/password against the emulator, exposed emulator-gated by each app's `firebase.ts`) instead of the Google popup, which needs the firewall-blocked `apis.google.com` (LEARNINGS #10 sibling). Real bugs found+fixed along the way: API had **no CORS** → browser cross-origin calls failed the preflight (now `enableCors` allowlist, LEARNINGS #10); admin `<Field label="Status">` had no `htmlFor` → unlabeled control (LEARNINGS #12); plus several harness selector fixes (loose `getByText`/`.or()` strict-mode violations → `exact`/`.first()`; the `ask()` done-signal; row "Manage" link). (opt-in `e2e/` workspace, excluded from default `pnpm test` like the live-DB integration suite: `playwright.config.ts` boots/attaches api+web+admin; emulator-based auth fixtures [`signIn`/`signInAdmin`; `getEmulatorIdToken` REST for API seeding]; 18 discovered tests across 7 specs grounded in the real app DOM — `web-chat` (ask→answer→save, feedback, NT.4 high-stakes disclaimer, insufficient-knowledge), `web-voice-and-consultation` (M2.2 voice + M7.2 booking), `web-history` (M3.3 search + M3.2 saved + rename), `web-upload` (M5.3 spreadsheet→searchable chunks + type rejection), `admin-portal` (role-aware nav + M8.1 review-gate queue), `account-billing` (M6.3 plan+usage meter **+ M6.2 self-serve upgrade CTA**), `data-deletion` (M8.4 deletion request); enabler: env-guarded `connectAuthEmulator` wiring added to web+admin `firebase.ts` [prod no-op] **+ the matching API-side fix — `createFirebaseApp` is now emulator-aware (inits with just a `projectId`, no service-account cert, when `FIREBASE_AUTH_EMULATOR_HOST` is set), so the API actually boots under the emulator instead of throwing on start; Playwright api `webServer` passes `FIREBASE_PROJECT_ID`** (LEARNINGS #9); `e2e/README.md` runner docs). **Consumer self-serve checkout now built** (`GET /me/plans` + `apps/web/app/account` Upgrade/Manage-billing → `POST /billing/checkout`/`/portal`), closing one fixme leg. **The expert-voice leg is now seeded** — `e2e/global-setup.ts` seeds an active expert + published `en` voice profile (idempotent, linked to e2e-expert@), so the M2.2 "AI rendition" test runs instead of skipping (validated against a live pgvector DB: picker + chat-resolution SQL both return the seed; expected E2E delta 15→16 pass, 4→3 skip). **Remaining: only the 3 documented `test.fixme` legs, which await an external/seed surface — full publish→retrieval round-trip + irreversible deletion cascade (seed prerequisite) and completing the Stripe-hosted checkout page (external surface).**

### M11.2 — Security tests (authz/RLS negative, prompt-injection regression, rate-limit) + `/cso` audit

**prompt-injection regression DONE** (`packages/ai/src/prompt/answer-prompt.ts` UNTRUSTED-INPUT instruction-hierarchy rule + `neutralizeInjection` forged-`[n]`-marker defang on untrusted SOURCES/QUESTION; `prompt-injection.test.ts` 6 deterministic fixtures); **rate-limit DONE** (`apps/api/src/rate-limit/` per-IP fixed-window limiter: `RateLimitService` over the bounded `LruCache` [Redis swap point] + `RateLimitGuard` as the **first** global `APP_GUARD` [throttles before token-verify] + `@SkipRateLimit()` on health + env-tunable `RATE_LIMIT_*`; sets `X-RateLimit-*`/`Retry-After`, `429 {reason:"rate_limited"}`; `rate-limit.service.test.ts`/`rate-limit.guard.test.ts`); **authz/RLS negative tests DONE** (`packages/db/src/rls.integration.test.ts` — 15 live-DB negative tests run as the non-superuser `app_user` role against a real Postgres, validating all three policy families [tenant_isolation / tenant_user_isolation / tenant_write+global_read] for cross-tenant + cross-user read isolation, WITH-CHECK write rejection, admin-bypass, and fail-closed-when-no-context; opt-in via `RLS_TEST_DATABASE_URL`, excluded from the default `pnpm test`, run with `pnpm --filter @expertos/db test:integration`; **now executed green via `infra/local-test-db.sh` / `pnpm test:integration`** — a Docker pgvector container + migrate/seed + `app_user`-LOGIN harness that runs these 15 RLS tests **plus** the 35 `apps/api` live-DB suites = 50 live tests passing, no GCP dependency); **`/cso` audit DONE** (`/security-review` over the full branch diff vs `main`, FEEDBACKS Security Cycle 1 — **PASS, no HIGH/MEDIUM findings**: raw-SQL paths parameterized, elevated RLS reads re-bounded by tenant_id+expert_id, every new admin/expert route guarded, webhook HMAC verified over raw body w/ constant-time compare, no XSS). **M11.2 COMPLETE.**

### M11.3 — Performance / caching tuning + load smoke test

**DONE.** Caching tuning is now *observable*: cumulative per-layer hit/miss/eviction/expiration instrumentation on the in-process `LruCache` (`stats()`) folded into `ResponseCacheService.stats()` across all three M6.4 layers (retrieval / answer-memory / persistent-semantic, with a combined `answerOverall` served-vs-lookups rate), surfaced at admin-guarded `GET /admin/analytics/cache` (`CacheAnalyticsDto`) — the signal that lets `cache.config.ts` `maxEntries`/`ttlMs` be sized from real eviction/expiry counts. Load smoke: dependency-free Node driver `load/smoke.mjs` (global `fetch` + `perf_hooks`, **not** a pnpm workspace — like `infra/`, out of the default gates) — fixed-concurrency timed phases over `health` / `me/entitlements` / cache-warming `chat`, p95 + error-rate gated (exit 1 on breach), optionally prints the cache hit rate after a run; `load/README.md` runner docs. **Remaining: execution against a live stack (opt-in, like `e2e/`) — not runnable in CI/sandbox.**

### M11.5 — Design-system conformance audit (`/design-review`): token usage (no hardcoded colors/px), citation render-after-resolve, upload-vs-knowledge color distinction, badge tones, hit-target/size minimums (§"Design System")

**DONE.** Conformance rules mechanized as unit tests (`packages/ui/src/primitives.test.ts`, +26: Cite render-after-resolve + upload/knowledge color, Badge tones, Button one-crimson-primary, Bar/UsageMeter quota meter; all 12 ds.css components 100% coverage, `jest.config.cjs` collectCoverageFrom flipped to `*.{ts,tsx}`); rendered emoji removed (anti-slop "no emoji": chat 👍/👎→Yes/No, ☆/★ save toggle, admin answer badges, failed-queries copy); token-usage audit clean (no hardcoded hex/px in either app — lint guards effective). **Audit note (design sign-off needed, not changed unilaterally):** ds.css interactive heights are below the 44px hit-target line (`.btn` ~39px, `.btn-icon` 36px, `.chip` ~33px) — flagged for the design owner since ds.css is the visual source-of-truth.

### M13.1 — i18n framework: add `next-intl` or a lightweight `t()` helper with EN/VI JSON dictionaries; wire the language toggle (M12.3.3) to switch the active locale and persist in localStorage + user profile

**DONE** (lightweight `t()`: pure i18n core in `@expertos/ui` — `Locale`/`translate`/`createTranslator`, 100% tested; web `LocaleProvider`/`useLocale`/`useT` in `apps/web/src/lib/i18n/` with EN/VI `dictionaries.ts`; persistence to localStorage [`expertos:locale`] **+** user profile via new `ProfileService` + `PATCH /me/locale` [RLS self-write] + shared `localeUpdateSchema`/`UserProfileDto`; resolution SSR-default → localStorage → profile-on-sign-in, `<html lang>` synced. The chat `language` state [M12.3.3] is lifted to the global locale so the toggle now drives BOTH UI language [`useT`] and answer language. Starter `chat` namespace wired [empty state + input placeholder]; full string extraction is M13.2–M13.5.)

### M13.2 — Translate web app: all static UI strings in `/chat`, `/history`, `/account` pages — sidebar labels, buttons, placeholders, empty states, error messages, tooltips, usage meter, plan names

**DONE** (EN/VI dictionaries grown to 3 page-scoped namespaces [`chat` 52 / `history` 42 / `account` 16 keys, lockstep-verified]; `useT` wired across all three pages + their page-local sub-components; plain non-hook helpers take a `Translator` param. Scope: system-generated content [`HIGH_STAKES_DISCLAIMER`, API CTA copy] = M13.4; locale-aware date/number/currency formatting [`formatPrice`/`when()`] = M13.5.)

### M13.3 — Translate admin portal: all static UI strings in admin pages — navigation, table headers, form labels, status badges, action buttons

**DONE.** Admin gets its own i18n layer mirroring the web app (`apps/admin/src/lib/i18n/`: `LocaleProvider`/`useLocale`/`useT` over the pure `@expertos/ui` core, `profile-client` for `GET /me`+`PATCH /me/locale`, localStorage key `expertos:admin-locale`), wired in `app/layout.tsx`. EN/VI language toggle (`.seg`) added to the `AdminFrame` topbar. Dictionaries are split **per-page namespace** (`dictionaries/<route>.ts`, each exporting `en`/`vi`, assembled by `dictionaries.ts`) — 24 namespaces, ~900 keys, EN/VI lockstep-verified. All 25 pages + the `AdminFrame` chrome (nav/groups/breadcrumb/role badge/footer/login/access-denied) wired through `useT`. Status badges: the generic `statusLabel` enum-humanizer became a locale-aware `useStatusLabel()` hook backed by a shared `common.status.*` map (43 lifecycle tokens EN+VI) with a safe underscore→space fallback for unknown tokens. Gates: admin `tsc` + `next lint` + root `lint:css` + `knip` all clean. (Locale-aware date/number/currency formatting reuses the M13.5 `@expertos/ui` formatters where pages add new formatting; existing `toLocaleString` call sites left as-is.)

### M13.4 — Translate system-generated content: high-stakes disclaimer (NT.4), concierge disclosure tooltip (OD#5), insufficient-knowledge message, consultation CTA copy

**DONE** (high-stakes disclaimer is now locale-aware: `HIGH_STAKES_DISCLAIMERS: Record<LanguageValue,string>` single-sourced in `@expertos/shared` [EN+VI together for one-place NT.4 legal review; `HIGH_STAKES_DISCLAIMER` kept as the canonical EN alias], rendered by the chat `HighStakesNotice` + history detail via the active `Locale`. Consultation CTA copy: `ChatConsultationCard` gained `maybeLaterLabel`/`askAnotherLabel` props [EN defaults preserved]; the web `ConsultationPrompt` now passes localized `heading`/`maybeLaterLabel`/`askAnotherLabel` via 3 new `chat` dictionary keys [`consultHeading`/`consultMaybeLater`/`consultAskAnother`, EN+VI, lockstep-verified]. Insufficient-knowledge message + concierge disclosure tooltip were already translated in M13.2 [`insufficientLabel`/`insufficientBody`, `reviewedTooltip`/`reviewedRefined`]. The funnel rule `reason` stays dynamic admin DB text [not statically translatable]. +5 tests [shared 3, ui 1 + the consult test split; web has no jest suite].)

### M13.5 — RTL-safe + locale-aware formatting: dates, numbers, currency display respect the active locale (Vietnamese date format, VND currency where applicable)

**DONE** (reusable pure formatters in the `@expertos/ui` i18n core — `localeTag`/`formatNumber`/`formatCurrency`/`formatDateTime` over `Intl.*`, NaN/invalid-date-guarded per §3.5, fully tested [+7 ui]; M13.3 admin will reuse them. Wired the two web call sites that still used the ambient system locale: `apps/web/app/account` `formatPrice` → `formatCurrency(locale, …)` + localized `/mo`·`/yr` suffix [new `perMonth`/`perYear` keys, EN+VI lockstep]; `apps/web/app/history` `when()` → `formatDateTime(locale, …)` at all 3 call sites. So VI shows day-first dates [`2 thg 6, 2026`] + comma-decimal currency [`4,99 US$`]; a VND-coded price renders `499.000 ₫`. RTL is N/A — both supported locales [EN/VI] are LTR. `relativeTime`'s English words are M13.2 copy scope, left untouched.)

### M12.1.1 — Create `.chat-layout` CSS: three-pane grid (sidebar 248px + chat flex:1 + sources-rail 320px) using ds.css tokens; responsive breakpoints (collapse sources-rail < 1280px, collapse sidebar < 900px)

**DONE** (`packages/ui/src/ds.css` `.chat-layout` named-grid-areas container [`sidebar`/`main`/`rail`] dropping the rail < 1280px and the sidebar < 900px via range-notation breakpoints; child hooks `.chat-sidebar`/`.chat-main`/`.chat-rail`. Also fixed the pre-existing red lint gate from the M12.8 login commit — login.css raw-px → rem/tokens, Google-brand-hex SVGs behind scoped eslint-disables; see LEARNINGS #13.)

### M12.1.2 — Extract `ChatLayout` component wrapping sidebar + main + rail; integrate into `/chat` route replacing the current single-card layout

**DONE** (`packages/ui/src/ChatLayout.tsx` over the M12.1.1 `.chat-layout` grid: optional `sidebar`/`rail` props → `.chat-sidebar`/`.chat-rail`, `children` → scrollable `.chat-main`; `Shell`-style render-region-only-when-given so M12.1.3 directions drop a pane via `undefined`; integrated into both renders of `apps/web/app/chat/page.tsx`; +3 ui tests, 100% coverage)

### M12.1.3 — Layout direction switcher state (classic / studio / focus) — studio = default three-pane; classic = two-pane + sources drawer overlay; focus = no sidebar + sources drawer

**DONE** (`packages/ui/src/layout.ts` `LayoutDirection` + `layoutPanes`/`isLayoutDirection`/`LAYOUT_DIRECTION_INFO`; `ChatLayout` `direction` prop drops panes per direction [classic = sidebar only, focus = chat only] + ds.css `.chat-layout-{classic,focus}` grid reflow; chat page owns the state — Tweaks `.seg` control + localStorage persistence is M12.7.2; +6 ui tests, 100% coverage)

### M12.2.1 — Sidebar component: ExpertOS `.logo` wordmark (white on dark), close/collapse button, "+ New conversation" `.btn-primary` full-width

**DONE** (`packages/ui/src/ChatSidebar.tsx` — dark `.side` rail filling the `.chat-layout` sidebar pane: white `.expert`/crimson `.os` wordmark, optional collapse button [opt-in until M12.7/M12.9.1 give it a re-open affordance], full-width `.chat-side-new` `.btn-primary`; body/footer slots for the search+list [M12.2.2–3] and usage meter [M12.2.4]; ds.css `.chat-side*` additions; wired into both `/chat` renders — "+ New conversation" clears the active chat; +4 ui tests, 100% coverage)

### M12.2.2 — Conversation search input (`.input` on dark bg, "Search all messages..." placeholder); wired to existing full-text search API (M3.3)

**DONE** (`packages/ui/src/ChatSearch.tsx` — controlled dark-rail search input [leading magnifier + "Search all messages…" placeholder, `.input` restyled for `--ink-900`] + a results list that appears once the user types; presentational only. Wired in `apps/web/app/chat/page.tsx`: debounced query [≥2 chars, latest-wins] → `searchConversations` [M3.3], guillemet-snippet hits render under the field, selecting one calls the new `openConversation` [fetch `GET /conversations/:id` → replay transcript into the message list, restore expert voice]; +5 ui tests, 100% coverage)

### M12.2.3 — Conversation list: "RECENT" `.navgroup` label; conversation items with expert-colored avatar circle (initials), truncated title, relative time, unread dot; `.navitem.active` for current; sorted most-recent-first; wired to history API (M3.2)

**DONE** (`packages/ui/src/ChatConversationList.tsx` — "RECENT" `.navgroup` over `.navitem` `.chat-convo` rows: expert-colored `.avatar` [`avatarInitials`/`avatarTone` + ds.css `.avatar.tone-*`], truncated title, `relativeTime`, optional unread dot, `.navitem.active`; presentational. Wired in `apps/web/app/chat/page.tsx`: `listConversations` [M3.2] on mount + post-turn refresh, `useMemo` resolves expert name from loaded voices + sorts most-recent-first, reuses `openConversation`, hidden while searching; +9 ui tests, 100% coverage)

### M12.2.4 — Usage meter at sidebar bottom: "questions this month" label, "N / M" count, `.bar` progress (crimson fill), plan badge (`.label`), "Upgrade" link (crimson); wired to `/me/entitlements` API (M6.1)

**DONE** (`packages/ui/src/ChatUsageMeter.tsx` — dark-rail "questions this month" + "N / M" count over a crimson `.bar` [amber `.bar.warn` near the cap], plan `.label` badge + crimson "Upgrade →" link; measures against the hard `limit` else the fair-use `softLimit` else "Unlimited", NaN-guarded, upgrade link omitted with no href; ds.css `.sidebar-usage` block; wired into `/chat` `ChatSidebar` footer from `/me/entitlements` `ask_question` quota, refreshed after each turn; +5 ui tests, 100% coverage)

### M12.3.1 — Topbar component: conversation title (auto-titled from M3.2), editable on click

**DONE** (`packages/ui/src/ChatTopbar.tsx` `ChatTopbar`: `.topbar .chat-topbar` strip with click-to-rename title [heading→`.input`, commit on Enter/blur, cancel on Escape] + right-aligned `.chat-topbar-aside` slot for M12.3.2–3; ds.css `.chat-topbar*`; wired into `/chat` with title state + `renameConversation` PATCH (M3.2) + post-turn auto-title sync; +6 ui tests, 100% coverage)

### M12.3.2 — Voice picker: "VOICE" `.label` + `.chip` / `.chip.active` pills for each expert voice; wired to existing experts API; selecting a chip switches the active expert for the conversation

**DONE** (`packages/ui/src/ChatVoicePicker.tsx` `ChatVoicePicker`: `.chat-voice-picker` "Voice" `.label` over `.chip`/`.chip.active` pills — a leading "Neutral" chip + one expert-colored-`.avatar` chip per voice; ds.css `.chat-voice-picker`/`.chat-voice-avatar`; presentational. Wired into the `.chat-topbar-aside` slot of `ChatTopbar` in `/chat`, replacing the old expert `<Select>` in chat-content; `onSelect("")`=neutral, reuses `expertId` on the next turn; hidden when no expert voices exist; +6 ui tests, 100% coverage)

### M12.3.3 — User identity display: avatar + name + language badge (EN/VI) right-aligned

**DONE** (`packages/ui/src/ChatUserIdentity.tsx` — right-aligned `.chat-user-identity`: expert-toned `.avatar` (initials) + user name + EN/VI `.badge.badge-ink` language toggle [interactive `<button>` when `onLanguageToggle` given, else static span; email-local-part then "You" fallback]; ds.css `.chat-user-identity`; wired into the `.chat-topbar-aside` slot after the voice picker, name/email from Firebase auth, the toggle drives a new chat `language` state replacing the hardcoded `"en"` on streamChat, disabled mid-stream; +6 ui tests, 100% coverage)

### M12.4.1 — User message bubble: `.msg-user` — dark bg (`--ink-900`), white text, `--r-lg`, max-width ~70%, right-aligned or left-aligned per layout direction

**DONE** (`packages/ui/src/ChatUserMessage.tsx` — `.msg-user` flex row hugging the right edge by default with a dark `.msg-user-bubble` [`--ink-900`/white/`--r-lg`, max-width 70%, `pre-wrap` to keep the user's own line breaks]; `align="start"` → `.msg-user-start` left-aligns it for focus mode; ds.css "Chat messages (M12.4)" block; presentational. Wired into `/chat`: the message map now branches — user turns render `ChatUserMessage`, assistant turns keep the existing Card [M12.4.2 restyles those]; +3 ui tests, 100% coverage)

### M12.4.2 — Assistant message: `.msg-assistant` — expert avatar (colored circle + initials), expert name (bold), "AI RENDITION" `.badge-ink`, "grounded in published knowledge + your upload" source label (`.muted` mono), optional "VERIFIED" `.badge-green`

**DONE** (`packages/ui/src/ChatAssistantMessage.tsx` — `.msg-assistant` header [expert-toned `.avatar` + bold `.msg-assistant-name` + `.badge-ink` "AI rendition" carrying the M2.2 "AI rendition of [Expert]" aria-disclosure + mono `.muted` source label + right-aligned `.badge-green` "Verified"] over a `.msg-assistant-body` children slot; ds.css "assistant turn" block; presentational. Wired into `/chat`: replaces the old green Assistant + amber rendition badges; `sourceLabel` derived from citation kinds [`answerSourceLabel`], `verified` = done+grounded+not-insufficient; reuses `AnswerView` [M12.4.3], degraded/insufficient/high-stakes/recommendation/feedback as body children; removed now-dead `renditionLabel`; +7 ui tests, 100% coverage)

### M12.4.3 — Answer prose with inline citations: existing `AnswerView` component (M4.2) with `.cite` crimson markers + `.cite.upload` blue markers; render-after-resolve behavior preserved

**DONE** (`packages/ui/src/AnswerProse.tsx` `AnswerProse` — extracted the prose-with-inline-citations from `AnswerView` into a tested, pure ds.css primitive: `[n]` markers → crimson `.cite` (knowledge) / info-blue `.cite.upload` (upload) chips, render-after-resolve preserved [verbatim text until `interactive` + ≥1 resolved; unresolvable `[9]` stays plain text], click-to-passage `onCite` + Enter/Space keydown; `apps/web` `AnswerView` now composes `AnswerProse` (mapping `ChatCitationDto.kind`→variant) over its existing sources drawer, decoupling prose (M12.4.3) from the sources list (M12.5); chat + history reuse unchanged; +7 ui tests, 100% lines/funcs)

### M12.4.4 — Action bar below each completed answer: "View sources (N)" toggle (`.btn-ghost`), "Save" (`.btn-ghost`), thumbs up/down feedback — refactor existing `AnswerFeedback` + `SaveAnswer` into a horizontal bar layout

**DONE** (`packages/ui/src/ChatAnswerActions.tsx` `ChatAnswerActions` — a presentational `.msg-actions` bar laying out the ghost "View sources (N)" toggle [render-after-resolve: shown only when `sourceCount > 0`; `aria-expanded`/`aria-pressed` reflect open state], the ghost Save control [→ static `.badge-green` "Saved" once bookmarked], and Yes/No feedback [`.btn-subtle`, active verdict promoted to primary/dark, helpful/not-helpful on `aria-label` — anti-emoji per M11.5], with the feedback reason field + errors as `children` below; ds.css `.msg-actions`/`.msg-actions-bar`; +10 ui tests, 100% coverage. Wired into `apps/web/app/chat/page.tsx`: the former separate `AnswerFeedback` + `SaveAnswer` page components merged into one stateful `AnswerActions` over `ChatAnswerActions`; new page-local `AssistantTurn` owns the per-message `sourcesOpen` toggle shared by the prose drawer + the bar; `AnswerView` gained an optional controlled `sourcesOpen` prop [omitted = always-show history default preserved].)

### M12.4.5 — Consultation recommendation card: warm background card with icon + heading + description + "Book with [Expert]" (`.btn-primary`) + "Maybe later" / "Ask another question" (`.btn-ghost`) — restyle existing `ConsultationPrompt`

**DONE** (`packages/ui/src/ChatConsultationCard.tsx` `ChatConsultationCard` — a presentational warm `.consult-card` [`--amber-50` bg per Design System]: calendar `currentColor` SVG icon + `--font-display` heading ["This looks worth a working session"] + the rule's reason + the three M7.2 actions [primary Book / ghost "Maybe later" / ghost "Ask another question"], each omitted when its callback is absent, all disabled while `busy`, follow-up error as `children`; ds.css `.consult-card*` block; +7 ui tests, 100% coverage. Wired into `apps/web/app/chat/page.tsx`: `ConsultationPrompt` now composes it [keeps the auth + `respondToRecommendation` funnel-attribution state, M10.2], passing a `bookLabel` of `Book {consultationType.name}` → `Book with {expertName}` → "Book a consultation".)

### M12.4.6 — Insufficient-knowledge, high-stakes, and degraded states: restyle existing cards to match the design system badge/card patterns

**DONE** (`packages/ui/src/ChatStateNotice.tsx` `ChatStateNotice` — a presentational ds.css primitive for the three post-answer states (PRD §"State Mapping"): a `card` variant = amber `.msg-notice` card (tone-tinted bg + matching `.badge` + body) for insufficient-knowledge (rephrase/book) and the high-stakes disclaimer (NT.4), and a `note` variant = compact `.msg-note` row (badge + muted text) for the fair-use degrade (M6.3); badge tone always matches the card accent; ds.css `.msg-notice`/`.msg-note` block; +5 ui tests, 100% coverage. Wired into `apps/web/app/chat/page.tsx`: `HighStakesNotice` + the inline insufficient-knowledge `Card` → amber `ChatStateNotice`, the degraded `Badge` → info `note` variant.)

### M12.5.1 — `.sources-rail` container: 320px sticky right panel, `--line` left border, scrollable; shows sources for the currently selected/latest answer

**DONE** (`packages/ui/src/SourcesRail.tsx` `SourcesRail` — the scrollable `.sources-rail` content container filling the 320px `.chat-rail` grid area [`--line` left border, `--surface` bg, full-height `overflow-y:auto`]: a `header` slot for the "SOURCES" label + trust badge [M12.5.2], a `children` cards slot [M12.5.3], and a muted empty state so the panel is never a blank gap; presentational. ds.css `.sources-rail`/`-head`/`-body`/`-empty` block; wired into `/chat` via `ChatLayout` `rail` prop [renders only in studio per M12.1.3]; +6 ui tests, 100% coverage)

### M12.5.2 — Rail header: "SOURCES" `.label` + passage count + `.trust-badge` ("ALL CITATIONS RESOLVED TO A REAL CHUNK" outlined crimson pill with checkmark)

**DONE** (`packages/ui/src/SourcesRailHeader.tsx` `SourcesRailHeader` — "SOURCES" `.label` + mono-muted passage count over an outlined-crimson `.trust-badge` with a checkmark SVG; render-after-resolve (OD#7): count + trust badge appear only once `count > 0`, singular/plural passage label; ds.css `.sources-rail-title`/`.trust-badge` block; presentational. Wired into `/chat`: a `railCitationCount` memo over the latest assistant turn's resolved citations feeds `SourcesRail`'s `header` slot; +5 ui tests, 100% coverage)

### M12.5.3 — Source cards: numbered, match percentage (mono, right-aligned), document icon (crimson for knowledge / blue for upload), title + version badge, location provenance (`.source-prov` mono), excerpt with left crimson/blue border (`.source-quote`); wired to existing citation data from `AnswerView`

**DONE** (`packages/ui/src/SourceCard.tsx` `SourceCard` — `.cite`/`.cite.upload` marker icon + bold `.source-card-title` + kind-toned version `.badge` + right-aligned mono `.source-card-match` (finite-only) + mono `.source-prov` + kind-accent-bordered `.source-quote`; optional `onSelect`→click-to-passage `<button>`; ds.css `.source-card*` (knowledge/upload accents never mixed). Wired into `/chat`: `railCitations` memo (latest answer's citations) → `SourceCard`s in `SourcesRail` body, title/provenance split from `sourceLabel` for uploads / `documentVersionId` for knowledge; `matchPercent`/`version` props built spec-complete but unwired (DTO lacks score/version); +7 ui tests, 100% coverage)

### M12.5.4 — Sources drawer fallback: when sources-rail is hidden (classic/focus mode or narrow viewport), sources open as a slide-over drawer instead

**DONE** (`packages/ui/src/SourcesDrawer.tsx` `SourcesDrawer` — a presentational slide-over: dimmed backdrop + right-anchored panel that reuses `SourcesRail` for the body [header + cards + empty state never drift from the persistent rail], dismissed by backdrop click / 44px close button / Escape-on-panel [no document listener → stays a pure function]; ds.css `.sources-drawer*` block + slide-in keyframes. New `apps/web/src/lib/use-media-query.ts` `useMediaQuery` (SSR-safe `matchMedia`). Wired into `/chat`: `railVisible = layoutPanes(direction).rail && wideViewport(≥1280px)`; when the rail isn't on screen (classic/focus or narrow), `AssistantTurn` gets `onOpenSourcesDrawer` and "View sources" opens the slide-over with that answer's citations instead of the inline list [single sources surface per layout]; shared `sourceCards()` helper feeds both rail + drawer; effect closes the drawer when the rail returns; +7 ui tests, 100% coverage)

### M12.6.1 — `.input-bar` sticky bottom container: attach document button (`.btn-subtle` icon left), text input ("Ask [Expert] anything about your business..." placeholder), send button (crimson circle with arrow icon, right)

**DONE** (`packages/ui/src/ChatInputBar.tsx` `ChatInputBar` — sticky `.input-bar` composer pinned to the foot of the `.chat-main` column: optional left `.btn-subtle .btn-icon` paperclip attach button [rendered only when `onAttach` given; `aria-expanded`/`aria-pressed` reflect the M12.6.2 popover state], the question `Textarea` [`.input-bar-input`], and a crimson circular `.input-bar-send` button [up-arrow SVG, enabled only when the trimmed draft is non-empty and not busy]; `children` slot above the row for the M12.6.2 popover + M12.6.3 helper text; ds.css `.input-bar*` block. Wired into `/chat`: replaces the old `Field`+`Textarea`+`Send Button`, moved out of the scrollable `.chat-content` into `.chat-main` so it stays pinned; `inputPlaceholder` memo → "Ask [Expert] anything about your business…" once a voice is picked. Keyboard send + auto-resize land in M12.6.4. +8 ui tests, 100% coverage)

### M12.6.2 — Upload attachment flow: clicking attach opens existing `UploadPanel` as a popover/dropdown above the input bar; show file type chips (XLSX, CSV, PDF, DOCX) + "TEMPORARY / NOT INDEXED" label after upload

**DONE** (`packages/ui/src/ChatUploadPopover.tsx` `ChatUploadPopover` — presentational `.upload-popover` dropdown chrome: header ("Attach document" + close X), a `.badge-info` mode label + accepted-format `.chip` pills (`UPLOAD_FILE_TYPES` = XLSX/CSV/PDF/DOCX), and a `children` slot for the upload controls; ds.css `.upload-popover*`. Wired into `/chat`: the existing `UploadPanel` (mode/file/upload state) now renders inside the popover with a mode-derived label ("Temporary · not indexed" / "Persistent · saved to knowledge", uppercased by the badge), mounted in the `ChatInputBar` `children` slot toggled by the M12.6.1 attach button (`attachOpen` state); removed the always-on inline panel from `.chat-content`; +8 ui tests, 100% coverage)

### M12.6.3 — Helper text below input: "Enter to send / Shift + Enter newline" (left), "N questions left this month" (right, from entitlements API)

**DONE** (`packages/ui/src/ChatInputHelper.tsx` `ChatInputHelper` — small muted two-column `.input-bar-help` row: static keyboard hint left ("Enter to send · Shift + Enter for newline", describing the M12.6.4 behavior) + remaining monthly quota right (mono `.input-bar-quota` "N questions left this month", singular/plural, clamped ≥0 + floored, NaN/Infinity-guarded → hidden, "Unlimited questions this month" takes precedence); render-after-resolve: the counter is omitted until the quota loads. ds.css `.input-bar-help`/`.input-bar-quota`; presentational. Wired into `/chat`: `inputQuota` memo over the same metered `ask_question` entitlement that feeds the sidebar meter (remaining = `limit ?? softLimit` − used; null threshold = unlimited) → rendered in the `ChatInputBar` `children` slot; +6 ui tests, 100% coverage)

### M12.6.4 — Keyboard behavior: Enter sends (unless Shift held); auto-resize textarea

**DONE** (`packages/ui/src/ChatInputBar.tsx` — `handleKeyDown` on the `Textarea`: Enter (no Shift, not mid-IME-composition via `nativeEvent.isComposing`) `preventDefault`s the newline and fires `onSend` when sendable; Shift+Enter falls through to a newline. Auto-resize via a hook-free `autoResize` inline ref callback — re-runs every render (keystrokes, deletions, and the parent clearing the draft after send), sets `height:auto` then `scrollHeight` while ds.css `min/max-height` clamp the rendered height (`overflow-y:auto` past the 200px cap). New optional `inputRef` prop on the shared `Textarea` primitive (a plain prop, not React's reserved `ref`, so `Textarea` stays a directly-callable function for the unit suite). No page changes — the existing `ChatInputBar` wiring inherits the behavior. +5 ui tests, 100% coverage)

### M12.7.1 — Tweaks floating panel: "Tweaks" header + close X, bottom-right overlay, card with shadow

**DONE** (`packages/ui/src/TweaksPanel.tsx` `TweaksPanel` — `role=dialog` `.tweaks-panel` fixed bottom-right card [`--surface`/`--sh-lg`/`--r-lg`, viewport-clamped + scrollable]: `.tweaks-panel-head` with an `<h3 className="h3">Tweaks</h3>` + `.btn-subtle .btn-icon` close X over a `children` slot for the M12.7.2 direction `.seg` / M12.7.3 density `.seg`+`.switch` sections; ds.css `.tweaks-panel*` block, all tokens. Presentational chrome only — not yet wired [M12.7.2 gives content, M12.7.4 the topbar show/hide toggle]; +5 ui tests, 100% coverage)

### M12.7.2 — Chat layout direction: `.seg` segmented control (classic / studio / focus) with one-line descriptions; persisted to localStorage

**DONE** (`packages/ui/src/TweaksLayoutControl.tsx` `TweaksLayoutControl` — the "CHAT LAYOUT — 3 DIRECTIONS" section: `.label` header over a full-width `.seg` segmented control [classic/studio/focus from `LAYOUT_DIRECTIONS`/`LAYOUT_DIRECTION_INFO`], the active option getting `.active`+`aria-pressed`, each button carrying its description as a `title` tooltip, and the chosen direction's one-line description below as `.muted` italic copy; ds.css `.tweaks-section`/`.tweaks-section-desc` block [full-width seg, italic desc]; presentational. Wired into `/chat`: `direction` is now settable + restored-from/persisted-to `localStorage` (`expertos:chat-layout-direction`, default-then-restore-after-mount to avoid hydration mismatch via `isLayoutDirection`); the `TweaksPanel` (M12.7.1) is now mounted with this control, open by default and dismissed by its close X [the topbar "Show tweaks" reopen affordance is M12.7.4]; +6 ui tests, 100% coverage)

### M12.7.3 — Density options: `.seg` segmented control (compact / regular / comfy); toggle switches for "Verified trust badge" + "Concierge review offer"

**DONE** (`packages/ui/src/TweaksDensityControl.tsx` "DENSITY & OPTIONS" section: `.label` over a full-width `.seg` (compact/regular/comfy from new `packages/ui/src/prefs.ts` `DENSITIES`/`DENSITY_INFO`/`isDensity`, mirroring `layout.ts`) + a `.tweaks-toggles` group of two `.switch` rows ("Verified trust badge" / "Concierge review offer"); ds.css `.tweaks-toggles`/`.tweaks-toggle` + `.chat-density-{compact,regular,comfy} .chat-thread` gap modifiers. `ChatLayout` gained a `density` prop → `chat-density-{density}` root modifier. Wired into `/chat`: density + both toggles restored-from/persisted-to localStorage (`expertos:chat-density` / `:show-verified-badge` / `:show-concierge-offer`); density drives the `.chat-thread` rhythm, the verified toggle gates the M12.4.2 "Verified" badge via a new `AssistantTurn` `showVerifiedBadge` prop, the concierge toggle is a forward-looking pref; mounted in `TweaksPanel` below `TweaksLayoutControl`; +11 ui tests, 100% coverage)

### M12.7.4 — "Hide tweaks" / "Show tweaks" button in the topbar toolbar

**DONE** (`packages/ui/src/ChatTweaksToggle.tsx` `ChatTweaksToggle` — a presentational `.btn-subtle .btn-sm .chat-tweaks-toggle` icon+label toggle: a sliders SVG ahead of a caption that flips with `open` ("Hide tweaks" while the panel shows / "Show tweaks" when hidden), `aria-pressed` reflecting state; ds.css `.chat-tweaks-toggle` (icon hugs label). Wired into `/chat`: mounted as the leading control in the `ChatTopbar` `.chat-topbar-aside` toolbar group, flipping the existing `tweaksOpen` state that mounts the M12.7.1 `TweaksPanel` [the panel's close X already clears it]; +5 ui tests, 100% coverage. **M12.7 (Tweaks panel) COMPLETE.**)

### M12.8.2 — Post-login redirect: after Google sign-in, redirect to `/chat` (existing); handle returning users (skip login if session active)

**DONE** (`apps/web/app/page.tsx`: redirect moved from a render-time `window.location.href` side effect to a `useEffect` + Next `useRouter().replace("/chat")` client navigation; `loading || user` now shows the loading view so an already-signed-in returning user with a persisted Firebase session never flashes the login form before being routed to `/chat`)

### M12.9.1 — Mobile responsive: sidebar as slide-over overlay (< 900px), sources rail hidden, input bar full-width

**DONE** (`packages/ui/src/ChatSidebarDrawer.tsx` left-anchored slide-over mirroring `SourcesDrawer` [dimmed backdrop + left panel + Escape, hosts the same `ChatSidebar` content] + `ChatMenuButton` hamburger + `ChatTopbar` `leading` slot; ds.css `.chat-sidebar-drawer*` + `chat-sidebar-drawer-in` keyframe + `.chat-topbar-leading`/`.chat-menu-btn`. Wired into `/chat`: `wideSidebar = useMediaQuery("(min-width: 900px)")`, `sidebarInGrid = layoutPanes(direction).sidebar && wideSidebar`; menu button shown in topbar leading only when `!sidebarInGrid`; effect closes the overlay when the sidebar returns to grid; new/open conversation dismisses it; shared `sidebarBody`/`sidebarFooter` feed both grid pane + overlay. Sources-rail-hidden + full-width input bar already handled by M12.5.4 / M12.6. +11 ui tests, 100% coverage.)

### M12.9.2 — ds.css conformance: no hardcoded colors/px; all new components use ds.css tokens; upload = info-blue, knowledge = crimson distinction maintained

**DONE (audit, no code changes).** Full M12 conformance sweep confirms the rules were held throughout: zero hardcoded hex outside the ds.css token definitions (the only app-code hex is the four Google-brand `fill` values in `apps/web/app/page.tsx`'s sign-in SVG, intentional + scoped-disabled per M12.1.1); zero off-scale px in app code (only inline styles are `Bar`'s dynamic `width:%` and one flex-alignment pair on the login left panel — neither a color/size token violation); upload=info-blue vs knowledge=crimson never mixed (verified across `SourceCard`/`Cite`/`AnswerProse`/`Badge` — `kind="upload"`→`.cite.upload`+`Badge tone="info"`, `knowledge`→`.cite`+`tone="red"`). Mechanized gates all green: root `lint:css` (stylelint), `@expertos/ui lint` + `web lint` (eslint no-hardcoded-color/px rules), ui jest 213, root `knip`.

### M12.9.3 — Dark sidebar: ensure all sidebar elements (logo, nav items, search, usage) render correctly on `--ink-900` background

**DONE (audit, no code changes).** `ChatSidebar` applies `cx("side", "chat-side", …)` so the `.side` base supplies the `--ink-900` bg in every host; all sub-elements carry explicit dark-context treatment (white/`--ink-300`/`--ink-400` text, `rgba(255,255,255,…)` hover/track surfaces): the `.brand` logo (white `.expert`/crimson `.os`), `.chat-side-collapse`, `.chat-side-new` crimson button, `.chat-search-*` (search field/results), `.chat-convo-*` (RECENT list + avatars + active=`--red-600`), `.sidebar-usage-*` (count/`.bar` lightened track/plan label/upgrade link), and the `.side-foot` identity footer. The mobile overlay is covered too — `.chat-sidebar-drawer .side { position:static; height:100% }` neutralizes the grid rail's `sticky`/`100vh` so the same dark content renders correctly in the slide-over (M12.9.1). **M12.9 (Polish & responsive) COMPLETE.**

### M12.9.4 — Loading/empty states: skeleton loaders for conversation list, "Start a new conversation" empty state for chat area, spinner for streaming

**DONE** (three new ds.css primitives: `Skeleton` (`packages/ui/src/Skeleton.tsx`, aria-hidden shimmer block; size/shape via paired class so no off-scale px in markup), `ChatTypingIndicator` (`ChatTypingIndicator.tsx`, three pulsing dots as a polite `role="status"` with accessible label — the streaming indicator under the assistant avatar per PRD §"State Mapping"), `ChatEmptyState` (`ChatEmptyState.tsx`, centered "Start a new conversation" prompt — speech-bubble glyph + `.h2` title + muted desc + children slot). `ChatConversationList` loading state now renders 4 shimmering skeleton rows (`aria-busy` nav) instead of a bare "Loading…" note. ds.css "Loading & empty states (M12.9.4)" block — `.skeleton`/`skeleton-shimmer` (dark-rail translucent-white variant under `.chat-side`), `.typing`/`typing-bounce`, `.chat-empty*` — plus a `prefers-reduced-motion` block disabling the shimmer/typing/drawer animations. Wired into `/chat`: `AssistantAnswer` shows the typing indicator while streaming (was a literal "…"); empty thread shows `ChatEmptyState`. +6 ui tests (213 total), 100% coverage; stylelint/eslint/knip green.)

### M13.1.1 — Restructure sidebar nav groups to match mockup: OPERATE (Dashboard, Knowledge + count badge, Conversation -> Knowledge, Low-confidence queries + count badge), MONETIZE (Plans & Entitlements, Revenue, Users & Subscriptions), EXPERT PORTAL (Voice profiles, Concierge queue + count badge)

**DONE** (`apps/admin/src/components/AdminFrame.tsx` `NAV` regrouped into the mockup's OPERATE/MONETIZE/EXPERT PORTAL groups + ANALYTICS/SYSTEM for the remaining working pages so none are orphaned; per-item `group`+`role`, `GROUP_ORDER`-driven `Sidebar` with role filtering; Dashboard `/` item w/ exact-match active; mockup-aligned labels. Count badges = M13.1.2.)

### M13.1.2 — Count badges on nav items: fetch counts from existing APIs (knowledge needing review, flagged queries, open concierge items) and render as `.navitem .tag`

**DONE** (`apps/admin/src/lib/use-nav-counts.ts` `useNavCounts` fetches the three attention counts once `/me` resolves the role — knowledge versions in `expert_review` via `listDocuments`, open concierge (`requested`+`in_review`) via `getConciergeReviews`, flagged 👎 answers via `getFailedQueries` [admin-only]; best-effort per badge [one API failure leaves its count `null`, doesn't block the others], display-capped at 99→"99+", UX-only [API still enforces role+RLS]. `AdminFrame` `NavItem` gained an optional `badge: keyof NavCounts`; `NavLink` renders the existing ds.css `.side .navitem .tag` chip [hidden until loaded + non-zero] on Knowledge / Low-confidence queries / Concierge queue. No CSS added — reuses the `.tag` class.)

### M13.1.3 — Bottom-pinned user identity: avatar (initials + colored circle) + name + role label ("Admin . ExpertOS"); Sign out button (`.btn-ghost` on dark)

**DONE** (`apps/admin/src/components/AdminFrame.tsx` `SidebarFooter` — `.side-foot` (margin-top:auto pin) hosts an `avatarTone`/`avatarInitials` `.avatar` + ellipsised name (displayName → email local-part → "You") + "Admin/Expert · ExpertOS" role label over a ghost Sign out (moved off the topbar; dark-context `.side-foot .btn-ghost` restyle); ds.css `.side-foot`/`.side-user*` block, stylelint-clean.)

### M13.1.4 — Topbar: breadcrumb ("ADMIN > Page Name" `.label`), role badge ("ADMIN VIEW" `.badge-red` / "EXPERT VIEW" `.badge-amber`), notification bell icon, "All screens" link

**DONE** (`AdminFrame` topbar: role-aware `.crumb` breadcrumb (mono `.label` "Admin"/"Expert Portal" prefix + chevron + `currentPageLabel(pathname)` display-font page name) + "Admin view"/"Expert view" `Badge tone=red/amber` + a `BellIcon` `.btn-icon .btn-subtle` (aria-labelled, presentational); ds.css `.crumb*` block. "All screens" link omitted — mockup-deck nav artifact, no screen-index page in the product.)

### M13.2.1 — Page header: greeting "Good morning, [Name]" (`.h1`), subtitle with validation loop text (`.lede`), time-range segmented control (7d / 30d / QTD `.seg`); wire to existing analytics APIs

**DONE** (`apps/admin/app/page.tsx` rebuilt the bare landing page into the dashboard: hour-keyed greeting + auth display-name, `.eyebrow` ("Phase 1 · MVP · Last Nd") + `.lede` validation-loop subtitle, and a `.seg` 7d/30d/QTD control [QTD resolves to a quarter-to-date day count] driving the analytics `days` query.)

### M13.2.2 — KPI stat cards: 4-up grid of `.stat` cards (MRR, Active Subscribers, Citation Resolve Rate, Consult Conversions) with `.v` display value + `.d` delta (`.up`/`.down`); wire to `/admin/analytics/usage` + `/admin/analytics/validation` + `/admin/revenue/report`

**DONE** (new ds.css `.kpi-grid` responsive N-up grid over the existing `Stat` primitive; `Promise.all` over `getRevenueReport`/`getFunnelAnalytics`/`getValidationAnalytics`: **MRR** (`mrrCents` + MoM `.up`/`.down` delta computed from the revenue period series), **Active subscribers** (`activeSubscriptions` + live-plan count), **Consult conversions** (funnel-attributed `consultations` + booked revenue), **Activation rate** (`validation.activation.activationRate`). Metric note: the mockup's "Citation resolve rate" is not a measured platform metric — resolvability is *enforced* by render-after-resolve (OD#7), never sampled — so the make-or-break activation signal stands in its place.)

### M13.2.3 — Questions Answered card: large number + badge row (GROUNDED `.badge-green`, LOW-CONF `.badge-red`, INSUFFICIENT `.badge-ink` with percentages) + stacked bar chart (new `.progress-bar-stacked`)

**DONE.** New backend rollup (no `usage_logs` aggregate carried a grounding signal): `AnalyticsService.questions` → `GET /admin/analytics/questions` (`@Roles("admin")` cross-tenant RLS; raw aggregate over `messages` partitioning each assistant answer by citation count — grounded ≥2 / low-conf =1 / insufficient =0, since `messages.confidence` is unused — into window total + breakdown + a trailing daily series). New `StackedBar` ui primitive (`.progress-bar-stacked`: crimson grounded / red low-conf / light insufficient) + ds.css `.qa-*` card (big total + 3 badge tones + overall proportional bar + daily stacked-column trend). Wired into `apps/admin/app/page.tsx` over the dashboard 7d/30d/QTD window. (Spec said "wire to `/admin/analytics/usage`" but usage has no grounding signal — added the dedicated questions endpoint.)

### M13.2.4 — Consultation Funnel card: horizontal bar rows (Questions -> Recommend -> Booked -> Revenue) using `.bar` with proportional fills + bottom summary text; wire to `/admin/analytics/funnel`

**DONE** (`apps/admin/app/page.tsx` `FunnelCard`: four proportional `.bar` rows [ink fill, distinct from the crimson quota meter] over the existing `data.funnel` from `/admin/analytics/funnel` — fills proportional to the funnel top [conversations=100%], Revenue tracks Booked width; recommend→book + avg-booking summary line; new ds.css `.funnel-*` block, reuses the `Bar` primitive.)

### M13.2.5 — Low-Confidence Queries card: `.eyebrow` header + query list (confidence circle badge on red-amber scale, question text, metadata `.muted`, "Draft knowledge" `.btn-ghost`); wire to `/admin/failed-queries`

**DONE** (`apps/admin/app/page.tsx` `LowConfidenceCard`: `.eyebrow`/`.h3` header + "Open pipeline →" ghost link, per-row `.conf-circle` red→amber confidence badge [neutral dash when no score] + 2-line-clamped question + muted meta [insufficient `.badge-amber` · reason · model · relative time] + "Draft knowledge" `.btn-ghost` → `/knowledge-drafts`; wired to `getFailedQueries` limit 6. New ds.css `.lowconf-*`/`.conf-circle` block. Meta line uses signals the DTO actually carries — no "asked Nx"/expert-name in `FailedQueryDto`.)

### M13.2.6 — Knowledge Pipeline card: status rows with badge tones (DRAFT `.badge-ink`, AI PROCESSING `.badge-info`, EXPERT REVIEW `.badge-amber`, PUBLISHED `.badge-green`) + counts; "Review queue ->" link; wire to `/knowledge` API

**DONE.** New backend rollup (the `/knowledge` list is `take:50`-bounded, so it can't give accurate stage counts — followed the M13.2.3 precedent of a dedicated endpoint): `AnalyticsService.knowledgePipeline` → `GET /admin/analytics/knowledge-pipeline` (`@Roles("admin")` cross-tenant RLS; one Prisma `groupBy` over `document.status` folded into a zero-filled `Record<PublishStatusValue, number>` + total — a live snapshot, not windowed). New dashboard `KnowledgePipelineCard` (in `apps/admin/app/page.tsx`): badge-toned status row per stage (DRAFT ink → AI PROCESSING info → EXPERT REVIEW amber → PUBLISHED green; `archived` omitted) with a right-aligned mono count + "Review queue →" link to `/knowledge`. New ds.css `.pipeline-*` block. `KnowledgePipelineDto` in shared. +2 api tests.

### M13.2.7 — Concierge SLA card: dark card variant (`.dark-card`, `--ink-900` bg, white text), "CONCIERGE SLA" label + queue count badge, large time display, "Open queue ->" button; wire to `/admin/analytics/concierge`

**DONE.** New reusable `.dark-card` ds.css primitive (M13.7.2: `--ink-900` bg, white text, `--r-lg` — dark-context `.label`/`.muted` overrides) + `.sla-*` block. Dashboard `ConciergeSlaCard` (in `apps/admin/app/page.tsx`): "CONCIERGE SLA" `.label` + "N in queue" `.badge-amber` (= `byStatus.requested + in_review`) over a large `--font-display` avg time-to-answer display (`sla.avgResponseMinutes` → "21h 04m", "—" when none answered), "avg time-to-answer · target 24h" subtitle (24h = M9.1 default SLA, stated as copy not re-fetched), and an "Open queue →" `.btn` (light, contrasts on dark) → `/concierge-reviews`. Wired to `getConciergeAnalytics` over the dashboard 7d/30d/QTD window.

### M13.4.1 — Page header: eyebrow "CONFIGURATION, NOT CODE", heading, subtitle explaining the matrix, "Reset to seed" (`.btn-ghost`) + "Publish changes" (`.btn-primary`)

**DONE** (`apps/admin/app/entitlements/page.tsx` rebuilt: `.eyebrow`/`.h1`/`.lede` header + a **staged-edit** model — edits stage locally, "Publish N changes" `.btn-primary` PATCHes every dirty cell via the existing per-cell `/admin/entitlements/:planId/features/:featureId` (effective immediately — no deploy), "Discard changes" `.btn-ghost` reverts to last-loaded server state. Honest deviation: no reset-to-seed endpoint exists, so the ghost action is **Discard changes** (revert unsaved edits), not a literal seed reset.)

### M13.4.2 — Matrix table (`.matrix-table`): column headers with plan name + real pricing (Free $0, Plus $4.99/mo, Premium $9.99/mo · $69.99/yr — surfaced via new `EntitlementPlanPriceDto`/`prices[]` on the matrix DTO + `PlanPrice` join in `EntitlementMatrixService.getMatrix`); top-tier column visually emphasized (`.matrix-col-premium` crimson-tinted bg + crimson bold header); row type badge (boolean=ink / metered=info). Honest deviation: the data model has only `boolean`/`metered` feature types — no `enum` type exists — so the spec's enum row is rendered per its real type.

**DONE**

### M13.4.3 — Cell rendering: boolean = `.switch` toggle (crimson when on; em-dash when off); metered = editable hard-limit `.input` + window `.select` (unit "/day"·"/week"·"/month") + soft-limit input, "UNLIMITED" `.label` when uncapped; dirty cells show an "Unsaved" `.badge-amber`, per-cell publish/validation errors inline.

**DONE** (new ds.css `.matrix-*` block; booleans unified on `.switch` rather than checkmark-vs-toggle split — the genuine editable control the concierge row already specified.)

### M13.4.4 — Footer info cards: 2-up grid below matrix (`.matrix-foot`), "FAIR USE" (`.badge-amber`) degrade-don't-block card + "QUOTA CELLS" (`.badge-info`) OD#4 card; wired to existing `/admin/entitlements` API.

**DONE**

### M13.5.1 — Page header: expert avatar (large, colored) + name (`.h1`) + meta (example count `.muted`) + "AWAITING YOUR SIGN-OFF" `.badge-amber` + sign-off action `.btn-primary`

**DONE (schema-honest).** New `apps/admin/app/voice-profiles/[id]/page.tsx` over the new `GET /voice-profiles/:id`: large `.avatar.avatar-lg` (expert-toned) + expert name + locale-aware status `.badge` + amber dot "Awaiting your sign-off" badge (when `expert_review`) + meta (language · example count) + lifecycle sign-off buttons (submit/approve/request-changes, approve = `.btn-primary`). *Omitted: role/topics/profile-version — not schema-backed (`VoiceProfile` has no version field; expert role/topics not modeled).*

### M13.5.2 — Warning banner: full-width amber card with warning text about reputation/voice/facts separation

**DONE** (amber `.badge` + body card on the detail page, EN/VI; the NT.2 "voice shapes tone, not facts" reputation copy).

### M13.5.3 — Voice Profile card (left): dimension bars (`.voice-bar` -- segmented crimson bar on gray track for Directness/Level of detail/Warmth with value labels), Structure `.chip` list, Terminology `.chip` list (mono), Rendition policy `.seg` (First person / Third person)

**DEFERRED: no schema backing** (only free-text `guidelines` exists; rendered as a guidelines card instead).

### M13.5.4 — Do & Don't Rules card (right): green checkmark + "do" rules, red X + "don't" rules

**DEFERRED: no schema backing** (do/don't rules are not modeled — folded into the free-text guidelines).

### M13.5.5 — Voice comparison card (right): "SAME FACTS . VOICE OFF VS ON" header + "FACTS IDENTICAL" `.badge-green`; two `.panel` blocks showing neutral vs voiced answer with visual distinction (muted border vs crimson accent)

**DEFERRED: no stored neutral-vs-voiced answer pair to render** (would require fabricated data).

### M13.5.6 — Voice Examples table (full width): TOPIC, SOURCE, FIDELITY, Action; example count header; wire to existing `/voice-profiles` API

**PARTIAL (schema-honest).** Real examples (`prompt` + `content`) are listed in `.panel` cards under an example-count header, wired to the new `GET /voice-profiles/:id`. *Omitted: FIDELITY scores (not measured/stored) + per-example Approve (sign-off is profile-level, not per-example).*

### M13.6.1 — Two-pane layout: queue list (~380px left) + review detail (flex:1 right), inside the existing `.shell` content area

**DONE** (`apps/admin/app/concierge-reviews/page.tsx` rebuilt the expandable list into `.review-pane` grid [`minmax(320px,380px)` `.queue-list` + flex `.review-detail`, stacks <980px]; new ds.css `.review-pane`/`.queue-list`/`.review-detail`/`.verdict-card` block.)

### M13.6.2 — Queue list: header "QUEUE . N OPEN" + "SLA 24H" `.badge-amber`, filter `.seg` (Open / Mine / Done), queue items (question text truncated, mode badge "AUTO . SILENT" `.badge-ink` or "USER-PROMPTED" `.badge-amber`, confidence badge `.badge-red`, time elapsed); active item highlighted; wire to `/concierge-reviews` list API

**DONE.** Open=requested/in_review, Done=answered/escalated/dismissed (client-bucketed over the loaded page). Honest deviation: "Mine" surfaces `claimedAt`-set items (the DTO carries no claimer identity); "SLA 24h" is the M9.1 default as a header chip.

### M13.6.3 — Review detail header: mode indicator badge, confidence badge, SLA time badge (all with dots)

**DONE** (added an optional `dot` prop to the shared `Badge` primitive + ui test; SLA badge shows remaining/overdue from `slaDueAt`). Honest deviation: "Claim" omitted — no claim endpoint exists.

### M13.6.4 — User question section: `.label` with user email + voice name, dark card (`.dark-card` / `.msg-user` style) with question text, context line (`.muted`) explaining retrieval score + flag reason

**DONE** (`.dark-card` question bubble + muted context from confidence/visibility). Honest deviation: user email + voice name omitted — `ReviewQueueDetailDto` carries neither.

### M13.6.5 — AI answer section: `.label` explaining "user saw this", `.panel` with "AI RENDITION" `.badge-ink` + "low confidence" + answer text

**DONE**

### M13.6.6 — Verdict section: "YOUR VERDICT" `.label`, three `.verdict-card` options (Bad: X + "flags source chunks", Good: checkmark + "deliver as-is", Great: star + "-> voice example"); selectable with hover/active state

**DONE** (selectable `.verdict-card` grid with tone-colored X/check/star SVG icons; reusable status-pipeline primitive per M13.7.3.)

### M13.6.7 — Refined answer section: `.label` + `.textarea` pre-filled with original, flywheel info text (`.muted`) explaining immediate + global effects

**DONE**

### M13.6.8 — Action bar: "Push refined update" (`.btn-primary` + icon), "Escalate to paid consultation" (`.btn-dark` + icon), "Dismiss" (`.btn-ghost`), "User notified by email on delivery" (`.muted`); wire to `/concierge-reviews/:id` respond/escalate APIs

**DONE** (Push refined update → `respond` [verdict + edit]; Escalate → `escalate`; email note). Honest deviation: "Dismiss" omitted — no dismiss endpoint exists.

### M13.7.1 — Role-aware sidebar: admin sees all groups; expert sees only EXPERT PORTAL group (existing `AdminFrame` role logic, restyle to match mockup grouping)

**DONE (already satisfied).** `AdminFrame` `Sidebar` filters `NAV.filter((item) => item.role === "expert" || role === "admin")`: admins see every group, experts see only the `expert`-role items (OPERATE knowledge/drafts/answers + EXPERT PORTAL); UX gate over the real `@Roles` boundary. Mockup grouping applied in M13.1.1.

### M13.7.2 — Dark card component (`.dark-card`): reusable `--ink-900` bg, white text, `--r-lg` radius card for Concierge SLA, user question bubbles

**DONE (built in M13.2.7).** `.dark-card` in ds.css (`--ink-900` bg + white text + `--r-lg` + dark-context `.label`/`.muted`); reused by the dashboard Concierge SLA card (M13.2.7) and the concierge review user-question bubble (M13.6.4).

### M13.7.3 — Kanban component: reusable `.kanban` + `.kanban-col` for any status-pipeline view

**DONE (built in M13.3).** `.kanban`/`.kanban-col` (+ `.kanban-card`/`.kanban-steps`) in ds.css power the M13.3 knowledge-approval board; columns flex/scroll independently, active card gets the amber border.

### M13.7.4 — Voice dimension bar component (`.voice-bar`): segmented bar with N filled crimson segments on gray track

**BLOCKED on M13.5** (voice profile page deferred pending a PM/schema decision — the mockup's voice dimensions have no schema backing; `.voice-bar` lands with that page).

### M13.7.5 — ds.css conformance: no hardcoded colors/px; all badge tones match Design System rules (Draft=ink, Processing=info, Review=amber, Published=green)

**DONE.** Badge tones match exactly (`apps/admin/src/lib/status-tone.ts` `PUBLISH_TONES` draft=ink / ai_processing=info / expert_review=amber / published=green); no hardcoded hex outside the scoped Google-brand SVG. **Two real px gaps found + fixed:** `apps/admin/src/components/admin-login.css` was full of raw px **but sat outside the `lint:css` glob** (`apps/**/app/**` only) so it never failed — converted px→rem at 1rem=15px (mirrors the web `login.css` fix) and broadened the `lint:css` glob to also cover `apps/**/src/**/*.css`; the Google sign-in button's inline `style={{ …gap:"12px" }}` (caught by neither linter) moved to a `.admin-login-google` class. Gates green: root `lint:css` (now lints admin-login.css) + admin `tsc` + `next lint` + root `knip`.

### M15.1.1 — Test harness setup: mock providers (Firebase auth, locale context, router), shared render helpers, MSW or manual fetch mocks for API calls (`/me`, `/conversations`, `/entitlements`, `/chat`)

**DONE.** `apps/web` now has a jsdom + Testing Library harness on the repo's ts-jest preset (not `next/jest` — its next-swc native binary is arch-broken in-sandbox, same as `next build`). Pieces: `jest.config.cjs` (ts-jest transform with `jsx:react-jsx`/CommonJS override, css→stub, default `roots` so node-module manual mocks resolve), `jest.setup.ts` (jest-dom matchers, `NEXT_PUBLIC_FIREBASE_*` env so `isFirebaseConfigured`, matchMedia/scrollIntoView polyfills, per-test reset); controllable mocks — `__mocks__/firebase/{app,auth}.ts` over `test/auth-state.ts` (`setMockUser`/`makeMockUser`), `__mocks__/next/navigation.ts` over `test/router-state.ts` (spy router + pathname), `test/api-mock.ts` (manual `fetch` registry keyed `METHOD pathname`, records calls, 404 default); `test/render.tsx` `renderWithProviders` wraps the unit in the REAL `AuthProvider`+`LocaleProvider` (exercises provider code) + re-exports the Testing Library surface. Validated by `test/harness.test.tsx` (5 tests: sign-in, signed-out, EN→VI locale, router redirect, fetch routing). knip: testing-library deps added to `ignoreDependencies` (test-only, knip-ignored files). Gates green: web `tsc` + `next lint` + jest (5) + root `knip`. See LEARNINGS #18.

### M15.1.2 — Chat page tests: message rendering (user bubble + assistant message + citations), send flow (input → API call → stream append), voice picker selection, layout direction switching, empty state, error states, insufficient-knowledge / high-stakes / degraded notice rendering

**DONE** (`apps/web/app/chat/page.test.tsx`, 10 tests; harness fetch mock gained SSE-stream support [`mockApi(...,{sse:[...]})` → ReadableStream body] so `streamChat`'s parser runs for real; jest.setup polyfills TextEncoder/TextDecoder/ReadableStream for jsdom)

### M15.1.3 — History page tests: conversation list rendering + search, conversation detail (message replay + saved answers), rename, delete

**DONE** (`apps/web/app/history/page.test.tsx`, 10 tests: signed-out/empty states, recent list, full-text search [snippet + no-match], open→transcript replay with cited answer, save-from-transcript, inline rename, saved-answers panel load+remove [no conversation-delete affordance exists → "delete" = remove saved bookmark `DELETE /saved-answers/:id`], list-load error)

### M15.1.4 — Account page tests: plan display + usage meter, locale toggle persistence, sign-out flow

**DONE** (`apps/web/app/account/page.test.tsx`, 8 tests: current-plan badge, M6.3 metered usage meter (used/cap), disabled-metered "not included", boolean included/not-included rows, self-serve upgrade CTA→`POST /billing/checkout` (M6.2) with redirect, "Manage billing"→`POST /billing/portal`, signed-out + load-error states, VI locale-aware currency formatting ("9,99 …/tháng"). **Honest scope note:** locale-toggle *persistence* + *sign-out* are not rendered by the account page — the toggle + sign-out live in the chat topbar, persistence lives in `LocaleProvider` — so those are covered by the i18n/shared-lib suites M15.1.5/.6; the account page's own locale dependence (price formatting) is covered here.)

### M15.1.5 — i18n tests: locale provider switching (EN→VI), dictionary key completeness (all `useT` calls resolve), formatted dates/currency respect active locale

**DONE** (`apps/web/src/lib/i18n/i18n.test.tsx`, 9 tests: LocaleProvider EN→VI switching + localStorage/`PATCH /me/locale` write-through + resolution order [localStorage pref > profile seed]; EN/VI dictionary lockstep [identical leaf-key set + non-empty leaves in both locales + `{placeholder}` parity per key]; locale-aware currency/date formatting wired to the active locale [EN "$4.99" → VI "4,99…"/"thg 6"])

### M15.1.6 — Shared hooks/lib tests: `useMediaQuery`, `useLocale`, `useT`, API client functions, `firebase.ts` emulator-aware init

**DONE** (`apps/web/src/lib/{use-media-query,api-clients,firebase}.test.ts*`, +54 → web 96): `useMediaQuery` (initial-match / breakpoint-cross change events / unsubscribe-on-unmount / re-subscribe-on-query-change / SSR-safe no-`matchMedia` guard, via a controllable per-test `matchMedia`); all web API client fns — chat (`streamChat` SSE parse + body/auth, `respondToRecommendation`, `submitFeedback` reason-opt, `fetchExperts`), account (`fetchEntitlements`/`fetchUpgradePlans`/`startCheckout`/`openBillingPortal`), history (list/get/search-encode/rename/saved CRUD incl. 409→`duplicate` + 204-delete), upload (`uploadFile` multipart + API `{message}` error surface), i18n profile (`fetchProfileLocale`/`updateProfileLocale`) — each asserting request method/path/auth-header/body + response pass-through + non-2xx throw; `firebase.ts` emulator-aware init (configured flag both ways, cached Auth, `GoogleAuthProvider`, emulator connect only when host set, `window.__e2eSignIn` install + delegation) via `jest.resetModules()`+typed `require` per env. (`useLocale`/`useT` already covered by M15.1.5; removed the now-redundant `@testing-library/react` knip `ignoreDependencies` entry.) **M15.1 (web jest suite) COMPLETE.**

### M15.2.1 — Test harness setup: mock auth context (admin vs expert role), locale context, admin-client fetch mocks, shared render helpers

**DONE.** `apps/admin` jsdom + Testing-Library harness mirroring web (M15.1.1): `jest.config.cjs` (jsdom/ts-jest, NOT next/jest — next-swc arch-broken), `jest.setup.ts`, `test/{render,api-mock,auth-state,router-state}.ts` + `__mocks__/firebase/*` + `__mocks__/next/navigation.ts`. `renderWithProviders(ui, { user?, role?, denied?, locale? })` wraps the REAL admin `AuthProvider`+`LocaleProvider`; delta vs web — role/denied resolve through `POST /me/admin-session` (M14 whitelist gate), auto-mocked (`{ok,role,user}` or 403→Access-Denied); `role` defaults `admin`; localStorage key `expertos:admin-locale`. `test/harness.test.tsx` (6 smoke tests). knip: per-workspace `ignoreDependencies` for the testing-library deps (trim in M15.2.2 when page tests land under `app/**`/`src/**`). admin `tsc`/`next lint`/`jest` 6 + root `knip`/`lint:css` green.

### M15.2.2 — AdminFrame tests: role-aware nav filtering (admin sees all groups, expert sees only EXPERT PORTAL), breadcrumb rendering, nav count badges, sidebar footer identity, access-denied gate when `denied=true`

**DONE** (`apps/admin/src/components/AdminFrame.test.tsx`, +12 → admin 18: role-aware nav [admin sees all 5 groups + MONETIZE/SYSTEM items; expert sees only OPERATE+EXPERT PORTAL, admin-only groups/items absent], topbar breadcrumb [`.crumb .label` prefix Admin/Expert Portal + `.crumb-page` active-page label resolved from pathname incl. root→Dashboard] + role badge tone, `.navitem .tag` count badges from the queue APIs [knowledge-review / open-concierge requested+in_review / flagged-queries; 99+ cap; null-when-API-fails → no chip], identity footer [avatar initials + display name + "Admin · ExpertOS"] + sign-out → falls back to sign-in screen, M14 access-denied gate [no nav/page body] + signed-out sign-in screen. knip note: the testing-library deps stay in the admin `ignoreDependencies` — knip's global `**/*.test.tsx` ignore means the only usage [via `test/render`] is in ignored files, so removing the ignore flags all 3 as unused [verified]; the M15.2.1 "trim when page tests land" note doesn't apply.)

### M15.2.3 — Dashboard tests: KPI cards render with mock analytics data, funnel bar proportions, low-confidence query list, knowledge pipeline status badges, concierge SLA card

**DONE** (`apps/admin/app/page.test.tsx`, +15 → admin 33: four KPI stats [MRR + MoM up/down/omitted delta, active subscribers + live-plan count, consult conversions + booked revenue, activation rate], Questions-Answered grounding breakdown + empty-series note, Consultation-Funnel proportional bar fills [100%/25%/5% from the conversation top] + recommend→book summary + 0%/em-dash empty case, Knowledge-Pipeline 4-stage badge tones [ink/info/amber/green] + counts [archived omitted], Concierge SLA dark-card avg-time "21h 04m"/em-dash + open-queue badge, Low-Confidence rows [conf-circle tone, insufficient badge, em-dash no-score, empty state], 7d/30d/QTD range refetch, load-error path. Full-shaped DTO factories feed the fetch mock. **Flakiness fix (LEARNINGS #19):** the page double-loads on mount — the auth memo recreates `getIdToken` when the admin-session resolves the role → the `load` effect re-fires with a transient `setData(null)` — so all data-dependent assertions are wrapped in `waitFor` to retry past the clear window; verified stable over 5 runs.)

### M15.2.4 — Knowledge page tests: kanban board column rendering per status, card actions (approve/request-changes), conversation-to-knowledge table

**DONE** (`apps/admin/app/knowledge/page.test.tsx`, +12 → admin 45: 4-column kanban [head labels + pipeline count badges from `/admin/analytics/knowledge-pipeline`, not the take:50 doc lists], per-status card bodies [Draft summary, AI-Processing `parse → chunk → embed` + 66% progress bar when chunked, Expert-Review approve+diff actions + amber `.is-active` highlight, Published `v4 live` badge + approval date], the "Approve & publish" action [→ `POST /knowledge/versions/:id/approve` + board reload; error-badge path surfacing the API `{message}`], the status-pipeline step filter [click "Published" → board narrows to one column], the Conversation → Knowledge table [draft row with from-chat "yes" + localized status badge + Draft link; empty state], and the load-error path. `listDocuments` is called once per status with `?status=` but the mock keys on pathname — so a single dynamic handler reads the status off the URL and serves that column. `waitFor` past the LEARNINGS #19 double-load. **Honest scope:** "request-changes" is a detail-page action, not on the board — the kanban exposes only Approve & publish.)

### M15.2.5 — CRUD page tests: entitlement matrix cell editing + staged publish + discard, access control add/remove/role-toggle + self-lockout guard, user management role change + deletion request

**DONE** (+26 → admin 71: `app/entitlements/page.test.tsx` (matrix render + plan pricing/premium emphasis + type badges + boolean/metered cells, staged toggle→"Unsaved"+"Publish N changes", publish→per-cell PATCH+note, discard→revert, metered non-integer validation, load + per-cell save errors); `app/access-control/page.test.tsx` (table + role badge/adder, empty state, add→POST+validation, role toggle→PATCH, remove→DELETE behind confirm + cancel, **self-lockout** = server rejection surfaced as error badge, load error); `app/users/page.test.tsx` (list row + plan fallback + Manage link + empty; detail render/stats, role change→PATCH `/role`+save-disabled-until-dirty, **data deletion** request→POST + execute→DELETE→`router.push("/users")` two-step confirm, load error). Harness: added `useParams` to the `next/navigation` mock + `setMockParams` (user detail uses `useParams`). **LEARNINGS #20:** these data pages re-load 3× on mount (the harness sets `currentUser` before render, so the page's first effect already has a token → load on mount, then again when the admin-session resolves the role; each load re-seeds editable drafts, wiping a staged edit), so staged-edit tests gate interactions on *quiescence* — load count stable across two consecutive `waitFor` polls — rather than the fragile fixed-count gate. Stable over 6 runs.)

### M15.2.6 — Concierge review queue tests: two-pane layout, queue list filtering (Open/Mine/Done), verdict selection, refined answer submit, escalate action

**DONE** (`apps/admin/app/concierge-reviews/page.test.tsx`, +12 → admin 83: `.review-pane` two-pane layout [queue list + detail], auto-select-first + detail load [question + AI answer scoped to `.review-answer` since the answer also pre-fills the refined textarea], Open/Mine/Done triage filter [requested/in_review vs answered vs `claimedAt`-set + per-tab empty copy], verdict-card selection [`aria-pressed`], verdict-only `respond` [`revisedAnswer:null` when unchanged + queue reload distinguished by the page load's `offset=` param vs the AdminFrame nav-count fetch], edited→"Push refined update" CTA + `revisedAnswer` sent, `escalate` POST, admin expert-picker gate [select-prompt until an expert is chosen, then `?expertId=` queue load — wait for the roster option before changing the combobox], load-error path. Mostly rendered as `role:"expert"` [API voice-scopes the expert so the queue loads without the admin picker]. `waitFor`-wrapped per LEARNINGS #19.)

### M15.2.7 — i18n tests: admin locale provider, `useStatusLabel` hook with all 43 status tokens, dictionary lockstep verification (EN/VI key parity across all 24 namespaces)

**DONE** (`apps/admin/src/lib/i18n/i18n.test.tsx`, +13 → admin 96: admin `LocaleProvider` (default EN + `{greeting}/{name}` interpolation, missing-key→dotted-token fallback, EN→VI switch persisting to localStorage `expertos:admin-locale` + `PATCH /me/locale`, localStorage-pref-over-profile resolution order, profile seed); `useStatusLabel` over the full 43-token `common.status.*` catalog in EN **and** VI + unknown-token→humanized fallback; dictionary lockstep (identical top-level namespace set [23 namespaces, >20 sanity floor], identical leaf-key set, every leaf non-empty in both locales, `{placeholder}` parity per key). **Bug found+fixed (LEARNINGS #21):** `useStatusLabel`'s miss-fallback compared the namespaced translator's return against the un-prefixed sub-key, so it never humanized — an unknown lifecycle status leaked the raw `common.status.<token>` to the UI; fixed to compare against the `common.`-prefixed path the translator actually returns.)

### M15.3.1 — Admin i18n E2E: toggle admin locale EN→VI, verify nav labels + page headers + status badges switch language

**DONE** (`e2e/specs/admin-i18n.spec.ts`: EN baseline → `.seg` VI toggle → nav group ("Cổng chuyên gia") + role badge ("Chế độ quản trị") + admin-only link + `<html lang=vi>` + cross-nav persistence to the `/knowledge` VI header ("Duyệt kiến thức"); failure-safe `afterEach` resets the admin profile locale to EN so it can't pollute later admin specs. Validated live: 20 pass / 3 fixme-skip.)

### M15.3.2 — Access control E2E: add email to whitelist, verify it appears in table, toggle role, remove email, verify access-denied screen for non-whitelisted user

**DONE** (`e2e/specs/access-control.spec.ts`: admin adds a throwaway email as expert → row appears + "Added …" notice → "Make admin" re-role → "Removed …" via the window.confirm dialog → row gone; second test signs a non-whitelisted identity (`users.other`) directly into the portal → Access Denied screen (M14 `POST /me/admin-session` 403). `global-setup.ts` now seeds `e2e-admin@`/`e2e-expert@` into `allowed_emails` — without it every portal spec hit Access Denied.)

### M15.3.3 — Concierge review E2E: open review queue, select item, submit verdict with edit, verify delivery

**DONE (sandbox deliverable; host run pending).** `e2e/specs/concierge-review.spec.ts` drives the M13.6 two-pane queue: signs in as the **expert** (API voice-scopes the queue → no admin expert-picker), opens the seeded case by its unique `"E2E concierge fixture answer"` marker, asserts the question + the AI answer the user saw, selects the **Good** verdict (`aria-pressed`), edits the refined answer so the primary action flips to **Push refined update**, submits, and asserts the case leaves the **Open** tab and reappears under **Done** (verdict recorded + request resolved); a second test walks the Open/Mine/Done triage `aria-selected` filter. Fixture seeded in `requested` for the e2e expert by `global-setup.ts` (conversation wiped+recreated each run → single open item). **Verified in-sandbox:** every selector + EN dictionary label the spec asserts confirmed against the current `apps/admin/app/concierge-reviews/page.tsx` DOM (`.queue-item`/`.review-detail`/`.review-answer`/`.review-section`, `role="tab"`+`aria-selected`, `verdict-card`+`aria-pressed`, `pushRefined`="Push refined update", `<h1>`="Review queue"); e2e `tsc`+`eslint` clean. Per §3.4.1 the host runs the live Playwright stack — sandbox can't run it.

### M15.3.4 — Knowledge approval E2E: navigate kanban board, filter by status, approve a document from Expert Review → Published

**DONE (sandbox deliverable; host run pending).** `e2e/specs/knowledge-approval.spec.ts` drives the M13.3 kanban round-trip: signs in as **admin** (satisfies the `@Roles("expert")` board via the role hierarchy), asserts the 4 status columns, scopes to the seeded `"E2E Expert-Review Note"` card in **Expert Review**, clicks **Approve & publish**, and asserts the card moves to the **Published** (last) column with the approve action gone; a second test exercises the numbered step-pipeline filter (narrow to one stage, re-widen). Fixture seeded in `expert_review` (document + latest version, reset every run) by `global-setup.ts`. **Verified in-sandbox:** selectors + EN labels confirmed against `apps/admin/app/knowledge/page.tsx` (`.kanban-col`×4, `.kanban-card`, `card.approve`="Approve & publish", `<h1>`="Knowledge approval", step `<button>` "Draft"/"Published" — no collision with the conv→knowledge `<Link>` "Draft"); e2e `tsc`+`eslint` clean. Per §3.4.1 the host runs the live Playwright stack — sandbox can't run it.

### M15.3.5 — Resolve existing `test.fixme` legs: full publish→retrieval round-trip (seed prerequisite), deletion cascade, Stripe checkout page (external surface) — unblock or document why each remains skipped

**DONE.** **Deletion cascade UNBLOCKED** as a live spec: `global-setup.ts` seeds a dedicated DB-only throwaway user (`e2e-deletable@…`) *with* an owned conversation (re-created every run, idempotent upsert on a fixed `firebase_uid`), and `data-deletion.spec.ts` drives the admin roster → search → Manage → "Delete data…" → "Confirm delete" → asserts the redirect + the row gone (a successful cascade — an FK-blocking owned row would surface the error badge instead; the pre-delete Conversations stat guards against a vacuous delete). The other two stay **documented fixme**: publish→retrieval needs real chunks+embeddings (the parse→chunk→embed ingest pipeline, seeded out-of-band — a hand-seeded row can't be retrieved deterministically; the approve→publish leg itself is already covered by M15.3.4), and Stripe-hosted checkout is an external surface the suite shouldn't drive. Static gates green (e2e `tsc`+`eslint`, root `knip`); per §3.4.1 the host runs the live stack — this drops the fixme count from 3 → 2.

### M15.3.6 — Web i18n E2E: toggle web locale EN→VI, verify chat UI labels + history page + account page switch language

**DONE** (`e2e/specs/web-i18n.spec.ts`: chat language toggle EN→VI → chat empty-state ("Bắt đầu cuộc trò chuyện mới") + placeholder + `<html lang=vi>`, then the persisted locale carries to `/account` ("Gói & mức dùng") and `/history` ("Lịch sử"); failure-safe `afterEach` resets the member profile locale to EN.)

### M15.3.7 — E2E negative-space expansion: regression-guard the recent Security/Product fixes and the untested failure/permission/isolation paths the happy-path suite missed

**DONE (sandbox/static deliverable; host run pending).** +8 tests (6 new spec files + 2 added tests), suite now collects 36 tests / 18 files (e2e `tsc`+`eslint`+collection clean): `web-isolation.spec.ts` (cross-user RLS via the two-context member/`other` scenario — the previously-unused `e2e-other@` identity); `web-citations.spec.ts` (citation trust *invariant* — grounded-with-sources XOR "Limited knowledge", never an uncited authoritative answer; guards DIRECTIVE #45/LEARNINGS #29); `web-entitlements.spec.ts` (Free-plan `document_upload` → 402 `.badge-red`; guards DIRECTIVE #44); `concierge-disclosure.spec.ts` (consumer OD#5 "Reviewed & refined by our team" badge in `/history`); `conversation-knowledge.spec.ts` (M8.2 conversation-sourced KnowledgeDraft read surface — no UI promote action exists); `voice-publish.spec.ts` (M2.3/M13.5 expert Approve→publish from `expert_review`); `access-control.spec.ts` +3rd test (whitelist *revocation* revokes portal access — guards DIRECTIVE #42, with a cleanup safety-net); `web-i18n.spec.ts` +2nd test (locale *persists* across reload + fresh nav). 3 idempotent fixtures added to `global-setup.ts` (refined-answer conversation, conversation-sourced draft, `expert_review` voice profile), all inside the existing admin-RLS-context transaction. Pre-flight selector extraction corrected 6 wrong DOM assumptions before writing. Host-run via `scripts/test-e2e-{users,admin}.sh` (gitignored local-only; folder tracked via `.gitkeep`). Per §3.4.1 the host runs the live Playwright stack — sandbox can't. LEARNINGS #32 / DIRECTIVES #48 capture the `NEXT_PUBLIC` build-time E2E gotcha surfaced enabling the host run.

### M16.1 — **Schema + encryption helper.**

**DONE.** `Expert` gained `tidycalApiTokenEnc`, `tidycalApiTokenLast4` (non-sensitive UI hint so reads never decrypt), `tidycalLink`, `tidycalPolledAt` (poll watermark); `Consultation` gained `expertId` (uuid?, FK→`Expert` `onDelete: SetNull`, `@@index([tenantId, expertId])`). New `apps/api/src/common/secret-crypto.ts` — AES-256-GCM `encryptSecret`/`decryptSecret` over `CREDENTIALS_ENCRYPTION_KEY` (base64 32-byte), wire format `ivB64:authTagB64:ciphertextB64`, hard-fail on missing/short key + `last4`/`isCredentialsKeyConfigured` helpers. Hand-written migration `20260605120000_per_expert_tidycal` (per the convention that avoids spurious pgvector-index drops); `prisma generate` run. 19 crypto unit tests (roundtrip, fresh-IV, tamper→throw, wrong-key→throw, malformed/bad-IV, key validation). No webhook-secret/token columns.

### M16.2 — **Per-expert provider factory + booking service.**

**DONE.** Deleted the `tidycal.defaults.ts` / `tidycal.tokens.ts` singleton; new `TidyCalProviderFactory` (`forExpert(expert)` decrypts the row's token → `HttpTidyCalProvider({ apiToken })`; env-global-token fallback; offline fallback) wired in `consultation.module.ts`. Kept Prisma-free (caller passes the loaded row) so precedence is pure + unit-tested. `HttpTidyCalProvider` lost `webhookSecret`/HMAC entirely — `verifyWebhook` now rejects (TidyCal posts none), `parseEvent` is a no-op; only the offline provider keeps the JSON-envelope seam. `BookingService` injects the factory; the gate keys on the API token. 9 factory tests (precedence + no-key-leak on decrypt failure).

### M16.3 — **Per-expert poll.**

**DONE.** `BookingService.reconcile({ since?, expertId? })` resolves targets (experts with a token, or one by id; env-global fallback poll when none configured), polls each via its provider, syncs through `applyBookingEvent`, and advances `tidycalPolledAt`; the poll `since` = explicit → expert watermark → 24h lookback. Synthetic ledger ids are namespaced per expert (`expert:<id>:reconcile:…`) so two calendars can't collide; the admin `POST /consultation-bookings/reconcile` is the Cloud-Scheduler trigger (cadence ~5–15 min; `@nestjs/schedule` is the in-process alternative, not added). The dead per-expert/`:token` webhook route was never built; the offline-envelope route stays as the test seam and is **disabled in production** (defense-in-depth — an unsigned envelope can't be trusted if prod is misconfigured). 8 reconcile tests (watermark, namespacing, per-expert pending-scope + attribution, env fallback, no-op).

### M16.4 — **Expert self-service + admin settings API.**

**DONE.** `@Roles("expert")` `GET/PATCH /expert/calendar-settings` (scoped to the caller's own `Expert` via `resolveExpert` — a non-admin's `?expertId=` is ignored, so no cross-expert access); admin `GET/PATCH /admin/experts/:id/calendar` on `AdminExpertService` (audited in-tx, field-names-only metadata). Shared `apps/api/src/expert/calendar-settings.util.ts` (`buildCalendarUpdate` encrypts; `toCalendarSettingsDto` returns `{ apiTokenConfigured, apiTokenLast4, tidycalLink }` — never the ciphertext/plaintext). Shared zod `expertCalendarSettingsUpdateSchema` + DTOs in `packages/shared`. Service + util tests assert the token is never echoed and never appears in logs/audit.

### M16.5 — **Expert Portal UI (`apps/admin`).**

**DONE.** "Calendar / Booking" `CalendarEditor` card on `apps/admin/app/experts/[id]/page.tsx` (between `ProfileEditor` and voice profiles): write-only password token input + "Configured ✓ ••••last4" + clear-token, optional booking link, inline "How to get your TidyCal API token" helper (Integrations → Automations). Role-aware via `useAuth` — admin hits `/admin/experts/:id/calendar`, a non-admin expert hits self-scoped `/expert/calendar-settings`. 4 typed admin-client fns; EN/VI `experts.detail.calendar.*` dict keys in lockstep. admin `typecheck`/`lint`/`jest` (101) green.

### M16.6 — **Default = Ngô Công Trường + back-compat.**

**DONE.** Idempotent backfill CLI `apps/api/src/consultation/backfill-tidycal.cli.ts` (`npm run backfill-tidycal [-- --commit] [--name=…]`): encrypts the legacy global `TIDYCAL_API_TOKEN` onto the primary expert (`displayName = "Ngô Công Trường"`, the exact chat default in `apps/web/app/chat/page.tsx`); no-ops when the token is unset / the expert already has one / the expert is absent; requires `CREDENTIALS_ENCRYPTION_KEY`. Env-global token fallback stays in the factory/poll until all experts migrate; `TIDYCAL_WEBHOOK_SECRET` is now vestigial and can be retired. Added to the knip api entry list.

### M16.7 — **Security + tests.**

**DONE.** Security review confirmed: token encrypted at rest (AES-256-GCM, env/Secret-Manager key), **never** returned (only `apiTokenConfigured` + `last4`) or logged (field-names-only), per-expert authz (self-scope ignores `?expertId=`; admin route is `@Roles("admin")` + audited), poll runs only in the system RLS context, the token is decrypted **only** when building a provider. Hardening added: the unsigned offline webhook seam is refused in production. Full gates green — api 770 tests / 99.85% lines (≥95% gate), api+shared `typecheck`/`lint`, shared 193, admin 101, root `knip` clean (no new findings). **Host run pending:** an offline-E2E leg driving the per-expert sync end-to-end is the remaining item (the live Playwright stack runs on the host per §3.4.1, not in this sandbox).

### Security Cycle 4 + Product Cycle 2 remediation — bulk-publish review-gate bypass + TidyCal decrypt fallback

**DONE (in-sandbox).** Cleared all three findings shared by the two FAIL verdicts.

- **Critical (review-gate bypass).** Deleted the `publish-drafts` CLI + script (it published `draft` versions directly, skipping AI-processing/expert-review). Extracted the publish side effects into one shared primitive `apps/api/src/knowledge/publish-version.ts` `publishReviewedVersionTx` that **enforces the `expert_review` gate itself** (`NotReviewedError` otherwise); both `KnowledgeService.approve` and the new `bulk-publish` CLI route through it (no drift). New `bulk-publish.cli.ts` only selects `expert_review` docs; commit requires env `KNOWLEDGE_BULK_PUBLISH=1` **and** a validated `--approver=<userId>` (admin/expert), writing a `knowledge.bulk_published` `admin_audit_log` row per publish. `VERSION_SELECT`/`VersionRow` moved to `knowledge.constants.ts` (avoids service↔primitive circular import).
- **High (TidyCal decrypt fallback).** `TidyCalProviderFactory.forExpert` now returns `TidyCalProvider | null`; a configured-but-undecryptable token returns `null` (skip) instead of `default()`, so the env-global calendar is never polled under the failing expert's id. `BookingService.resolveTargets` filters nulls + counts `failedTargets`; `reconcile` surfaces the new `BookingReconcileResultDto.failedTargets` + a `logger.warn`; admin reconcile page shows the stat + amber hint (EN/VI). No-token experts still use the env/offline default (permitted).
- **Medium (stale cache).** The CLI clears persistent `semantic_cache` rows per affected tenant after commit (in-process LRU lives in the API process — TTL/restart, documented in CLI output).
- **Gotchas:** on THIS Linux host the Prisma **library** engine works for unit tests (mocked tx); the `PRISMA_CLIENT_ENGINE_TYPE=binary` prefix from the CLI headers errors here ("Invalid client engine type") — that hint is for a different sandbox. Also fixed two pre-existing `knip` misses the last commit introduced (the now-deleted CLI was never added as an entry; `e2e/global-teardown.ts` was unlisted).
- **Tests/gates:** +8 api (publish-version gate ×6, booking decrypt-skip regression, factory rewrite) → api 779 pass; shared 193, admin 101; api/admin/shared typecheck+lint, root knip + lint:css green.


### M17.1 — Settings persistence (schema + shared types)

**DONE.** Foundation for the runtime answer-tuning settings (M17).

- **Schema.** `AppSettings` Prisma model (`packages/db/prisma/schema.prisma`): global singleton mirroring `ReviewConfig` — `llmTemperature Float=0.2`, `defaultChatModel String="gpt-4o-mini"`, `retrievalScoreFloor Float=0`, `+createdAt/updatedAt`, mapped `app_settings`, **no RLS** (admin-guarded at the controller layer, M17.2). The embedding provider is intentionally NOT a column (env+restart only — switching embedders invalidates existing vectors).
- **Migration.** Hand-written `20260605130000_app_settings/migration.sql` (per DIRECTIVES #35 — never apply a Prisma-generated migration verbatim): `CREATE TABLE app_settings` + explicit `GRANT … TO app_user` (the app connects as the non-superuser RLS role). Followed the `review_config` migration shape exactly.
- **Shared types.** `packages/shared/src/app-settings.ts`: `CHAT_MODELS` allowlist `["gpt-4o-mini","gpt-4o"]` (every choice guaranteed a `model-pricing.ts` entry) + `chatModelSchema`; `appSettingsUpdateSchema` (temp ∈ [0,2], floor ∈ [0,1], model ∈ allowlist); `AppSettingsUpdateInput` + `AppSettingsDto` (the DTO carries a read-only `embeddingProvider` string + `updatedAt`). Exported from `index.ts`.
- **Gotchas.** `defaultChatModel` is `String` in the schema (allowlist enforced in the zod/service layer, not a DB enum) — matches how `recommendation_rules`/config tables keep leaf unions out of the DB. No seed row needed: the M17.2 `SettingsService` upserts on first read (clones `ConciergeConfigService`).
- **Tests/gates.** +9 shared tests (`app-settings.test.ts`: allowlist parity, temp/floor bounds, off-allowlist rejection) → shared 202 pass. shared build+typecheck+lint, db typecheck+lint, api typecheck, root knip green.

### M17.2 — SettingsService + controller (runtime answer-tuning settings API)

**DONE.** The admin read/write API over the `app_settings` singleton + a hot-path snapshot cache for M17.3/M17.4.

- **Service.** `apps/api/src/settings/settings.service.ts` — clones `ConciergeConfigService` exactly: `getSettings(user)` (RLS-scoped read → `AppSettingsDto`, launch defaults when unseeded), `updateSettings(actor, input)` (upsert the singleton + `AdminAuditService.record` in the **same tx**, action `app_settings.updated`, then bust the cache). Adds `getCached()` for the answer path — a 30s in-process TTL snapshot of the tunable triple (`llmTemperature`, `defaultChatModel`, `retrievalScoreFloor`) read through the **global `PrismaClient`** (the row has no RLS, so no per-user context), so a Save is live on the next message with **no restart and no per-message DB hit**. `updateSettings` sets `this.cached = null` so the change is visible immediately.
- **Controller.** `app-settings.controller.ts` — `@Controller("admin/app-settings")` `@Roles("admin")`, `GET`/`PATCH`, body validated by the shared `appSettingsUpdateSchema` via `ZodValidationPipe`. Branchy logic stays in the service (the coverage gate collects `*.service.ts`).
- **Module.** `settings.module.ts` — imports `AuthModule` (RlsService + guards) + `AdminModule` (AdminAuditService), declares the controller, provides `SettingsService` + the read-only embedding-provider name (`settings.tokens.ts` `resolveEmbeddingProviderName` → `EMBEDDING_PROVIDER==="openai" ? "openai" : "hashing"`, mirroring the M17.6 gate), **exports `SettingsService`**. Imported by `ChatModule` (M17.3) + `RetrievalModule` (M17.4) + registered in `app.module.ts`.
- **Key decisions.** (1) `getCached` reads via the global client, not `RlsService.run` — `app_settings` is RLS-exempt config and the hot path has no `AuthUser`; a transaction per message would be wasteful. (2) `normalizeModel` defensively narrows a stored `defaultChatModel` string to the `CHAT_MODELS` allowlist (falls back to `gpt-4o-mini`) so a hand-edited/legacy row can't leak an unpriced model onto the answer path — the write schema already guards new saves. (3) `AppSettingsRuntime` left **un-exported** (knip) until M17.3/M17.4 consume it; `getCached`'s public return type still resolves structurally.
- **Gotchas.** Turbo `pnpm typecheck`/root scripts SIGILL in this sandbox (LEARNINGS #25) — ran per-workspace (`pnpm --filter @expertos/api exec tsc --noEmit`). No DI-boot/e2e test in-sandbox; wiring confidence comes from typecheck + mirroring the proven concierge pattern (same `AdminModule`/`AuthModule` import shape, no new cycle).
- **Tests/gates.** +9 api (`settings.service.test.ts`: get/update upsert+audit, defaults, off-allowlist fallback, cache hit within TTL, re-read after 30s TTL via `Date.now` spy, bust-on-update, unseeded defaults) → **api 788 pass**, settings.service.ts 100% cov. api typecheck+lint, root knip clean.

### M17.3 — Thread temperature + model through the LLM call

**DONE.** The standard-tier answer path now applies the admin-tunable temperature + default model in real time (no restart), via a per-call override threaded into the single LLM call.

- **`LlmCallOptions` (packages/ai/src/providers.ts).** New `{ temperature?, model? }` — optional per-request overrides sourced from the runtime settings. Added a trailing `options?: LlmCallOptions` param to `LlmProvider.complete` + `completeStream`. Exported from `index.ts`.
- **Drivers honor it.** `StreamingLlmProvider.complete`/`completeStream` (`http.ts`) pass options through. OpenAI + Anthropic set `model: options?.model ?? this.name` and add `temperature` to the body only when provided (omitted = provider default — never sends `temperature: undefined`). Gemini carries the model in the URL path, so it was restructured to store `baseUrl` (not a precomputed `url`) and rebuild the URL per-call from `options?.model ?? this.name`; temperature goes in `generationConfig`. Echo accepts (ignores) the options — it is deterministic, so overrides have no effect on output (params prefixed `_` per eslint `argsIgnorePattern`).
- **Chat call site (`chat.service.ts`).** Standard tier reads `settings.getCached()` once and builds `callOptions = { temperature, model }`; the **degraded/fair-use tier is deliberately untouched** (`callOptions` stays `undefined`, stays on its own cheap provider — M6.3). `effectiveModel = callOptions?.model ?? llm.name` replaces every `llm.name` *model-identity* use — answer-cache key, `lookupAnswer` model filter, persisted turn model, usage/cost log, and the new `model` field on the "chat answer completed" log line — so the recorded/cached/priced model matches what actually ran. `getCached()` is the M17.2 seam; `gpt-4o` is already priced PREMIUM in `model-pricing.ts`.
- **Decisions.** (1) Effective-model threading is the load-bearing part: if the override only changed the request body but not the cache key, a `gpt-4o` answer could be served from a `gpt-4o-mini`-keyed cache entry and vice versa. (2) Temperature is omitted from the body when unset rather than defaulting to a number in the driver — keeps each provider's own default authoritative and the request minimal. (3) The cache-hit path also calls `getCached()` (before lookup) — cheap (30s TTL snapshot), and the hit's own `hit.model` is used for its persistence, so no behavior change there.
- **Tests/gates.** +6 ai (OpenAI/Anthropic/Gemini each: override threads model+temperature into body/URL, provider `name` unchanged; + no-override omits temperature) → **ai 201**. +2 api (chat: standard tier threads `{temperature,model}` + effective model flows to cache key/persist/usage/log; degraded tier reads no settings + keeps its own model) → **api 790**. The chat test harness adds a `SettingsService` stub whose `getCached` defaults `defaultChatModel` to the standard provider's own name so existing model-identity assertions stay valid. ai+api typecheck+lint, ai 201 + api 790 tests, root knip green. (Turbo root scripts SIGILL in-sandbox per LEARNINGS #25 — ran per-workspace.)

### M17.4 — Retrieval score floor

**DONE.** The admin-tunable retrieval relevance floor (`app_settings.retrievalScoreFloor`) now drops weak chunks before they reach the model, applied in real time (30s settings snapshot, no restart).

- **`RetrievalRequest.minScore?` (packages/ai/src/retrieval/types.ts).** New optional field: the minimum **fused RRF** score a chunk must reach to survive. Documented units — it is the small-magnitude RRF score (~0.016/top rank from `fuseHybrid`), **not** a 0–1 cosine; `<= 0`/omitted = off (the default). Rebuilt `packages/ai` so `apps/*` consume the new `dist/` type.
- **Filter after fusion (`pgvector.store.ts`).** `retrieve()` filters the `fuseHybrid` output by `chunk.score >= floor` only when `minScore > 0`. Correctness note: `fuseHybrid` already returns the top-K sorted by score desc, so filtering that slice is equivalent to filter-then-slice — no chunk above the floor is lost. A non-positive/`null` floor is a no-op.
- **`RetrievalService` reads the setting.** Injected `SettingsService` (already exported by `SettingsModule`, already imported by `RetrievalModule` from M17.2 — no new wiring). `retrieve()` reads `getCached().retrievalScoreFloor` once, threads it as `request.minScore`, **and** forks the retrieval cache key on it.
- **Cache-key fork (`response-cache.service.ts`).** `retrievalKey(tenantId, query, minScore = 0)` adds the floor as a key segment (directive #43 — a result-shaping input must fork every cache key it influences), so lowering/raising the floor never serves chunks filtered under the old floor. The default `0` keeps existing 2-arg callers' keys byte-stable (the cache tests still pass unchanged).
- **Scope decision (deliberate).** Kept strictly at the retrieval layer — `chat.service` / the **answer** cache key are untouched. The M17.3 test asserts the degraded tier never reads settings ("degraded tier untouched"), and the answer-cache floor staleness for *cacheable standalone* turns is the same bounded class already accepted for temperature (TTL-bounded, publish-busted via `invalidateTenant`). The trust invariant is unaffected: a stale cached answer still cites only real published chunks — the floor only widens/narrows which weak chunks are admitted, never fabricates a citation. The retrieval-key fork still delivers real-time floor for every non-cacheable turn (history/uploads present) and for cacheable cold misses.
- **Tests/gates.** +3 api retrieval-service (reads floor + forks key on it; drops sub-floor fused chunks; off-by-default keeps all), +2 api pgvector-store (drops below floor / keeps at-or-above; ignores non-positive floor), +1 api cache (`retrievalKey` forks on floor, default 0 == omitted) → **api 796**. ai+api typecheck+lint, ai 201 + api 796 tests, root knip green. (Turbo root scripts SIGILL in-sandbox per LEARNINGS #25 — ran per-workspace.)

### M17.5 — Admin Settings page

- **New page `apps/admin/app/settings/page.tsx`.** Cloned `concierge/page.tsx` exactly (load → `SettingsEditor` form → Save). Three editable fields: `llmTemperature` (`Input type=number` 0–2 step .05), `defaultChatModel` (`Select` gpt-4o-mini | gpt-4o), `retrievalScoreFloor` (`Input type=number` 0–1 step .005 + RRF helper text). `embeddingProvider` rendered **read-only/disabled** with a "restart required — `EMBEDDING_PROVIDER` env" note (M17 baked decision: env+restart, not a live toggle). Inline validation reuses a `parseBounded(raw, min, max)` helper (mirrors concierge's `parseThreshold`); model is validated through `chatModelSchema.parse` on change so the union type is sound.
- **Directive #38 (label association).** Unlike the concierge clone, every `Field` passes `htmlFor` + a matching control `id` (`settings-temperature` / `settings-model` / `settings-score-floor` / `settings-embedding-provider`) — the DS `Field` renders the `<label>` separately, so association is explicit, not implicit.
- **Client (`admin-client.ts`).** `getAppSettings` (`GET /admin/app-settings`) + `updateAppSettings` (`PATCH`) added under a new "M17 — runtime answer-tuning settings" section; imported `AppSettingsDto`/`AppSettingsUpdateInput` from `@expertos/shared` (types already existed from M17.1). API surface (controller/service) already shipped in M17.2 — no backend change.
- **i18n.** New `dictionaries/settings.ts` (EN+VI), registered in `dictionaries.ts` (`settings` namespace, alpha-ordered between `revenue` and `users`). The existing i18n lockstep test (`i18n.test.tsx`) iterates all namespaces, so EN/VI key parity for `settings` is auto-covered — no new test needed. Nav label `nav.settings` added to `common.ts` (EN "Answer settings" / VI "Cài đặt câu trả lời").
- **Nav (`AdminFrame.tsx`).** Added `{ href: "/settings", labelKey: "nav.settings", group: "SYSTEM", role: "admin" }` after concierge config. SYSTEM group, admin-only — experts don't see it. The breadcrumb derives from the nav labelKey, so it resolves automatically.
- **Gotcha — flaky test run.** First `pnpm --filter @expertos/admin test` showed 5 jsdom timeouts (5000ms) from parallel cold-start contention; a clean re-run is all-green (no source cause — `AdminFrame.test`/`i18n.test` pass). The added nav item does not break AdminFrame's role-filtering test.
- **Gates.** admin tsc + `next lint` (0 warnings) + jest green; root knip + `lint:css` clean. No CSS added — page uses only existing ds.css classes (`pagehead`/`eyebrow`/`h1`/`muted`/`col`/`gap2`/`row`/`gap1`).

### M17.6 — OpenAI embedding provider + re-embed CLI

**DONE.** The real semantic embedder (`text-embedding-3-small`, 1536-dim) is now an opt-in production driver, and an operator CLI re-embeds every existing chunk so the env cutover is coherent.

- **New driver `packages/ai/src/embedding/openai-embedding-provider.ts`.** `OpenAiEmbeddingProvider implements EmbeddingProvider`. `POST /v1/embeddings` with `{ model, input: batch, dimensions }`. Reuses the LLM layer's `defaultFetch`/`LlmRequestError`/`FetchLike` from `./llm/http` (PRD direction — no new HTTP plumbing). `name = "text-embedding-3-small"` (so usage logs price correctly — already in `model-pricing.ts`), `dimensions = 1536` (matches the `chunks.embedding vector(1536)` column + HNSW index). Pure given an injected `fetch` (network/key/gating live in the factory). Exported (+ `OpenAiEmbeddingConfig`) from `index.ts`.
- **Order + batch guarantees.** Batches inputs at `MAX_BATCH=256` per request; reassembles by each response item's `index` (the API may reorder within a batch) and concatenates batches in input order, so the `EmbeddingProvider` contract (index-aligned, same length) holds — a caller storing vectors against chunks positionally is never handed a transposed vector. Validates count==input, per-item index in range, and per-vector dim==1536; any mismatch throws `LlmRequestError("openai-embedding", …)`. Empty input → `[]` with no request.
- **Gate (`apps/api/src/ingestion/ingestion.defaults.ts`).** `createDefaultEmbeddingProvider(env = process.env)` now returns the OpenAI driver when `EMBEDDING_PROVIDER=openai` **and** `OPENAI_API_KEY` is set; otherwise the dev `HashingEmbeddingProvider`. Set-but-keyless **throws** (fail loud) rather than silently writing hashing vectors into what is meant to be the OpenAI space. Single swap point shared by ingestion DI + retrieval + voice/concierge matching + the re-embed CLI (one vector space). Back-compat: the factory is still a zero-arg `useFactory` for every NestJS module — env defaults to `process.env`. This matches the gate `resolveEmbeddingProviderName` (settings.tokens.ts) already surfaces read-only in the admin Settings UI (M17.5).
- **Re-embed CLI `apps/api/src/ingestion/reembed.cli.ts`** (mirrors `bulk-publish.cli.ts`: dry-run default, `--commit` to write). Resolves the **active** provider via the same factory (set the cutover env, run it, get OpenAI vectors). Snapshots every chunk's `id`+`content` under a single admin RLS context — `USING app.is_admin()` spans all tenants — re-embeds the stored `content` (exactly the text `IngestionService` embeds, so re-embed and fresh-ingest share a vector space), and `UPDATE chunks SET embedding = $1::vector` (via `toVectorLiteral`) in per-batch transactions. Flags: `--batch=N` (clamped 1..256), `--limit=N` (smoke test). Prints a warning when the active provider is the offline `hashing-dev`. `"reembed"` npm script + `knip.json` api entry added. CLI is coverage-exempt (api `collectCoverageFrom` is `*.service.ts` only).
- **Cutover (M17.7, next).** ship (default stays hashing) → set `EMBEDDING_PROVIDER=openai` + `OPENAI_API_KEY`, restart → `pnpm --filter @expertos/api reembed -- --commit`. During the ~1–2 min window vector search is degraded but keyword search keeps serving (hybrid retrieval, M1.2).
- **Tests/gates.** +9 ai (`openai-embedding-provider.test.ts`: name/dims, request shape, out-of-order reassembly, >256 batching with global order, empty input no-request, non-2xx → `LlmRequestError`, count mismatch, wrong-dim, keyless construction) → **ai 210**. +4 api (`ingestion.defaults.test.ts`: hashing by default, OpenAI when flag+key, throws flag-without-key, ignores unrelated provider value) → **api 800**. ai+api typecheck+lint+test, root knip + `lint:css` + all-workspace tsc green. (Turbo root scripts SIGILL in-sandbox per LEARNINGS #25 — ran per-workspace.)

### M17.7 — Embedding cutover: local validation + reembed CLI hardening

**PARTIAL (local-validated; prod deploy/run/verify pending).** Dry-ran the full M17 cutover procedure against the local stack end-to-end, fixed two real defects it surfaced, and left the prod deploy/verification (the genuinely external part) for the deploy operator.

- **Procedure validated locally.** Set `EMBEDDING_PROVIDER=openai` in `.env` (`OPENAI_API_KEY` already present; `.env` is gitignored). Dry-run (`pnpm --filter @expertos/api reembed`) reported the real provider — `Active embedding provider: text-embedding-3-small (1536 dims)`, 2676 chunks — i.e. the M17.6 gate flipped correctly (no `hashing-dev` warning). Full `reembed -- --commit` then rewrote **all 2676 chunks, 0 failed**, with the documented default `--batch=256`. Local DB is now coherent in the OpenAI vector space.
- **Bug #1 — reembed default batch blew the tx timeout.** The CLI wraps a whole batch of sequential `UPDATE`s in one Prisma **interactive transaction**; at the documented `--batch=256` this overran Prisma's default **5s** timeout, failing every full batch with `Transaction already closed: Could not perform operation` and committing only the small trailing batch (116) — leaving `chunks` in a **mixed vector space** (the worst outcome: a partial, silent half-cutover). Root-caused and fixed in `apps/api/src/ingestion/reembed.cli.ts`: pass `{ timeout: 30_000 }` to the write `$transaction` (`TX_TIMEOUT_MS`). Re-verified the previously-failing default 256-batch now completes 2676/2676 with 0 failed. Committed `ae7e234`, pushed to `main`; api typecheck+lint + **800 tests** still green. The documented one-liner cutover command now works as written.
- **Bug #2 — host-DB migration drift → admin Settings 500.** The admin "Answer settings" page (M17.5) showed INTERNAL SERVER ERROR; `GET /admin/app-settings` 500'd because the M17.1 `20260605130000_app_settings` migration (authored in-sandbox) had **never been applied to the host DB** — `getSettings → findFirst(appSettings)` hit a missing table. `prisma migrate status` confirmed the single unapplied migration; `prisma migrate deploy` applied it (table + the hand-added `app_user` GRANT verified present). Not a code bug — a deploy-ordering requirement: **the deploy pipeline must run `prisma migrate deploy` before traffic reaches the Settings page or the answer path**, or prod hits the same 500.
- **Engine note (host).** Build + reembed run on the darwin host with the **default (library) Prisma engine**; the `PRISMA_CLIENT_ENGINE_TYPE=binary` override is only for the Linux sandbox (LEARNINGS #25). Needed a host `nest build` first — the sandbox-built `dist/` is Linux-targeted.
- **Still pending (external deploy/run gate).** Deploy `main` (now carries `ae7e234`) → set `EMBEDDING_PROVIDER=openai` + `OPENAI_API_KEY` in prod + restart → run `pnpm --filter @expertos/api reembed -- --commit` against the prod DB → restart/await cache TTL → run the four M17.7 verifications (temp=0 in the request within 30s, usage log shows `gpt-4o`, score floor drops low-score chunks, a paraphrased query retrieves the right note). Only then does M17.7 flip to `[x]`.

### M18 — Uploaded document management ("My Knowledge")

**COMPLETE.** Read+delete management surface over the M5 `uploaded_files` a user already produces — `GET /uploads` + `DELETE /uploads/:id` API, an `apps/web` "My Knowledge" page, and a discoverable sidebar entry point. No change to the upload/parse/embed/retrieval/scoping logic. The functional core (M18.1–M18.3) shipped in commit `d6dad4a`; this pass added the M18.3.2 mockup design-parity polish + the M18.4.1 E2E and reconciled the manifest.

- **M18.1.1 — shared schema.** `uploadListQuerySchema = z.object({ scope: z.enum(["persistent","temporary","all"]).default("all") })` + `UploadListQuery` in `packages/shared/src/upload.ts`, exported from `index.ts`. **Reused `UploadedFileDto`** for list rows (no new DTO) — its shape already carries everything the manage view needs.
- **M18.2 — API (RLS-scoped, NOT entitlement-gated).** `UploadService.list(user, query)` runs `this.rls.run(user, tx => tx.uploadedFile.findMany(...))` newest-first, `take: 100`, `scope` filtering on `mode` (via `scopeToWhere`); `chunkCount` derived live from the `_count` of the `upload_chunks` relation so the searchable badge stays truthful after a retention sweep. `remove(user, id)` captures `gcsUri` then deletes the row inside `rls.run` (404 when a peer's/missing row reads back null — cross-user delete is naturally invisible, never a 403), chunks cascade via `ON DELETE CASCADE`, then best-effort `deleteStorageObjects` **after** commit, outside the tx (directive §46 — log-not-throw on blob failure). The inline post-upload mapper was extracted to a shared `toUploadedFileDto` + `UPLOADED_FILE_SELECT` so list/create never drift. `upload.controller.ts` `@Get()` + `@Delete(":id")` carry **`@Roles("user")` only — no `@RequiresEntitlement`** (a downgraded/over-quota user must still see + delete their saved data; only `POST` keeps the `document_upload` guard); delete uses `ParseUUIDPipe` + `@HttpCode(204)`. 49 upload tests green, `upload.service.ts` 100% lines.
- **M18.3 — Web.** `upload-client.ts` `listUploads(token, scope?)` / `deleteUpload(token, id)`. `apps/web/app/knowledge/page.tsx` ("My Knowledge"): fetches `scope=all` on mount, splits into a **Saved (persistent)** + **Temporary (expiring)** section; each row has the mode `Badge` (green/info), size, searchable-chunks `Badge` (green when indexed, amber `stored — not searchable yet` at 0), added date, and for temporary rows a relative `expires in N days` from `expiresAt` (one render-time `now`, `Number.isFinite` guard per directive §9). Per-row Delete has an inline confirm step (copy notes past saved answers keep their citation snapshot). Sidebar entry point = a `.navitem` `<a href="/knowledge">` in the chat sidebar footer (M18.3.3) — the actual "where did my remembered file go?" UX fix. New `knowledge` i18n namespace (EN+VI lockstep); the orphaned `chat.optionTemporary`/`optionPersistent` keys were pruned. **M18.3.2 design parity (mockup 02-my-knowledge):** wrapped the page in `.umain` > `.pagehead` (`.eyebrow` "Upload management" + `.h1`), each row now leads with a document `FileIcon`, the filename renders as `.h3`, the size as `.mono`, and Delete is a right-aligned ghost `Button` (the confirm block drops below the row). New `knowledge.eyebrow` key (EN/VI). 10 web jest suites / 112 tests green (incl. `knowledge/page.test.tsx`).
- **M18.4.1 — E2E.** `e2e/specs/web-my-knowledge.spec.ts`: sign in as member → chat → attach popover → persistent CSV upload (per-run-unique `e2e-my-knowledge-<ts>.csv`) → click the sidebar **My Knowledge** link → assert the file + a searchable-chunk count appear → Delete + confirm → assert the row is gone. Self-cleans (the test deletes its own upload) and is userId-scoped so the global teardown reclaims it if a leg fails early. tsc + eslint clean; host-run pending (sandbox cannot run live E2E, LEARNINGS #22).
- **Gates.** web typecheck + lint + 112 tests, e2e typecheck + lint, root `lint:css` + `knip` all green.
- **Gotcha.** The functional code predated this loop (committed in `d6dad4a` without flipping the manifest or writing notes); this pass verified each leg against the spec, finished the two genuinely-open pieces (3.2 design parity, 4.1 E2E), and reconciled PRD-TRACKING + BUILD-NOTES so the board reflects reality.

### M19.1.1 — history page design parity (mockup 01-history)
- **Page (`apps/web/app/history/page.tsx`).** Reworked the list view from one big `.card.card-pad` into the M19 consumer pattern: `.umain` > `.pagehead` (`.eyebrow` "Your conversations" + `.h1` "History") with a **right-aligned `.seg` tablist** (Conversations / Saved answers) that *switches* the body rather than stacking both. Conversations tab = full-width search row (`Input` flex:1 + primary Search `Button` + Clear) over a `.col gap2` list; Saved tab = the existing `SavedAnswers` panel. Conversation/search rows now render via a new `ConversationRow` = `<button class="card urow">` with a leading chat-bubble `ChatIcon`, the title (`.urow-title`, truncating), a trailing date (`.urow-meta` mono, `aria-hidden`) + `Chevron`. The button's accessible name is `aria-label={title}` only (date is decorative) so the existing `getByRole("button",{name:title})` tests/specs keep matching.
- **ds.css.** Added `.urow` / `.urow-title` / `.urow-meta` / `.urow-ic` (clickable full-width `.card` row: flex, padding, hover lift via `--ink-400` border + `--sh-md`, `font: inherit` to undo the button reset) in the user-app-shell section. All tokens, no hardcoded hex; off-scale px live in ds.css (stylelint green).
- **i18n.** New `history` keys `eyebrow` / `tabConversations` / `tabSaved` (EN+VI lockstep). Dropped the unused `Field` import (search row is now a bare `.row` so the head can host the seg). `.umain` isn't flex and margins are reset, so list sections are wrapped in `.col gap4`/`.col gap2` for vertical rhythm between sibling row cards.
- **Test churn (saved answers moved behind a tab).** Web: `app/history/page.test.tsx` "lists saved answers" now clicks the **Saved answers** `role="tab"` first. E2E: `web-history.spec.ts` (both legs) + `web-isolation.spec.ts` click the Saved-answers tab before using the panel's "Open conversation" jump. Headings ("History"/"Lịch sử") + "Recent conversations" + search-placeholder selectors unchanged, so the other history/i18n/concierge-disclosure specs still pass.
- **Gates.** web typecheck + lint + 112 jest, ui 251 jest (ds.css consumers), e2e tsc + eslint, root `lint:css` + `knip` all green. Host-run E2E pending (sandbox cannot run live E2E, LEARNINGS #22).

### M19.1.2 — account page design parity (mockup 03-account)
- **Identity header (`AccountIdentityHeader`, in `src/components/account-panel.tsx`).** The panel previously rendered content-only (host supplied a plain heading). Added the screenshot's identity block the panel lacked: `.avatar.avatar-lg` (initials + deterministic `avatarTone`, from `useAuth` display-name / email local-part) + an "Account" `.h2` + the signed-in email `.muted`. Returns `null` when signed out (the panel's sign-in badge stands alone). Reused the existing `account.modalTitle` ("Account" / "Tài khoản") key — **no new i18n key** (the planned `accountTitle` was only "if distinct from `heading`"; `modalTitle` already carries the exact string EN+VI). `avatarInitials`/`avatarTone` imported from `@expertos/ui`.
- **Section labels → `.eyebrow`.** The four in-card section labels (`usageThisPeriod`, `features`, `upgrade`, `billing`) changed from `.label` to `.eyebrow` (mono crimson uppercase) to match the screenshot's "USAGE THIS PERIOD" / "FEATURES" treatment. Meters/badges were already correct, untouched.
- **Modal `header` slot (`packages/ui/src/Modal.tsx`).** Added an optional `header?: ReactNode` rendered in the head's left slot in place of the plain `title` heading; `title` is still used for the dialog `aria-label` (accessibility preserved — the chat test's `getByRole("dialog",{name:"Account"})` keeps passing). Backward compatible (`header ?? (title ? <h2/> : <span/>)`). Rebuilt `@expertos/ui` so the emitted `.d.ts` exposes the prop to web/admin consumers.
- **Wiring.** `apps/web/app/chat/page.tsx` account `Modal` now passes `header={<AccountIdentityHeader/>}` → the avatar + Account + email sit on the modal head row beside the existing close ×, matching the screenshot. `apps/web/app/account/page.tsx` renders `<AccountIdentityHeader/>` in place of the old `<h1>{heading}</h1>` (the standalone route keeps its "← Back to chat" escape hatch). The `account.heading` key is now orphaned but left in the dictionary (harmless; i18n test asserts key parity/non-empty, not usage).
- **ds.css.** Added `.account-identity` (flex row: avatar + text column) + `.account-identity-text` (column, `gap: 2px`) + `.account-identity .modal-title { margin: 0 }` in the account-surface section. All tokens; off-scale px confined to ds.css (stylelint green).
- **Tests.** Modal `header` slot covered in `packages/ui/src/primitives.test.ts` (asserts the header node fills the left slot while `aria-label` stays `title`) → ui **252**. New account-page test "renders the identity header (avatar initials + Account title + email)"; the VI locale test re-pointed from the removed "Gói & mức dùng" heading to the identity title "Tài khoản" → web **113**. E2E selectors updated: `account-billing.spec.ts` + `web-i18n.spec.ts` now assert the "Account" / "Tài khoản" identity heading (host-run pending, LEARNINGS #22).
- **Gates.** ui/web/admin/e2e typecheck + lint + jest (ui 252, web 113, admin 101), root `lint:css` + `knip` all green.

### M19.4.2 — consultation funnel: stage attribution card (mockup 19-funnel)
- **Page (`apps/admin/app/funnel/page.tsx`).** Added the screenshot's centerpiece the page lacked — a "STAGE ATTRIBUTION" card of horizontal `Bar` rows, one per funnel stage. Each row's bar fill = that stage's share of the **top** stage (Conversations = 100%, NaN/∞-guarded via the `top > 0` check + the `Bar` clamp); the trailing `.funnel-row-value` (mono) shows `count · NN.N%` where the % is the conversion vs the **previous** stage (reusing the existing `rate()` helper, which renders an em-dash on a zero base). Stages top→bottom: Conversations (count only — no prior stage) → Recommendations (vs conversations) → Booked (`byResponse.book` vs recommendations) → Consultations (vs booked) → Revenue (`usd(bookedRevenueCents)`, value-only row, **no bar**). Card sits between the KPI `Stat` row and the byTrigger/byResponse/byStatus breakdowns, per the worked-template ordering.
- **Reused, not rebuilt.** The card reuses the dashboard's purpose-built `.funnel-card` / `.funnel-rows` / `.funnel-row-head` / `.funnel-row-label` / `.funnel-row-value` classes (M13.2.4, ink-fill `.bar > i`) → **zero new ds.css**. Header is `.eyebrow` "Stage attribution"; each `Bar` carries a composed `aria-label="{stage}: {value}"`.
- **Removed the redundant conversion-rate `Stat` row.** The two standalone rate cards (Conversation→recommendation, Recommendation→booked) are now encoded inline in the stage bars, so they were dropped to match the screenshot (KPI cards → stage card, no intermediate rate row). Their now-orphaned keys `rateConversationToRecommendation` / `rateRecommendationToBooked` were deleted from both EN+VI dicts (lockstep preserved).
- **Copy + i18n (`dictionaries/funnel.ts`).** Eyebrow updated "Analytics" → "M10.2 · Platform attribution" (EN) / "M10.2 · Phân bổ nền tảng" (VI) per the screenshot's house-style milestone eyebrow. New keys `stageAttribution` + `stage{Conversations,Recommendations,Booked,Consultations,Revenue}` (EN+VI lockstep).
- **Tests (`app/funnel/page.test.tsx`, new).** 4 cases: stage rows render with count + prior-stage conversion + value-only revenue row (no bar); KPI row + breakdowns retained; divide-by-zero (0 conversations) renders `0 · —` not `NaN%`; load-error badge. **Gotcha (LEARNINGS #19):** the page's mount-time reload fan-out (`setReport(null)`→`setReport(data)`, fired several times by the admin-session role resolution) **detaches** any DOM node captured by an early `findByText`, so a later synchronous `within(captured).getByText(...)` intermittently misses under parallel-run load. Fix: assert via `findByText` on the **unique composed stage strings** (`"412 · 4.9%"` etc.) which retry across re-renders, and only `closest('.funnel-card')` off a freshly-found node. Stable across repeated `--runInBand` runs.
- **Gates.** admin typecheck + lint + 105 jest (`--runInBand`; the parallel runner OOMs workers in-sandbox), root `lint:css` + `knip` all green. No new ds.css, no hardcoded hex/px.

### M19.4.1 — usage analytics page design parity (mockup 18-analytics)
- **Scope.** Verify+polish on the already-built `apps/admin/app/analytics/page.tsx` (`getUsageAnalytics` → `UsageAnalyticsDto`). The page already had the house recipe (`.pagehead` eyebrow+h1, window `<Select>`, 5-`Stat` row, byFeature/byModel/byDay `.h3` `Table`s); this loop only adjusted copy + cost-column alignment to match screenshot 18. No structural rebuild.
- **Copy + i18n (`dictionaries/analytics.ts`).** EN `eyebrow` "Analytics" → "AI cost & token usage", `title` "Usage & cost" → "Usage analytics"; VI `eyebrow` "Phân tích" → "Chi phí AI & sử dụng token", `title` "Mức sử dụng & chi phí" → "Phân tích sử dụng". No new keys (lockstep unchanged).
- **Right-aligned cost column.** Added a minimal reusable ds.css helper `.table th.num, .table td.num { text-align: right; }` (specificity 0,2,1 beats the default `.table th { text-align:left }`; no color/px so stylelint-safe) and applied `className="num"` to the COST `<th>`+`<td>` in all three tables. **Honest deviation from the task's "right-align numeric columns" (plural):** screenshot 18 right-aligns **only** the currency (COST) column — the EVENTS / PROMPT TOKENS / COMPLETION TOKENS / ACTIVE USERS columns stay left-aligned in the mockup — so I right-aligned the cost column across all three tables (the only right-aligned column in the screenshot) and left the integer columns as the mockup shows. Verification gate is the screenshot, which this matches.
- **Tests (`app/analytics/page.test.tsx`, new).** 4 cases: KPI row + three breakdown tables render; cost header+cell carry `.num` (3 headers, ≥2 `$486.10` cells); per-section empty-state note (3×); load-error badge. **Gotcha (LEARNINGS #19, same as M19.4.2):** the mount-time reload fan-out detaches nodes captured across an await boundary — `expect(await findByText(x)).toBeInTheDocument()` resolves then the node detaches before the matcher runs. Fix: settle on a late section (`"By day"`) first, then re-query each assertion with `findByText` (retries) and wrap the point-in-time `getAllBy*` cost-cell checks in `waitFor`. Stable under `--runInBand`.
- **Gates.** admin typecheck + lint, root `lint:css`, admin 109 jest (`--runInBand`; parallel runner OOMs workers in-sandbox → 40s worker timeouts, all green serially), ui 252 jest + build, root `knip` all green. No hardcoded hex/px.

### M19.2.5 — expert-detail page design parity (screenshot 08)
- **Scope.** `apps/admin/app/experts/[id]/page.tsx` (`getExpert`/`updateExpert`/`setExpertActive` → `AdminExpertDetailDto`; `CalendarEditor` untouched). Styling/alignment-only pass on the **real** expert-detail data — screenshot 08 actually renders a draft-editor layout, so per §M19 I mirrored its **hierarchy** (eyebrow → big h1 → muted-mono meta → right-aligned status badge → stacked card sections), not its content. No new keys, no new ds.css.
- **Status badge → pagehead (right-aligned).** Moved the active/inactive `Badge` out of the old `row gap2` below the head into the `.pagehead`'s second flex child (right-aligned via the existing `justify-content: space-between`). Paired it with the activate/deactivate toggle `Button` in a `.row.gap2` so the action sits beside the status it changes (same "actions in pagehead" recipe as M19.2.4). The expert `slug` moved from that old row to a `.muted .mono` meta line under the `.h1` (matching the knowledge-detail head shape).
- **Stat cards promoted to the top.** The two `Stat`s (Voice profiles / Documents — already `.stat` cards via the `Stat` component) now render as the first `.row.gap3` inside the content column, directly under the pagehead, ahead of the Profile/Calendar/voice-link editor sections. The old badge+slug+toggle row was removed (its parts relocated into the pagehead).
- **Editor sections.** `ProfileEditor`, `CalendarEditor`, and the voice-profiles link section were already `.card .card-pad` — verified, no change needed.
- **Tests (`app/experts/[id]/page.test.tsx`).** Added one M19.2.5 case to the existing CalendarEditor suite: asserts the pagehead renders the status badge, slug meta, the Deactivate toggle, and both stat cards with their values (`getAllByText("Voice profiles")` since that label also names the voice-link card). The existing 7 CalendarEditor cases still pass unchanged.
- **Gates.** admin typecheck + `next lint` clean; root `lint:css` + `knip` clean; admin experts suites green (9 tests across both experts pages). No hardcoded hex/px, no new i18n keys, no new ds.css.

### M19.2.4 — experts list page design parity (screenshot 07)
- **Scope.** `apps/admin/app/experts/page.tsx` (`listExperts`/`createExpert` → `AdminExpertSummaryDto`). The page already had the `.pagehead` (eyebrow + h1 + intro), the active-filter `<Select>` + search `Input` + Apply, and the NAME / SLUG / TITLE / STATE / VOICES `Table`. This loop applied the four screenshot-07 layout deltas; no copy/key changes (the task lists "No new keys" and no copy alignment).
- **"New expert" → pagehead (right-aligned).** Lifted the create-form `open` state out of the `CreateExpert` child into the page (`createOpen`); the trigger `Button` now lives as the `.pagehead`'s second flex child (right-aligned via the existing `justify-content: space-between`). `CreateExpert` became a controlled child — it only renders the form (gated on `createOpen`) and takes a new `onClose` prop; the old self-owned "if (!open) return <trigger>" branch was removed.
- **Leading avatar cell.** The NAME `<td>` now wraps a `.row.gap2` cluster: a `.avatar.avatar-sm.tone-{avatarTone(name)}` circle with `avatarInitials(name)` (both from `@expertos/ui`, same helpers as `AdminFrame`/voice-profile-detail) before the display name.
- **New ds.css `.avatar-sm` (32px).** There was only `.avatar-lg` (52px) + ad-hoc per-context sizes; added a reusable `.avatar-sm { width:32px; height:32px; font-size:12px }` next to `.avatar-lg` (mirrors the existing `.side-user-avatar` size) so table-row avatars have a named size. No hardcoded hex; px is inside ds.css (the scale source).
- **Manage → button.** The Manage cell switched from `<Link className="navitem">` to `<Link className="btn btn-subtle btn-sm">` (the repo's established "link styled as button" pattern, e.g. `knowledge/page.tsx`), keeping the `/experts/{id}` href.
- **Lede.** Intro `<p>` class `.muted` → `.lede`.
- **Tests (`app/experts/page.test.tsx`, new — page had none).** 3 cases: pagehead (lede present, form closed until the pagehead `.btn-primary` is clicked → form fields appear); roster table (leading `.avatar.avatar-sm.tone-*`, active `.badge-green` / inactive `.badge-ink`, Manage `a.btn` href); load-error badge. M15.2.1 harness; `mockApi` matches on pathname so the `?limit=50` query is ignored.
- **Gates.** admin typecheck + `next lint` clean; root `lint:css` clean; admin 118 jest + ui 252 jest green (`--runInBand`; the parallel runner times out workers on the OWC mount — the same suites pass in isolation, confirming resource contention not regression). No `knip`/`deadcode` script is configured in this repo. No hardcoded hex/px.

### M19.2.3 — knowledge-draft-detail design parity (screenshot 06)
- **Scope.** `apps/admin/app/knowledge-drafts/[id]/page.tsx` (`getDraft`/`updateDraft`/`draftAction` → `KnowledgeDraftDto`). The page already had the `.pagehead` back-eyebrow + `.h1` + `.muted .mono` lang/from-conversation meta + status `Badge`, the Title `Input` + Content `Textarea`, and the lifecycle action `Button`s. This loop added only the screenshot's info-callout above the Title field.
- **Info-callout (`.panel card-pad row gap2`).** Renders an `ⓘ` glyph (`aria-hidden`) + a `.muted` `<p>` with the static `detail.autoDraftedNote` copy ("Auto-drafted from low-confidence asks with no strong chunk. Edit, then publish to close the gap."). Reuses the existing `.panel`/`.card-pad` ds.css classes (the same callout shape as `concierge-reviews`'s AI-answer panel); no new CSS, no hardcoded hex/px.
- **Honest gating (real data, not faked).** The note describes auto-drafting from conversation gaps, which is only true for drafts promoted from chat — so it renders **only when `draft.conversationId != null`** (the same real signal that drives the existing "· from conversation" meta), never on a manually-authored draft where the claim would be false. Per the baked-in M19 decision, the screenshot's "N asks" count + "View source queries" link are **omitted** (no `KnowledgeDraftDto` field backs them) — static note only.
- **i18n (`dictionaries/knowledge-drafts.ts`).** New `detail.autoDraftedNote` key in EN + VI lockstep; the existing i18n parity test auto-covers it.
- **Tests (`app/knowledge-drafts/[id]/page.test.tsx`, new — page had none).** 3 cases: callout renders with the note + status badge for a conversation-sourced draft; callout omitted for a `conversationId: null` draft; load-error badge. Mirrors the M19.2.1 detail-page test harness.
- **Gates.** admin typecheck + `next lint` clean; admin 115 jest green (`--runInBand`; the parallel runner OOMs/times-out workers in-sandbox — all green serially, confirming resource contention not regression); root `knip` clean.

### M19.2.2 — knowledge-drafts page design parity (mockup 05-knowledge-drafts)
- **Scope.** Minimal parity delta on `apps/admin/app/knowledge-drafts/page.tsx` (`listDrafts` → `KnowledgeDraftSummaryDto`). The page already had the `.pagehead` (eyebrow + h1), the status-filter `<Select>` right-aligned, and the TITLE / STATUS / LANG / FROM CHAT / UPDATED `Table` (title `Link`, status `Badge`). This loop only added the screenshot's `.lede` subtitle under the `.h1`.
- **Lede (`<p className="lede">{t("lede")}</p>`).** Rendered inside the `.pagehead` left column, below the `.h1`. Copy: "Drafts authored directly or auto-created from recurring conversation gaps. Submit → expert review → publish." (EN) / VI lockstep. Reuses the existing `.lede` ds.css class (no new CSS).
- **Omitted illustrative data (baked-in M19 decision).** Screenshot 05's "From chat · N asks" count has no `KnowledgeDraftSummaryDto` field — kept the existing `From chat` / `—` (`fromChatYes`/`fromChatNo`) cell, no faked count. Existing eyebrow ("Drafts") / title ("Conversation-to-knowledge") left as-is — the task delta lists only the new `lede` key; the mockup's wordmark/copy is layout-only per §M19.
- **i18n (`dictionaries/knowledge-drafts.ts`).** New `lede` key added to EN + VI in lockstep; the existing i18n parity test (`src/lib/i18n/i18n.test.tsx`) auto-covers it (green).
- **Gates.** admin typecheck + `next lint` clean; admin 109 jest green (`--runInBand`; the parallel runner OOMs/timeouts workers in-sandbox, all green serially — the previously-"failing" funnel/experts/knowledge suites pass in isolation, confirming resource contention not regression). No hardcoded hex/px, no new ds.css.

### M19.2.1 — knowledge-detail design parity (screenshot 04, verify+polish)
- **Scope.** `apps/admin/app/knowledge/[id]/page.tsx` (`getDocument` → `KnowledgeDocumentDetailDto`, `versionAction`). The page already matched the screenshot on the `.pagehead` (back-eyebrow `Link` + `.h1` + `.muted .mono` scope·lang·versions + status `Badge`) and the versions `Table` (VERSION / STATUS / CHUNKS / CHANGE SUMMARY / CREATED / ACTIONS) with `publishStatusTone` badges. Two small parity deltas applied.
- **Version-column chip (the screenshot-04 delta).** The mockup stacks a publication-state chip under each version number: green "LIVE" on the published version, gray "DRAFT" on the draft version. The page only rendered the LIVE chip (inline). Changed the version cell to stack (`<div>v{n}</div>` + chip below) and added the ink-toned DRAFT chip — but **only when `version.status === "draft"`**, so an `expert_review`/`archived` version (not in the screenshot) gets no chip and is never mislabeled "draft"; its status `Badge` carries the truth.
- **Back-eyebrow copy.** `detail.back` "← Knowledge" → "← Back to knowledge" (EN) / "← Quay lại Kiến thức" (VI) to match the screenshot's "BACK TO KNOWLEDGE". New `detail.draft` key ("draft" / "bản nháp") for the chip; both EN+VI in lockstep (`dictionaries/knowledge.ts`).
- **Test (new file, page had none).** `app/knowledge/[id]/page.test.tsx` (3 cases): pagehead (back link href + title + version meta + pagehead status badge), version chips (ink "draft" on v3 / green "live" on v2, scoped per-row to avoid the duplicate status-label text), and the load-error path. Uses `setMockParams({id})` + the M15.2.1 harness.
- **Real-data fidelity.** Mockup's "Unit Economics Memo" / J&P branding ignored per §M19; no DTO fields added. No new ds.css (reused `.badge`/`.badge-ink`/`.badge-green`, existing `.pagehead`/`.mono`).
- **Gates.** admin typecheck + `next lint` clean; admin 112 jest green (`--runInBand`; parallel runner OOMs workers in-sandbox); root `knip` + `lint:css` clean.

### M19.2.6 — voice-profile-detail design parity (screenshot 09)
- **Scope = warning treatment only.** `apps/admin/app/voice-profiles/[id]/page.tsx` already shipped the M13.5 head row the screenshot wants: right-aligned Approve (primary) + Request-changes (ghost) sign-off `Button`s (driven by `ACTIONS[status]`), the `.avatar avatar-lg tone-*` identity, the `publishStatusTone` status `Badge` + amber "Awaiting your sign-off" badge, and the `.muted .mono` lang/example meta. Verified each against the mockup; no change needed.
- **Reputation warning → warm amber callout with a ⚠ glyph.** The one real delta. Was a plain white `.card card-pad` with an amber `Badge` title + `.muted` body. Restyled to the shared `.msg-notice tone-amber` card (amber-50 bg + amber border, the same surface the web chat's `ChatStateNotice` uses) with a `.msg-notice-head` = ⚠ warning-triangle inline SVG (`.consult-card-icon`, the project's amber inline-flex icon idiom, `currentColor`) + `.msg-notice-title`, and the body as `.msg-notice-body`. Both existing `detail.warningTitle`/`detail.warningBody` keys kept (no new i18n keys per task).
- **No new ds.css** — reused `.msg-notice`/`tone-amber`/`.msg-notice-head`/`.msg-notice-title`/`.msg-notice-body` + `.consult-card-icon`. The task's `.bar .warn` mention is a progress-bar fill, not a callout; the amber notice card is the right surface for a warning paragraph and matches the screenshot.
- **Test (new file, page had none).** `app/voice-profiles/[id]/page.test.tsx` (1 M19.2.6 case): the warning text resolves inside a `.msg-notice.tone-amber`, both sign-off buttons + the awaiting badge render for `expert_review`, and the `.avatar.avatar-lg` is present. `setMockParams({id})` + M15.2.1 harness; mocks `GET /voice-profiles/:id`.
- **Real-data fidelity.** Mockup "J&P GLOBAL" branding / sample voice ignored per §M19; no DTO fields added (the structured voice-dimension/do-don't widgets stay deferred — no schema backing, M13.5.3–5.6).
- **Gates.** admin typecheck + `next lint` clean; admin 120 jest green (`--runInBand`; the parallel runner flakes/OOMs workers in-sandbox); root `knip` + `lint:css` clean.

### M19.3.1 — concierge-settings radio cards (screenshot 10)
- **Scope.** `apps/admin/app/concierge/page.tsx` (`getConciergeConfig` / `updateConciergeConfig` → `ReviewConfigDto`). Replaced the single mode `<Select>` with three selectable radio `.card`s and moved Save into the `.pagehead` as the primary action, per the mockup. No API/DTO change.
- **Radio cards = `.card .card-pad` panel → `.label` "Trigger mode" → `[role="radiogroup"]` of three `.verdict-card`s** (the existing selectable-card primitive: hover + crimson `.is-active` 2px border + red-50 bg — exactly the screenshot's active Mode A card; **no new ds.css**, per §M19's baked-in "reuse `.card` with a crimson active border"). Each card is a `<label>` wrapping a native `<input type="radio" name="concierge-mode">` (styled `accentColor: var(--red-600)` — token, not hex) so selection is keyboard-accessible and the radio dot is crimson; `.verdict-card-name` row = radio + `.grow` title + a static metadata `Badge`, `.verdict-card-note` = description.
- **Static per-mode metadata badges** (parallel descriptors, not selection-state): Off → ink `badgeNoTrigger`; Mode A → red `badgeActive` (ties to the crimson active theme — the mockup's solid-dark "ACTIVE" pill has no ds tone, red is the honest closest); Mode B → amber `badgeAwaitingSignoff` **with a dot**, rendered only while `!silentReviewAllowed`. Mode B's radio is `disabled` until the OD#5 sign-off flips `silentReviewAllowed` (server rejects it too — UX gate only); once granted, the awaiting badge drops and the radio enables.
- **Save in `.pagehead`.** Lifted the pagehead into `ConfigEditor` (which owns the save state) so the primary `Button` + green "Saved" `Badge` sit top-right; the parent renders a minimal action-less pagehead during load. The save body still collapses Off→`enabled:false` keeping the last on-mode as the inert stored `triggerMode`. Added `htmlFor`+`id` to the threshold/SLA/cap `Field`s (directive #38 — the old Select-era fields had unassociated labels); wrapped them in a second `.card .card-pad`.
- **i18n (`dictionaries/concierge.ts`, EN+VI lockstep).** Added `modeOffTitle`/`modeATitle`/`modeBTitle` (with the screenshot's "·" separator), `badgeNoTrigger`/`badgeActive`/`badgeAwaitingSignoff`. Reworded `modeHelp.userPrompted`/`.autoSilent` to the mockup's card copy. **Pruned** the now-orphaned Select-era keys (`modeOff`, `modeUserPrompted`, `modeAutoSilent`, `modeAutoSilentPending`, `silentReviewPending`) — confirmed unreferenced (concierge-reviews/concierge-analytics use their own namespaces). i18n parity test green.
- **Test (new file, page had none).** `app/concierge/page.test.tsx` (3 cases): three mode cards + metadata badges + active/checked state + Mode B gated-disabled; Mode B enables and awaiting-badge drops when `silentReviewAllowed`; pagehead Save posts `{enabled:false}` (await the "Saved" badge before inspecting the recorded PATCH — see learning). Each radio carries `aria-label={title}` so its accessible name is deterministic.
- **Gotcha (LEARNINGS #39).** A sync `getByRole("radio", { name })` for the **controlled `checked`** radio (Mode A) intermittently "Unable to find" right after an async `findByRole` for a sibling — the checked input commits a tick later under React 18. Use async `findByRole` for the controlled/checked option.
- **Real-data fidelity.** Mockup "J&P GLOBAL" branding ignored per §M19; no DTO fields added. No hardcoded hex/px; no new ds.css.
- **Gates.** admin typecheck + `next lint` clean; admin 123 jest green (`--runInBand`; the parallel runner flakes/OOMs/serves stale files in-sandbox — DIRECTIVE #40 — all green serially); root `knip` + `lint:css` clean.

### M19.3.2 — concierge-analytics design parity (screenshot 11)
- **Page (`apps/admin/app/concierge-analytics/page.tsx`).** Replaced the three full-width `.h3` stacks (SLA adherence / by trigger mode / by visibility) with **two side-by-side titled `.card`s** in the established `.matrix-foot` 2-up grid (reused from M13.4.4 entitlements footer — semantically identical "two titled info cards"; **zero new ds.css** per §M19). Card 1 = `.eyebrow` "SLA adherence" over the 4 SLA `Badge` chips; card 2 = `.eyebrow` "By trigger mode & visibility" over the trigger-mode **and** visibility chips merged into one panel (the mockup combines them). Badge rows use `.row gap2 wrap`. Moved the block up so it sits directly under the stat row; `byStatus` table / `reviewerVerdicts` / `knowledgeQuality` kept below.
- **Green stat deltas.** Added `delta` + `trend="up"` (green `.d.up`) to two stat cards: **Answered** → new `answeredRate` key ("{rate} answered" = answered/totalRequests, e.g. "92.0% answered"); **SLA met** → reused the existing `slaTracked` ("Tracked: {count}") key. Other three cards unchanged.
- **i18n (`dictionaries/concierge-analytics.ts`, EN+VI lockstep).** Added `answeredRate` + `byTriggerModeVisibility` (combined panel title — needed for screenshot parity since the two old section headers merge into one card). **Pruned** the now-orphaned `byTriggerMode` / `byVisibility` section-header keys (only this page referenced them). `triggerModeBadge`/`visibilityBadge`/`slaAdherence` reused as-is.
- **Test (new file, page had none).** `app/concierge-analytics/page.test.tsx` (4 cases): stat row carries exactly two green `.stat .d.up` deltas in order [answered-rate, sla-tracked] (scoped to `.stat` since the SLA panel renders the same "Tracked: 305" text as a badge chip); two `.matrix-foot > .card` panels each titled by an `.eyebrow`, second card carries both trigger-mode + visibility chips; status/verdict/knowledge sections remain below; unmocked GET → 404 error badge.
- **Real-data fidelity.** "J&P GLOBAL" mockup branding ignored; no DTO fields added (visibility label stays lowercase via `useStatusLabel`, matching the real catalog). No hardcoded hex/px; no new ds.css.
- **Gates.** admin typecheck + `next lint` clean; admin 127 jest green (`--runInBand`; parallel runner flakes/times-out in-sandbox — DIRECTIVE #40); root `knip` + `lint:css` clean.

### M19.3.3 — answers (answer-review feed) design parity (screenshot 12)
- **Page (`apps/admin/app/answers/page.tsx`).** Two small parity edits only — this page was already the house recipe (`.pagehead` → answer-card feed). (1) Model render: `<Badge tone="info">{row.model}</Badge>` → non-interactive `<span className="chip">{row.model}</span>` (the mockup's "GPT-PREMIUM"/"GPT-STANDARD" gray pills). Used a plain `<span>`, **not** the `Chip` component (which is a `<button>`) — the model is static metadata, so a button would be a misleading a11y target; matches the existing non-interactive-chip precedent in `ChatUploadPopover.tsx`. (2) Intro `<p className="muted">` → `<p className="lede">`. The HELPFUL/UNHELPFUL/INSUFFICIENT/confidence `Badge`s + `.mono` timestamp + QUESTION/ANSWER blocks unchanged; no API/DTO change.
- **i18n (`dictionaries/answers.ts`, EN+VI lockstep).** `eyebrow` "Quality" → "M8.5 · Render quality feed" (matches the screenshot's "M8.5 · RENDER QUALITY FEED"; kept the milestone prefix per the funnel convention `M10.2 · …`); `heading` "AI answers" → "Answer review". VI mirrored. No new keys; i18n parity test green.
- **Real-data fidelity.** "J&P GLOBAL" mockup branding ignored; model shows the real value (`gpt-4o-mini`, not the mockup's illustrative "GPT-PREMIUM"); no DTO fields added. No hardcoded hex/px; no new ds.css.
- **Gates.** admin typecheck + `next lint` clean; admin 126/127 jest green serially (the 1 fail = an order-dependent `experts/page` act-warning flake, passes in isolation; parallel runner OOM/SIGKILLs workers in-sandbox — DIRECTIVE #40); answers page has no dedicated test (2 cosmetic edits, lockstep covers the dict); root `knip` + `lint:css` clean.

### M19.3.4 — failed-queries (inspector feed) design parity (screenshot 13)
- **Page (`apps/admin/app/failed-queries/page.tsx`).** Three deltas to match the mockup. (1) Added the per-card **primary "Draft knowledge"** action the page lacked — a `<Link href="/knowledge-drafts" className="btn btn-primary btn-sm">` in a trailing `.row`, mirroring the dashboard low-confidence card (`app/page.tsx:390`) which routes the same way (the draft pipeline is a separate page, not an in-card mutation; no DTO/API change). (2) Intro `<p className="muted">` → `<p className="lede">`. (3) Imported `Link` from `next/link`. The badge-row (insufficient-knowledge amber / model info / confidence ink) + `.label` QUESTION/ANSWER/REASON card feed + Load-more pagination all unchanged.
- **i18n (`dictionaries/failed-queries.ts`, EN+VI lockstep).** `eyebrow` "Quality" → "Content roadmap · Inspector"; `title` "Flagged answers" → "Failed queries"; `intro` → "Answers users flagged as unhelpful or that returned insufficient knowledge — the raw signal for what to write next."; `reason` "Reason" → "Reason flagged"; **new key `draftKnowledge`** "Draft knowledge". VI mirrored (`Lộ trình nội dung · Trình kiểm tra` / `Truy vấn thất bại` / `Lý do gắn cờ` / `Soạn kiến thức`). i18n parity test green.
- **Real-data fidelity.** "J&P GLOBAL" mockup branding ignored; cards keep the real `FailedQueryDto` values + existing `Badge` tones (manifest said "keep the badge-row" — left model/confidence as info/ink `Badge`s rather than the mockup's flat gray pills). No hardcoded hex/px; no new ds.css.
- **Gates.** admin typecheck + `next lint` clean; admin **127/127 jest green** with `--maxWorkers=2` (default parallel runner OOM-crashes workers in-sandbox → 7 spurious "suite failed to run" — DIRECTIVE #40; failed-queries page has no dedicated test, lockstep covers the new dict key); root `knip` + `lint:css` clean.

### M19.3.5 — validation (product-validation scorecard) design parity (screenshot 14)
- **Page (`apps/admin/app/validation/page.tsx`).** Parity edits only — page was already the house recipe (`.pagehead` → per-section `.h3` + `.muted` description + `.row gap1` `Stat` grid). (1) Added a section-level `<div className="eyebrow">` above each of the four `.h3` section headings (Activation / Engagement / WTP / Funnel), so each section reads `eyebrow → question heading → description → stat grid`. (2) Page intro `<p className="muted">` → `<p className="lede">` with the hypothesis phrase wrapped in `<em>`, rendered as `{t("intro")} <em>{t("introHypothesis")}</em> {t("introOutro")}` (split into three keys since `useT` returns a string and can't carry inline markup). No API/DTO change (`getValidationAnalytics` → `ValidationAnalyticsDto` untouched); all 14 `Stat` cards unchanged.
- **i18n (`dictionaries/validation.ts`, EN+VI lockstep).** `eyebrow` "Analytics" → "M10.4 · OD#1 · Go / No-go"; `heading` "Validation scorecard" → "Product validation". Section headings reworded as questions: Activation → "Did new users ask a first question?", Engagement → "Did they come back and keep asking?", WTP → "Are they willing to pay?", Funnel → "Do recommendations turn into booked consultations?". The four old section-heading words became the new `*Eyebrow` keys. `intro` split into `intro` + **new** `introHypothesis` + **new** `introOutro` (drops the cumulative-window nuance from the old intro to match the shorter screenshot copy — that nuance is still carried per-section in `wtpDescription`). New keys: `activationEyebrow`, `engagementEyebrow`, `wtpEyebrow`, `funnelEyebrow`, `introHypothesis`, `introOutro`. VI mirrored ("M10.4 · OD#1 · Tiếp tục / Dừng" / "Kiểm chứng sản phẩm" / question headings). i18n parity test green.
- **Real-data fidelity.** "J&P GLOBAL" mockup branding ignored; no DTO fields added; real aggregates rendered. No hardcoded hex/px; no new ds.css (`.eyebrow`/`.lede`/`<em>` all existing).
- **Gates.** admin typecheck + `next lint` clean; admin **127/127 jest green** with `--runInBand` (default parallel runner OOM/times-out in-sandbox → spurious "suite failed to run" — DIRECTIVE #40; validation page has no dedicated test, lockstep covers the new dict keys); root `knip` + `lint:css` clean.

### M19.4.3 — conversions (expert consultation-conversions) design parity (screenshot 17)
- **Page (`apps/admin/app/conversions/page.tsx`).** Three small parity edits only — page was already the house recipe (`.pagehead` → 3-`Stat` row → `By trigger` `Table` → byResponse/byStatus `Badge` rows → recent `Table`). (1) Right-aligned the "By trigger" RECOMMENDATIONS count column: `<th>` → `<th className="num">` and the count `<td className="mono">` → `<td className="num mono">` (`.table th.num/td.num { text-align: right }` in ds.css — same pattern as `analytics/page.tsx` cost columns). (2) Intro `<p className="muted">` → `<p className="lede">`. No API/DTO change (`getExpertConversions`/`listExperts`, `ExpertConversionsDto` untouched); the byResponse/byStatus/recent breakdowns (a superset of the mockup) left intact per the task note.
- **i18n (`dictionaries/conversions.ts`, EN+VI lockstep).** `eyebrow` "Funnel" → "M8.5 · Your voice → consultations" (renders uppercase via `.eyebrow` CSS, matching the screenshot's "M8.5 · YOUR VOICE → CONSULTATIONS"; milestone-prefix convention per funnel `M10.2 · …`); `title` "Consultation conversions" → "Conversions"; `intro` → "The funnel from conversations held in your voice. Admins can pick any expert." VI mirrored ("M8.5 · Giọng nói của bạn → tư vấn" / "Chuyển đổi" / "Phễu từ các cuộc trò chuyện theo giọng nói của bạn. Quản trị viên có thể chọn bất kỳ chuyên gia nào."). No new keys; i18n parity test green.
- **Real-data fidelity.** "J&P GLOBAL" mockup branding ignored; real `ExpertConversionsDto` aggregates rendered (mockup's "271/29/$8,700" are illustrative). No hardcoded hex/px; no new ds.css (`.num`/`.lede`/`.mono` all existing).
- **Gates.** admin typecheck + `next lint` clean; admin **127/127 jest green** with `--runInBand` (default parallel runner OOM/times-out in-sandbox → spurious "suite failed to run", different suites each run — DIRECTIVE #40; conversions page has no dedicated test, lockstep covers the dict); root `knip` + `lint:css` clean.

### M19.4.4 — recommendation-rules (consultation-trigger editor) design parity (screenshot 15)
- **Page (`apps/admin/app/recommendation-rules/page.tsx`).** Three parity edits, no API/DTO change. (1) Intro `<p className="muted">` → `<p className="lede">`. (2) Left-cell trigger-kind: the info/ink `<Badge>{rule.kind}</Badge>` → a static `<span className="chip">{rule.kind.toUpperCase()}</span>` (renders "KEYWORD"/"THRESHOLD" as the mockup's flat gray pill). Followed the M19.3.3 answers precedent (`<span className="chip">` for a static value) rather than the interactive `Chip` button component, since the kind is a derived label, not selectable. (3) Per-row enable control: the raw `<input type="checkbox">` → the ds.css `.switch` pill (`<span className="switch"><input type="checkbox" .../><span className="track" /></span>`), copying the entitlement-matrix toggle markup (`app/entitlements/page.tsx:417`); wrapper `<label className="row gap1">` → `row gap2` for the wider control. `RuleEditor` logic (keyword/threshold field switch, priority/consultation-type, save/validation) untouched.
- **i18n (`dictionaries/recommendation-rules.ts`, EN+VI lockstep).** `eyebrow` "Consultation funnel" → "When to recommend a consultation" (renders uppercase via `.eyebrow` CSS, matching screenshot's "WHEN TO RECOMMEND A CONSULTATION"). VI: "Phễu tư vấn" → "Khi nào nên đề xuất tư vấn". No new keys (`kind` value is uppercased in-component, not translated — it's a derived enum label). i18n parity test green.
- **Real-data fidelity.** "J&P GLOBAL" mockup branding ignored; real `RecommendationRulesDto` rules + consultation-types rendered (mockup's "acquisition/valuation/multi-unit" keywords illustrative). No hardcoded hex/px; no new ds.css (`.switch`/`.chip`/`.lede` all existing). `Badge` import retained (still used for the load-error / saved / save-error states).
- **Gates.** admin typecheck + `next lint` clean; admin **127/127 jest green** with `--runInBand` (default parallel runner OOM/times-out in-sandbox → spurious "suite failed to run" — DIRECTIVE #40; recommendation-rules page has no dedicated test, lockstep covers the dict); root `knip` + `lint:css` clean.

### M19.4.5 — reconcile design parity (screenshot 16)
- **Page (`apps/admin/app/reconcile/page.tsx`).** Two CSS-class swaps, no structural change (the page already matched the mockup's layout): intro `<p className="muted">` → `.lede`; the "RUN RECONCILE" card heading `<div className="label">` → `.eyebrow` (matches the screenshot's small-caps eyebrow style on the card). Existing `.pagehead` (eyebrow + h1), since-`Input` + primary "Run reconcile" `Button`, result `Stat` row (Polled/Applied/Matched/Already-seen + conditional Skipped), and the unmatched `.h2` `Card` feed all kept as-is. Client (`reconcileBookings`/`getUnmatchedBookings`) + DTOs untouched.
- **i18n (`dictionaries/reconcile.ts`, EN+VI lockstep).** `eyebrow` "Consultations" → "M7.3 · OD#10 · TidyCal" (technical ref — kept identical in both locales, not translated); `heading` "Bookings" → "Reconcile bookings" / VI "Lịch hẹn" → "Đối soát lịch hẹn". No new keys. Lockstep parity test green.
- **Real-data fidelity.** Mockup "J&P GLOBAL" branding + sample numbers (128/6/4/118) ignored; real DTO-driven stats kept. No hardcoded hex/px; no new ds.css (`.lede`/`.eyebrow` existing).
- **Gates.** admin typecheck + `next lint` clean; admin **127/127 jest green** with `--runInBand` (default parallel runner times out in-sandbox → spurious suite failures, DIRECTIVE #40); root `knip` + `lint:css` clean. No dedicated reconcile page test — i18n lockstep covers the dict change.

### M19.4.6 — retention design parity (screenshot 25)
- **Page (`apps/admin/app/retention/page.tsx`).** Two changes, no structural rebuild (the `.pagehead` + ELIGIBLE-NOW card + 5-`Stat` row + Refresh/Run-sweep `Button`s + result card already matched the mockup): (1) added a `<span className="muted">{t("cronNote")}</span>` inside the existing `.row gap2` action row so the "In production a Cloud Scheduler job hits this same endpoint on a cadence." note sits **beside** the buttons (matches screenshot 25 — note to the right of Run sweep); (2) thousands-formatted every `Stat` value via `.toLocaleString()` on both the preview row and the result row (mockup shows `1,902` / `48,310`). `Stat.value` is `ReactNode`, so passing the formatted string is fine.
- **i18n (`dictionaries/retention.ts`, EN+VI lockstep).** One new key `cronNote` (EN cron-cadence note + VI translation). No other copy touched. Lockstep parity test green (13/13).
- **Real-data fidelity.** Mockup "J&P GLOBAL" branding + sample counts (214/1,902/48,310/12) ignored; real DTO-driven `RetentionPreviewDto`/`RetentionSweepResultDto` stats kept. No hardcoded hex/px; reused existing `.muted` / `.row gap2` ds.css (no new CSS). Intro kept as `.muted` (task scoped to cronNote + formatting only; did not convert to `.lede`).
- **Gates.** admin typecheck + `next lint` clean; i18n lockstep 13/13 green; retention page + concierge-analytics + experts suites green in isolation (full parallel admin run flakes one rotating async-timeout suite in-sandbox — OOM under load, DIRECTIVE #40; all 127 pass individually); root `knip` + `lint:css` clean. No dedicated retention page test exists; i18n lockstep covers the dict change.

### M19.5.1 — users design parity (screenshot 20)
- **Page (`apps/admin/app/users/page.tsx`).** Three deltas, no structural rebuild (the `.pagehead` + role `Select` + search `Input` + Apply `Button` + EMAIL/NAME/ROLE/PLAN/JOINED/Manage `Table` already matched the mockup): (1) intro `.muted` → `.lede`; (2) the premium plan now renders as a colored `Badge tone="red"` (crimson, matching the matrix premium emphasis) — only `planKey === "premium"` is badged; `plus`/other keys stay plain text and the `· {status}` suffix stays `.muted` (screenshot 20 shows "Plus · active" plain vs "PREMIUM · active" badged); wrapped the cell children in `.row gap1`; (3) the "Manage" action switched from `<Link className="navitem">` to `<Link className="btn btn-subtle btn-sm">` (button-styled link, same pattern as the experts page M19.2.4) — keeps the `<a>`/link role + href so navigation + the existing `getByRole("link", { name: "Manage" })` test are unaffected.
- **No new i18n keys** (task says "No new keys") — reused `list.intro`, `list.manage`, and the raw `planKey` string in the Badge (Badge text-transforms to uppercase via ds.css `.badge`).
- **Tests (`app/users/page.test.tsx`).** Added "renders the premium plan as a colored badge" — a premium+active user row asserts the `premium` cell carries the `badge` class. Existing "Manage" link/href + free-plan-fallback assertions still pass unchanged.
- **Real-data fidelity.** Mockup "J&P GLOBAL" branding + sample rows ignored; real `listUsers`/`AdminUserSummaryDto` kept. No hardcoded hex/px; reused existing `.lede`/`.row gap1`/`btn btn-subtle btn-sm`/`Badge` ds.css (no new CSS).
- **Gates.** admin typecheck + `next lint` clean; users suite 9/9 green; full admin suite 128/128 green with `--runInBand` (the default parallel run flakes rotating async-timeout suites in-sandbox — OOM under load, DIRECTIVE #40; all pass individually/in-band); root `knip` + `lint:css` clean.
